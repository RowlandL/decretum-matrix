# Decretum Matrix court open

Order: `SKILL < startup < {profile,dossier}`; then child acceptance before
business. No preview; fresh acceptance reads no past tasks/memory and performs
no source/install/history/memory scan.

Public MCP validates/status; public `decretum-matrix` CLI mutates. PATH fallback
is its npm-prefix executable, never internal Python; host delivers.

Native flow: `admit → native-request → host_invocation → native-capture → returned office command/request → preload-ack`.
After start, child emits exact `child_acceptance`, notifies its direct superior,
and waits; relayed ACK success permits business. Missing evidence stays PENDING.
[Path/ACK details](sections/court-office-name-profile-skill-binding.md).

Load later volumes from `SKILL.md`. Closeout pointer:
`references/court-closeout-validation.md`; require Menxia result, memorial
shard, and archive receipt.
