interface:
  display_name: "Decretum Matrix（诏令矩阵）"
  short_description: "Route local skills through 三省六部."
  default_prompt: "Use $decretum-matrix to route this task through the local capability catalog and 三省六部 workflow."
