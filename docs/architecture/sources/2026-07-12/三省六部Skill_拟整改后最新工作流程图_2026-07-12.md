# 三省六部 Skill 拟整改后最新工作流程图

- 日期：2026-07-12
- 对照基线：`release/beta0.5.9`，commit `040f707`
- 状态：目标设计，尚未实施
- 对照文件：`三省六部Skill_现行整改前工作流程图_2026-07-12.md`
- 方案文件：`三省六部Skill_结果导向与会话分流整体整改方案_2026-07-12.md`

## 1. 拟整改后的完整主流程

```mermaid
flowchart TD
    A["用户最新消息"] --> B{"太子会话分流门"}

    B -->|"CASUAL_CHAT"| C["自然闲聊回应"]
    C --> C1["不开正式任务；不改变在办任务"]

    B -->|"TRIVIAL_DIRECT"| D["轻量直接回答"]
    D --> D1["无工具、无状态变更、无风险时结束"]

    B -->|"TASK_CANDIDATE／AMBIGUOUS"| E["三省限域讨论分流问题"]
    E --> F["中书拟定任务化选项"]
    E --> G["门下审查提问必要性和风险"]
    E --> H["尚书判断答案对执行的影响"]
    F --> I["三省上奏澄清意见"]
    G --> I
    H --> I
    I --> J["太子只问一个最高影响问题"]
    J --> K["用户答复"]
    K --> B

    B -->|"UNCLEAR_RELATION"| L["询问：补充当前任务还是新任务"]
    L --> K

    B -->|"FORMAL_TASK／TASK_CONTINUATION"| M["太子定性：建立结果章程"]
    B -->|"TASK_CORRECTION"| N["暂停旧章程的继续执行"]
    N --> O["按最新旨意修订结果章程"]
    O --> M

    M --> P["中书省：拟制最终结果与验收标准"]
    M --> Q["门下省：前置封驳、范围与风险"]
    M --> R["尚书省：可执行性、资源与统合判断"]

    P --> S["三省上奏"]
    Q --> S
    R --> S
    Q -->|"封驳重拟"| M

    S --> T["太子回奏／按权限取得执行裁定"]
    T --> U["尚书省从最终结果反向拆解差遣"]

    U --> V["吏部：能力与责任主体适任"]
    U --> W["户部：环境、依赖、预算与证据覆盖"]
    U --> X["礼部：用户契约与必要文档"]
    U --> Y["兵部：实施、并行、集成与恢复战术"]
    U --> Z["刑部：风险、负面场景与回归审查"]
    U --> AA["工部：实际营造、集成与运行验证"]

    V --> AB["六部按结果契约向尚书省回奏"]
    W --> AB
    X --> AB
    Y --> AB
    Z --> AB
    AA --> AB

    AB --> AC["尚书省统合为系统级可用结果"]
    AC --> AD{"门下省五级结果复核"}

    AD -->|"最终结果缺失"| U
    AD -->|"闭环失败或发生回归"| AE["兵部／工部重办；刑部复审"]
    AE --> U
    AD -->|"风险越界"| AF["门下封驳；刑部提出整改条件"]
    AF --> U
    AD -->|"证据不足"| AG["户部组织补证；相关六部重验"]
    AG --> AB
    AD -->|"普通文牍缺口"| AH["礼部补齐或裁定 DONE_WITH_CONCERNS"]
    AH --> AD

    AD -->|"五级门禁通过"| AI["史馆记录结果、证据、余险和回退过程"]
    AI --> AJ["太子结诏"]
    AJ --> AK["Done：最终可用结果已经成立的状态投影"]
```

## 2. 太子会话分流子流程

```mermaid
flowchart TD
    A["读取最新消息和当前会话状态"] --> B{"是否有在办正式任务"}

    B -->|"否"| C{"消息是否明确要求产物、分析或动作"}
    C -->|"是"| D["FORMAL_TASK"]
    C -->|"否"| E{"是否只是明确闲聊或简单无工具问题"}
    E -->|"闲聊"| F["CASUAL_CHAT"]
    E -->|"简单问题"| G["TRIVIAL_DIRECT"]
    E -->|"有痛点、愿望或行动可能"| H["TASK_CANDIDATE"]
    E -->|"无法判断"| I["AMBIGUOUS"]

    B -->|"是"| J{"最新消息与在办任务的关系"}
    J -->|"继续办理"| K["TASK_CONTINUATION"]
    J -->|"修改范围、权限或验收"| L["TASK_CORRECTION"]
    J -->|"明显无关闲聊"| M["SIDE_CHAT；保持原任务状态"]
    J -->|"明显是独立任务"| N["NEW FORMAL_TASK"]
    J -->|"关系不明"| O["UNCLEAR_RELATION"]

    H --> P["三省讨论是否任务化"]
    I --> P
    O --> P
    P --> Q["太子只问一个最高影响问题"]
    Q --> R["用户答复后重新分类"]
```

### 会话分流的关键规则

- 闲聊不自动开朝，也不自动取消或完成已有任务。
- 明确任务不因语气随意而被降格为闲聊。
- 只有痛点或设想、但没有明确请求动作时，先取得任务化同意。
- 拿不准时不猜测，只问一个能决定路由的关键问题。
- 能由本地上下文或低成本只读检查确定的事实，不转问用户。
- 用户修改范围、权限、非目标或验收标准时，按 `TASK_CORRECTION` 更新章程并返回三省复议。

## 3. 结果章程形成流程

```mermaid
flowchart LR
    A["明确任务"] --> B["任务类型"]
    A --> C["最终可用结果"]
    A --> D["用户可用场景"]
    A --> E["功能闭环"]
    A --> F["不退化要求"]
    A --> G["风险边界"]
    A --> H["结果证据要求"]
    A --> I["停止门禁"]
    A --> J["史馆策略"]

    B --> K["太子结果章程"]
    C --> K
    D --> K
    E --> K
    F --> K
    G --> K
    H --> K
    I --> K
    J --> K

    K --> L["三省分别会审"]
```

结果章程不以“完成多少任务”为核心，而以“任务结束后用户真正获得什么”为核心。

## 4. 六部结果契约流程

```mermaid
flowchart TD
    A["尚书省收到批准后的结果章程"] --> B["从最终结果反向拆解"]
    B --> C["确定每个步骤对最终结果的贡献"]
    C --> D["确定交付物、状态或能力"]
    D --> E["绑定验收标准和结果证据"]
    E --> F["标注回归、风险、停止和回退条件"]
    F --> G["生成六部差遣"]

    G --> H["六部／工坊办差"]
    H --> I["每部提交结果贡献和证据"]
    I --> J["尚书省检查依赖和冲突"]
    J --> K{"能否形成系统级结果"}
    K -->|"否"| B
    K -->|"是"| L["形成尚书统合奏报"]
    L --> M["送门下五级结果复核"]
```

六部任务全部关闭只代表分项流程结束；只有尚书省成功统合并通过门下复核，才代表系统级结果成立。

## 5. 门下省五级结果复核子流程

```mermaid
flowchart TD
    A["收到尚书省统合奏报"] --> B{"第一级：最终可用结果是否存在"}
    B -->|"否"| B1["退回尚书省／工部；不得 Done"]
    B -->|"是"| C{"第二级：功能是否闭环且不退化"}

    C -->|"否"| C1["兵部／工部重办；刑部复审"]
    C -->|"是"| D{"第三级：风险边界是否可接受"}

    D -->|"否"| D1["门下封驳或 BLOCKED"]
    D -->|"是"| E{"第四级：是否有直接、可信、新鲜的结果证据"}

    E -->|"否"| E1["PARTIAL／NOT_VERIFIED；组织补证"]
    E -->|"是"| F{"第五级：文牍和单项任务是否一致"}

    F -->|"普通缺口"| F1["礼部补齐或 DONE_WITH_CONCERNS"]
    F -->|"安全／强制记录缺口"| D1
    F -->|"通过"| G["门下裁定 PASSED"]

    G --> H["史馆实录"]
    H --> I["太子结诏／Done"]
```

### 门下裁定顺序

```text
最终可用结果
> 功能闭环且不退化
> 风险边界
> 可验证证据
> 文档与单项任务完成
```

这里的优先级表示：低层证据不能替代高层事实。风险门仍是全程硬约束，不意味着可以先越过风险完成实现。

## 6. 拟整改后的状态语义

正式运行时主链保持：

```mermaid
stateDiagram-v2
    [*] --> ConversationGate

    ConversationGate --> CasualReply: CASUAL_CHAT
    ConversationGate --> DirectAnswer: TRIVIAL_DIRECT
    ConversationGate --> ClarificationPending: TASK_CANDIDATE / AMBIGUOUS / UNCLEAR_RELATION
    ClarificationPending --> ConversationGate: 用户答复
    ConversationGate --> Pending: FORMAL_TASK / 已确认任务化

    Pending --> Taizi
    Taizi --> ThreeDepartments
    ThreeDepartments --> ThreeDepartmentsPetition
    ThreeDepartmentsPetition --> TaiziReply
    TaiziReply --> ShangshuDispatch
    ShangshuDispatch --> SixMinistries
    SixMinistries --> Workshops
    Workshops --> MenxiaReview

    MenxiaReview --> ShangshuDispatch: 结果或闭环失败
    MenxiaReview --> ThreeDepartments: 章程或语义需重拟
    MenxiaReview --> Partial: 证据不足
    MenxiaReview --> Blocked: 风险越界
    MenxiaReview --> ShiguanRecorded: 结果验收通过

    ShiguanRecorded --> Done

    CasualReply --> [*]
    DirectAnswer --> [*]
    Partial --> [*]
    Blocked --> [*]
    Done --> [*]
```

说明：

- `ConversationGate` 是正式任务创建前的逻辑门，不一定需要成为持久化运行时状态。
- `CASUAL_CHAT`、`TRIVIAL_DIRECT` 不创建正式 court task。
- `TASK_CORRECTION` 更新结果章程后必须回到三省会审。
- `Partial`、`Blocked` 可以采用独立状态或结构化 closeout status，最终实现时需要结合兼容性设计决定；无论采用哪种存储方式，都不得被映射成 `Done`。
- `Done` 必须经结构化完成命令产生，不能由自由文本 evidence 直接进入。

## 7. 结果证据与控制面证据

```mermaid
flowchart LR
    A["办差产生证据"] --> B{"证据作用域"}
    B -->|"result_plane"| C["用户结果、端到端行为、不退化、目标状态"]
    B -->|"control_plane"| D["队列、gate、计数、文档、史馆、任务关闭"]

    C --> E["可参与最终结果验收"]
    D --> F["只能证明过程受控或提供辅助支撑"]
    F --> G["不得单独满足 Done"]
    E --> H["与闭环、风险和门下裁定共同决定完成"]
```

## 8. 拟整改后的结诏关系

```mermaid
flowchart LR
    A["真实结果成立"] --> B["门下五级复核通过"]
    B --> C["史馆记录结果与证据"]
    C --> D["太子回奏：用户现在能用什么"]
    D --> E["Done"]

    F["仅任务完成"] -.->|"不足"| E
    G["仅 gate 通过"] -.->|"不足"| E
    H["仅文档闭合"] -.->|"不足"| E
    I["仅史馆 checkpoint"] -.->|"不足"| E
```

## 9. 与整改前流程图的对照点

| 对照项 | 整改前 | 拟整改后 |
| --- | --- | --- |
| 会话入口 | 太子分类原则分散在规范文本中 | 独立会话分流门和结构化分类 |
| 闲聊 | 可跳过正式流程，但边界不够集中 | `CASUAL_CHAT`，不开任务且不改变在办任务 |
| 可任务化意图 | 归入一般非明确任务澄清 | `TASK_CANDIDATE`，先询问是否正式任务化 |
| 在办任务关系 | 主要依赖自然语言理解 | 明确续办、修正、旁支闲聊、新任务和关系不明 |
| 澄清 | 每轮一个问题 | 保留一问一审，并优先解决任务化和任务归属 |
| 太子定性 | 语义章程 | 加强为以最终可用结果为核心的结果章程 |
| 尚书分派 | 按职责和依赖派遣六部 | 从最终结果反向拆解，每份差遣绑定结果贡献 |
| 门下复核 | 完整性、风险、证据和漂移复核 | 固定五级结果优先顺序及失败回退路径 |
| Done | 文档要求严格，但运行时只要求非空 evidence | 结构化结果门通过后才能原子化完成 |
| 史馆 | 记录正式过程和证据 | 明确后置记录已成立结果，不能反向制造完成 |

## 10. 本文件状态

本图展示拟整改后的最新目标流程，不代表当前 skill 或运行时已经具备这些状态、字段和门禁。生成本文件时未修改 `SKILL.md`、governing references、运行时代码、测试、活动安装副本或史馆数据。
