# beta1.1.0 本地实施与验收结果

beta1.1.0 本地候选完成 T01–T09 修复与验收。验证源码为 `b938188ee825026eef1d5dd524033f47f5ce896d`，
分支 `release/beta1.1.0`；门下终审为 `PASSED_WITH_CONCERNS`。

## 修复结果

| 任务 | 已验证结果 |
| --- | --- |
| T00 | 全新本地会话接收，保留批准文档，建立 goal；三省分别履职，尚书组织必要六部 |
| T01 | 真实安装投影与公开入口闭合，repository-only 冲突负例仍拒绝 |
| T02 | 保留太子、三省、尚书统合、serial_inline 与 P00 语义，九署预载符合 20 KiB 上限 |
| T03 | 四处检查实现定位与临时 Git 发现边界修复，保留原拒绝断言 |
| T04 | 默认读取权威编号索引，有效 session 跨日复用，损坏分配仍拒绝 |
| T05 | 定向 Git 提交、文件与索引失败恢复、持久回执和原交易提交证明 |
| T06 | 互补事实转待审、未来记录不替代当前记录、拒绝／部分失败／NOOP 正确传播 |
| T07 | 路径不污染内容分类，混合肯定／否定保留双方证据与 review/conflict |
| T08 | 会办绑定真实发件署与当前有效接收署；真实归档保存余险；缺 producer 证据不得完成 |
| T09 | 版本、SBOM、SKILL 身份、包清单一致；完整回归与实际安装后验收通过 |

## 验收证据

- **38 组当前必需检查通过**，均绑定同一验证源码提交。
- 新隔离 home 的 `.agents`、`.codex` 两个根安装成功，共 890 文件。
- 两根分别完成 CLI 版本／JSON 帮助、MCP 发现与只读调用；MCP 工具数为 12。
- 两根九署预载最大 **19394 / 20,480 字节**。
- 重复安装通过；投影写入后的受控失败回滚保留安装文件字节。
- 门下对会办身份、失效／未准入接收方、缺失 producer 两字段、错 digest、假路径及实际归档篡改作定向复验。

完整命令、退出码、源码绑定和本机原始路径见
[结构化证据](beta110-20260905/evidence-summary.json)。原始证据根为
`D:\project\.staging\dm110-20260905`，本轮只使用隔离 home/shared/runtime 与临时 Git。
源码预算首次检查遇到 7 个编译缓存；缓存已移至 `cache-b938188` 保留，同提交重验通过。

## 保留事项

- 历史 `check_court_preload_semantics.py` 保留 4 项既存 RED、18 项行为通过；与未修改
  beta1.0.9 完全一致，未登记为当前 release gate。门下裁定
  `HISTORICAL_A02_DIAGNOSTIC_NONBLOCKING`，本次未把它改成 GREEN。
- 本轮部分新逻辑任务复用了同一原生宿主。实际新准入、followup 与回奏已有记录，
  产品对应 start 投影仍标 `START_PROJECTION_UNSUPPORTED/runtime_degraded`，未冒称新 spawn。
- 活动安装、宿主全局配置和外部发布不在本轮交付范围；本结果是本地候选与隔离验收。

## 开发与交接依据

产品起点 `0003b3800cd245abf870a82ff464a802cc0faeaf`；原 1.0.8/1.0.9 身份保留。
批准文档已保存于 `069c3a0`，实现候选与版本对齐提交为 `019eaab`、`d17ff9e`，
最后证据闭环修复为 `b938188`。根仓项目映射提交为 `29a6ee5`。

所有实际模型记录均已核验：主线程 Astra ultra，子实例 Terra ultra。发生宿主关闭后，
已失去 handle 的旧实例未被声称仍在执行；恢复后的官署保持同一任务、职责与文件边界。
原准入半写失败保留前像，并以一次明确修复取消占位；未补造原准入事件。

接收事实见[交接回执](../handoffs/2026-09-05-beta1.1.0-handoff-receipt.json)。
记忆裁定：SKIP。
