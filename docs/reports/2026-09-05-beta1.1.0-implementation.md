# beta1.1.0 本地实施与隔离验收

状态：CANDIDATE_PENDING_INTEGRATION。T01–T07 分项检查通过；T08 已写入真实归档适配与终审来源校验，等待剩余验证回奏及统一验收。

## 接收与开发基线

- 本轮为全新本地会话 `01a0715e-8957-7910-9800-eb19ed5d1437`，非 fork。
- 产品基线：`0003b3800cd245abf870a82ff464a802cc0faeaf`，原分支 `release/beta1.0.9`。
- 现有本地检出切换至 `release/beta1.1.0`，未创建额外工作树；根仓映射为
  `codex/decretum-matrix/release/beta1.1.0`，由 repo-control 刷新分支目录。
- 根仓项目映射提交 `29a6ee5`；批准的架构、计划、历史证据与交接文档提交 `069c3a0`。
- 接收时根与产品索引均为空；20 份既有文档中仅父任务获准更新的交接回执变化，
  其余文档核对 SHA-256 一致。历史 1.0.8/1.0.9 分支与发布身份保留。
- 用户授权：super / parallel / native。实际原生会话 turn_context 核对根为
  `gpt-6-astra / ultra`，中书、门下、尚书为 `gpt-5.6-terra / ultra`；后续实例逐一续核。
- 原生 goal 已建立为 active，未设置 token budget。

父任务完成的接收核验见 [交接回执](../handoffs/2026-09-05-beta1.1.0-handoff-receipt.json)。
本机原始证据位于 `D:\project\.staging\dm110-20260905`，包含 `baseline.json`、
`baseline.patch`、`model-evidence.json`、语义 checkpoint/verify 与 `decree-open.json`。
本轮实际 court 状态仅存放在该目录的 `court-state`，未访问活动史馆；语义回执只证明
对应机器门禁，实际官署履职以原生派遣、署名回奏及其工作证据为准。

## 执行责任与依赖

中书完成拟旨和验收拆解，门下完成前审，尚书完成可行性及互斥写集规划；太子给出
`execution_gate=APPROVED`。三省为实际独立实例，六部由尚书差遣。

| 任务 | 责任 | 结果状态 |
| --- | --- | --- |
| T01 安装投影与公开入口 | 工部 | 分项通过；真实隔离安装两根各 445 文件 |
| T02 预载预算 | 吏部 | 分项通过；九署最大 19,394 / 20,480 字节 |
| T03 检查模块定位 | 工部本体 | 三个检查器定位修复，分项通过 |
| T04 编号默认入口及跨日复用 | 礼部 | 编号与历史兼容检查通过 |
| T05 账册 Git 事务 | 户部 | 定向提交、失败恢复、原交易回执证明通过 |
| T06 冲突与失败传播 | 户部，依赖 T05 | 时效、结构化冲突、拒绝／部分失败／NOOP 检查通过 |
| T07 分类输入及混合断言 | 刑部 | 内容与路径分离、混合断言保留证据，检查通过 |
| T08 协作与结果证据映射 | 中书／门下定性，尚书组织兵部实现 | 真实归档链与来源校验等待最终回奏 |
| T09 版本、隔离安装与集成验收 | 太子协调，门下五级终审 | 候选集成中 |

T08 初始无生产写集；若涉及 `archive_checkpoint.py`，必须与 T04 单一写者串行。
T03 增补中书实测发现的 `check_court_agent_lifecycle.py` 同类路径错误，保留原断言。
Git 索引、提交、根仓治理映射与最终 release manifest 由太子唯一协调。

本轮另实测发现准入封装缺少 `note` 时已写任务但未写事件的问题，已纳入 T08 的默认值与成对回滚修复。
原失败准入及前像保留在 `orphan-recovery`，由一次明确的本地修复取消占位，没有补造原准入事件。
T03、T06 及追加 T08 使用新逻辑准入与现有原生实例 followup；当前产品不能生成对应的新逻辑
start 投影，证据如实记录 `START_PROJECTION_UNSUPPORTED/runtime_degraded`，不把它记成新 spawn。

## 验收标准与保留边界

旧版真实安装失败仍见 [历史隔离安装报告](2026-09-05-beta108-beta109-isolated-install.md)。
本轮必须以最终真实 manifest 在新的隔离 home 执行 plan/apply，并从两个实际安装根
运行版本、CLI 帮助、MCP 工具发现与只读调用、九署预载；检查重复安装和失败回滚。
原有 32 项源码检查的驱动已复用，并加入受影响的聚焦回归。

活动安装、宿主全局配置、私有史馆正文、下载依赖以及 push/tag/PR/Wiki/npm 发布均
不在本轮范围。候选 metadata 的 `latest_tag` 不是真实 Git tag 或外部发布证明。
最终结果待真实验收证据与门下终审后更新。

额外运行的 `check_court_preload_semantics.py` 是历史 A02 诊断：未修改 beta1.0.9 与当前候选
均为 18 项通过、相同 4 项 RED，且未被当前 release-gates 登记。门下裁定保留
`HISTORICAL_A02_DIAGNOSTIC_NONBLOCKING`；不以它恢复旧 registry 字段，也不把该诊断写成 GREEN。
对照证据在 `preload-contract-comparison`。
