# beta1.1.0 opaque capture, preload and lineage repair

The test task `01a0743b-4c11-7021-b2ad-31f2dfe23df5` stored native spawn
messages as opaque values. The old capture adapter required a plaintext marker
and rejected all three real spawns. It now joins the host call ID, completed
SubAgentActivity and child session metadata. Root thread and session IDs remain
distinct. Opaque plaintext is never decrypted or declared verified.

Native capture proves spawn only. Child readiness also requires installed SKILL
then own profile/dossier reads, followed by the exact bounded child acceptance
JSON in a host child-thread AgentMessage and its adjacent assistant commentary
output. Parent declarations alone cannot acknowledge delivery. Missing evidence
stays PENDING without closing the office. Plaintext native receipts use the same
gate. Active installed aliases are accepted through installation declarations;
source checkouts and npm extraction caches are not accepted read roots.

Installation renders preload identities once. Runtime bootstrap, agent probe,
host resolution and recruitment consume declared identity values, without
rehashing files or the identity JSON. Missing pins require updating the install.
External binaries retain path/version/provenance evidence without fabricated
digest verification. Semantic P00, case and receipt digests are unchanged.
All 14 generated role anchors state SKILL -> own materials -> child acceptance
and superior ack -> business CLI/MCP, with PENDING before acknowledgement.

Installed acceptance also exposed a cached transactional upgrade early return
that skipped native role synchronization. Both upgrade branches now share role
sync against the installed canonical profiles. Role failures preserve the
applied installer transaction and backup evidence as FAIL_PARTIAL_APPLIED.
The local role refresh updated 14 files with a pre-write backup.

The historical two-character code for the review label was IK. Repeated UIK
segments escaped the IKU detector's alphanumeric boundary and hid uncertain
lineage. This defect exists in both examined beta1.0.8 and beta1.0.9 references
(`37df40d`, `0003b38`). New review codes use SREVIEW; existing issued codes remain
unchanged and known legacy patterns carry REVIEW metadata through CLI/MCP and
archive receipts. Uncertain sources cannot automatically repair records.
Placeholder repair uses bound producer evidence, exact preimages, a durable
write-ahead transaction and rollback. No real historical record was rewritten.

Development verification passed: native opaque/read-order 8 groups, lineage
review 16 tests, no-file-rehash and external identity 12 tests, preload semantics
22 checks, native bridge/dispatch, lifecycle and complete court runtime, IKU
repair/taxonomy, unified CLI and fast startup. Both shared and portable native
projections import the new trace module in isolated smoke tests.

Read-only replay of the reported task captured 3/3 host spawns. Its old child
reads/ack evidence remains NOT_VERIFIED. This replay and the development checks
are not fresh, memory-free model acceptance. Installed public CLI/MCP results
are recorded separately in local installation evidence after building this
revision. No push, tag or public release is part of this repair.

The 1.0.9 single recall core, A+B layering, three distinct departments and six
distinct ministries remain the architecture; the 1.1.0 additions are retained.
