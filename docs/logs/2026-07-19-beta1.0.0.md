# beta1.0.0 Final-Stage Source Log

- Branch: `release/beta1.0.0`.
- Baseline: accepted beta0.5.13 source and the activated single-writer handoff.
- Version transition: `beta0.5.13 -> beta1.0.0`, with no intermediate beta0.5.x
  branch, commit, candidate, or release artifact.
- Version identity: VERSION, SBOM, release/package constants, npm-derived
  candidate identity, release-facing documentation, and generated release
  manifest are aligned to beta1.0.0.
- Architecture: the 通用任务治理框架 validates fact, interpretation, ruling,
  action, validation, memory, and presentation relationships without creating
  another state machine or ledger.
- Intake: `court.request_understanding.v1` evaluates goal, usage scenario, key
  requirements, and acceptance criteria before detailed planning; below 95 it
  asks one material question. `RESTATE_CONFIRM` cannot create a formal task;
  confirmation advances to `DIRECT_EXECUTION`, while already-clear requests may
  enter that state directly without forced questions.
- Governance: `three-departments-six-ministries` is the only default official
  治理实现; non-default `direct-review` proves bounded replacement through the
  same 能力与运行适配层 and 呈现层.
- Memory: 史馆 GBrain provides metadata-first advisory recall, preserves source,
  conflict, and applicability, never receives execution authority, and always
  yields to the latest user decree.
- Privacy: `pending_body_access=NO`; no pending/private body is read, hashed,
  moved, deleted, or marked seen.
- Deferred scope: `OFFICE_PACK_Q1_Q8=DEFERRED_TO_QUEUED_POST_A02_HANDOFF`.
- Source acceptance: 34/34 non-Super-CC source gates passed serially, including
  governance, hierarchy, runtime, concurrency, privacy, legal, portability,
  manifests, npm harness, and artifact-builder self-tests.
- Super CC evidence: the eight dedicated Super CC source checks are
  `NOT_RUN/current_session_not_in_terminal_runtime` because this Codex desktop
  session is not running inside the required terminal runtime. The product's
  Super CC contract and release-policy entries remain unchanged.
- External state: 外部发布未授权; push, tag, GitHub Release, npm publication, and
  asset upload are not run or asserted by this source log.
- Completion boundary: work packages A-G and semantic cleanliness are source
  accepted. Candidate creation, local installation, shared Shiguan closeout,
  and project final acceptance remain receipt-gated until their corresponding
  final-stage gates run.

Durable raw evidence is stored outside the repository under
`D:/project/.repo-control/evidence/decretum-matrix/CCR-R2-SHIR-20260714-A02/`.
