# beta1.0.6 阶段执行日志

本文件是 append-only 阶段日志。已有条目不得改写、重排或删除；后续状态只能以新的顺序编号条目追加。本日志不保存 private/pending body、原始对话、凭证、raw runtime payload 或模型自造的诏令/结诏编号。

## Entry 0001 — 恢复点接收

- 日期：`2026-08-03`（Asia/Shanghai）。
- 事件：`RESUME_CHECKPOINT_ACCEPTED`。
- 来源任务：`019fb7f5-0398-7663-a18d-68890fbe444c` 的 handoff/两书承接要求；旧线程本身不作为执行权。
- 当前工作树：`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`。
- 当前分支：`release/beta1.0.6`；基线：`release/beta1.0.5@40ba6c4`。
- 当前游标：`Phase 1/result-recovery`。
- P00：已重新完整加载 `C:/Users/32893/.agents/skills/decretum-matrix/SKILL.md`；尚书、工部、刑部均按需加载各自 profile/dossier。
- 层级：太子只调中书、门下、尚书；尚书调工部与刑部。非三省六部链路结果不得直接采用。
- 外部动作：未安装、未同步 active copies、未 push/tag/PR/release/upload/publish。

## Entry 0002 — 恢复基线证据

- 事件：`BASELINE_GATES_VERIFIED`。
- `python -B scripts/check_court_dispatch_hierarchy.py`：`PASSED`，29 fixtures、58 adapter evaluations、6 invalid manifest cases rejected。
- `python -B scripts/check_p00_semantic_dispatch_context.py`：`PASSED`。
- `python -B scripts/check_court_dispatch_policy.py`：exit `0`。
- `python -B scripts/check_court_office_assignment_binding.py`：exit `0`，`COURT_OFFICE_ASSIGNMENT_BINDING_OK`。
- child-profile required contract 已恢复为六个 `*_sha256` digest；packet/capsule id 不由 fixture 或 builder 伪造。
- `git diff --check` 已对 hierarchy 三文件与 assignment-binding 单文件通过。
- 结论：上述证据建立 Phase 0 的局部绿基线；不代表版本完成、安装完成或可发布。

## Entry 0003 — JSON CLI native-host fixture

- 事件：`JSON_CLI_FIXTURE_ALIGNED`。
- 修改边界：`scripts/check_court_agent_lifecycle.py` 中 `check_office_lifecycle_json_cli()`。
- 实现：复用现有 `load_bridge`、`_native_host_request` 和 `_mint_native_host_receipt`，为 production `office start` fixture 提供合法 spawn receipt；未放宽 production fail-closed 门。
- `python -B scripts/check_court_agent_lifecycle.py`：exit `0`，`COURT_AGENT_LIFECYCLE_OK`。
- `python -B scripts/check_court_runtime.py`：exit `0`，`COURT_RUNTIME_SELF_TEST_OK`。
- `git diff --check -- scripts/check_court_agent_lifecycle.py`：exit `0`。

## Entry 0004 — P0 结果恢复链审查

- 事件：`RESULT_RECOVERY_CONTRACT_REVIEWED`。
- 中书最小合同：`QUARANTINED -> Menxia review -> lawful superior handoff -> target new result -> existing finish -> CAS CONSUMED`。
- 门下条件：原 quarantine immutable；actor/target 权威推导；target 已 admit/start/preload；receipt succeeded 且全字段绑定；state/revision/digest CAS；raw/private 不落盘；wrong actor/target、replay、refusal、stale、privacy 与 success 全链测试。
- 刑部封驳：pre-adapt `source_payload_sha256` 与 normalized digest 分离；host receipt 必须进入 replay ledger；普通 finish 不得绕 recovery lock；scrubbed projection 必须 exact-whitelist 并使用递归 privacy sentinel。
- 刑部新增阻断：quarantine 后 source 不能继续保持可执行 writer。必须拒绝其 report/followup/finish，并释放 active write claim 或进入可证明等价的 quarantine-terminal 状态；该具体语义须先由中书拟制、门下裁定。
- 结果隔离整改：保留初始隔离安全门，但禁止“隔离即终态/丢弃”；合法提取、上级复核、handoff 和新 envelope 才能回到正常流程。

## Entry 0005 — 两书门前暂停

- 事件：`PAUSED_FOR_TWO_BOOKS_REVIEW`。
- 最新指令：先核验并落地 beta1.0.6 计划书、执行书与阶段 log，再按两书固定循环推进。
- 中书计划书目标：`docs/plans/2026-08-03-decretum-matrix-beta1.0.6-remediation-plan.md`。
- 尚书执行书目标：`docs/plans/2026-08-03-decretum-matrix-beta1.0.6-execution-book.md`。
- 本阶段 log：`docs/logs/2026-08-03-beta1.0.6.md`。
- P0 production 写入状态：未开始。`scripts/court_semantic_continuity.py`、`scripts/court_runtime.py`、`scripts/check_semantic_continuity.py` 和 state-runtime reference 均无本轮新 P0 diff。
- 保留基线：hierarchy digest 修复、assignment-binding 现行 reference 断言、JSON CLI native-host fixture。
- Git 索引快照：根索引为空；child 索引为空。根与 child 既有 unstaged/untracked 内容继续保留，不纳入清理授权。
- 下一步：等待中书计划书落地；门下审查两书与本 log。只有门下 `APPROVE` 且 quarantine-terminal/write-claim 语义获中书拟制、门下裁定后，尚书才重新派工部从 RED 开始 Phase 1。
- 后续独立 P0：安装版缺少 shard；当前阶段不得覆盖或降级该整改点。
- 结诏门：当前仅为 `handoff_or_pause` 状态。没有统一 CLI `shiguan archive-checkpoint` 的有效 receipt，不得自造诏令编号、handoff 编号、结诏编号或十四行。

## Entry 0006 — 门下退回与唯一游标纠正

- 事件：`BOOK_REVIEW_RETURNED_P0`。
- 三文件已落地：
  - `docs/plans/2026-08-03-decretum-matrix-beta1.0.6-remediation-plan.md`
  - `docs/plans/2026-08-03-decretum-matrix-beta1.0.6-execution-book.md`
  - `docs/logs/2026-08-03-beta1.0.6.md`
- 门下首轮裁定：`RETURN (P0)`，要求十项修订：计划书不得兼称执行书；三文件统一唯一 cursor；append-only log 只能追加；immutable quarantine core 与独立 recovery head；source 精确 terminal/write-claim；target 全字段重验；review/handoff/consume exact-schema typed receipts；crash journal 与 deterministic event/idempotency；legacy 只读；target result exact-whitelist。
- 唯一 cursor 纠正为：`stage-3/result-recovery-chain`。Entry 0001—0005 保持原样；其中旧 cursor 记载仅为历史快照，不再作为当前恢复游标。
- 中书计划与尚书执行书已按该裁定补入 source=`failed/failed/closed/QUARANTINED`、immutable core/recovery head、target exact binding、typed receipts、三段 crash journal、legacy read-only 与 result exact-whitelist 合同。
- P0 production 状态：继续暂停。未恢复被中断工部，未新建 result-recovery 实现工部，未修改 result-recovery 生产代码。
- 外部动作：未安装、未同步 active copies、未 push/tag/PR/release/upload/publish，未执行史馆正式结诏。
- 下一步：门下复审三文件；只有 `APPROVE` 后，尚书才在同一唯一 cursor 下派 fresh 工部从 Stage 3 RED 开始。

## Entry 0007 — 安装根与 npm 旁路审查

- 事件：`INSTALL_ROOT_AND_NPM_DELEGATED_REVIEW`。
- 证据分类：本条记录 delegated evidence 与后续 RED 合同，不是 current live registry、安装或发布验收。
- 正确 npm 身份：scoped package `@rowlandl/decretum-matrix`，registry=`https://npm.pkg.github.com`。发布阶段须使用正确 scoped 命令实时重验，不能查询 unscoped package 或默认 npmjs registry。
- delegated observation：`2026-08-03` 时 `beta` 仍指向 `1.0.5-beta.0`；发布时必须重新查询 version/dist-tags，不能把本条旧观察当作当前事实。
- 根名纠正：shared primary 固定为 `~/.agents`（复数）；`~/.agent`（单数）仅是笔误，不是产品规则、兼容根或新增根。
- 当前冲突：worktree 中 fixed-five checker/sync 与 `INSTALL-PROMPT.md` 的 fanout-forbidden/current-tool-only 规则不一致。该冲突未整改前，不得执行 active-root write。
- 统一方向：root selection 必须 receipt-driven。`~/.agents` 为共享主根；current-tool root 需 verified host/install receipt；其余工具根与 Qoder 只在最新用户明确目标下逐根 opt-in，并逐根绑定 provenance、preimage、backup/rollback 与 post-write proof。五根+Qoder 不得作为默认 fanout。
- 必需 RED：receipt/provenance/release-gate/package-mutator、错误 package scope/registry、旧 beta、未授权 fanout、Qoder 未 opt-in、root alias/physical identity mismatch、candidate hash 后被改写。
- proxy `502`：单独记录为外部 runtime/network 错误；不作为本机源码、包、安装根或同步状态的正反证据。
- 当前执行状态：P0 production 继续暂停，等待门下复审两书与 append-only log；未安装、未同步、未发布。

## Entry 0008 — 声明分类纠正

- 事件：`DECLARATION_CLASS_CORRECTION`。
- append-only 边界：Entry 0001—0007 一字未改；本条仅追加对 Entry 0007 的权威解释。
- Entry 0007 中的 delegated npm snapshot、scoped package/root identity、fixed-five/INSTALL-PROMPT 冲突判断、receipt-driven root selection、selected-root fanout、package mutator 和 RED 目标，统一按 `[PLANNED_UNVERIFIED]` 解释；它们不是当前远端事实或已实现能力。
- `[CONTROL_PLANE]` 仅包括：发布前必须 live 重验；未验证时禁止误报 current beta/package/root 状态；没有本机可重复证据时禁止用 proxy `502` 改变本机归因或验收结论；P0 门未通过时保持停止。
- 本纠正不提升任何 capability claim，不授权安装、同步、发布或外部写入。
- 当前唯一 cursor 仍为 `stage-3/result-recovery-chain`；P0 production 继续暂停，等待门下复审。

## Entry 0009 — 两书批准与 Stage 3 恢复门

- 事件：`BOOKS_APPROVED`。
- 门下最终裁定：`APPROVE`。
- 批准文件：
  - `docs/plans/2026-08-03-decretum-matrix-beta1.0.6-remediation-plan.md`
  - `docs/plans/2026-08-03-decretum-matrix-beta1.0.6-execution-book.md`
  - `docs/logs/2026-08-03-beta1.0.6.md`
- 唯一 cursor：`stage-3/result-recovery-chain`；stage 0—2 不回退，Stage 4 不得提前进入。
- P0 恢复条件：尚书省必须重新创建 fresh 工部 direct child；旧中断工部不得恢复写权；fresh 工部完整加载 exact installed skill、工部 profile/dossier、计划书、执行书与本 log。
- 执行顺序：先在 Stage 3 checker 范围落 RED 并回奏预期失败；RED 证据被尚书接收后才可进入 GREEN production write set。禁止以先写生产代码再补测试代替。
- 首批 diff 后，尚书省重新派遣或复用刑部作只读复查；刑部不得写入或直接集成。
- hierarchy：太子/root -> 尚书省 -> 工部/刑部。六部只向尚书回奏，不得直接向太子或用户交付实施结果。
- 本条写入时 P0 production 尚未恢复；未安装、未同步 active roots、未提交、未发布、未执行正式结诏。

## Entry 0010 — Stage 3 RED 候选记录与尚书接收门

- 事件：`STAGE3_RED_CANDIDATE_RECORDED`。
- 继承锚点：task=`beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-3/result-recovery-chain`。
- 规则校正：Entry 0009 的 `BOOKS_APPROVED` 保持不变；本条将两书的暂停文案校正为“RED 候选待尚书接收”，不回退游标、不改写历史条目。
- RED 命令：`python -B scripts/check_semantic_continuity.py --json`；exit=`1`。
- 首错：`SEMANTIC_BINDING_CORE_RED` / `STAGE3_RESULT_RECOVERY_PURE_SCHEMA_CORE_HEAD_IDEMPOTENCY_RED` / `STAGE3_RESULT_RECOVERY_PURE_HELPERS_MISSING`。
- 缺失 helper：`office_result_envelope_json_schema`、`result_quarantine_core_json_schema`、`result_recovery_head_json_schema`、三类 typed receipt schema、`source_result_payload_sha256`、core/head build/validate、target binding、deterministic event、disposition、operation apply，共 15 项。
- 写集事实：本轮 RED 只新增 `scripts/check_semantic_continuity.py` checker；`scripts/court_semantic_continuity.py` 与 `scripts/court_runtime.py` 未产生本轮生产 diff。root/child index 均为 `0`，`pending_body_access=NO`，`git diff --check` 通过。
- 门禁：RED 候选尚未由尚书正式接收；在接收前不得派 GREEN 工部、不得授予 production write set。现有 lifecycle/native-host/unified-cli 基线不替代 Stage 3 接收证据。
- 下一步：尚书接收本条 RED 证据后，再按三省回奏和层级规则派 fresh 工部；失败保持 `stage-3/result-recovery-chain` 并停止。

## Entry 0011 — 三省复核准许尚书接收 RED

- 事件：`THREE_DEPARTMENTS_APPROVED_SHANGSHU_RED_RECEIPT`。
- 中书复核：`APPROVE`，仅准许尚书正式接收 Entry 0010；不授予 GREEN production write set。
- 门下复核：`APPROVE`，确认执行书/计划书已同步为 `ACTIVE_GOVERNED_EXECUTION` / `RED_CANDIDATE_PENDING_SHANGSHU_RECEIPT`；不准工部、刑部或 Stage 4 提前动作。
- 绑定锚点：task=`beta106-local-stage-019fb7f5`；child=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；cursor=`stage-3/result-recovery-chain`；authority=`super`；behavior=`parallel`；runtime=`native`；`fork_turns=none`。
- P00 复带要求：尚书接收回执必须复带真实 capsule=`715caacdf4906119e1793c541b2291b7d6599cbe0822c6d057a93ad8285d1f95`、semantic receipt 指针、Entry 0010 证据摘要和精确 child/root Git 状态；不得用 synthetic capsule 替代。
- 当前机器事实：`python -B scripts/check_p00_semantic_dispatch_context.py` exit=`0`；`python -B scripts/check_semantic_continuity.py --json` exit=`1`，首错仍为 `STAGE3_RESULT_RECOVERY_PURE_HELPERS_MISSING`（15 helpers）；`pending_body_access=NO`；root/child index=`0/0`；`git diff --check` 通过。
- 写集事实：本条仅追加 append-only log；未修改 `scripts/court_semantic_continuity.py`、`scripts/court_runtime.py`、`scripts/court_operation_journal.py` 或 native-host production code；未创建 worktree、branch、mapping、安装、同步、发布或外部动作。
- 下一步：尚书正式接收 Entry 0010 并追加其 receipt；在该 receipt 产生前保持 STOP，不得授予 GREEN write set、派 fresh 工部或推进 Stage 4。

## Entry 0012 — 尚书正式接收 Stage 3 RED

- 事件：`SHANGSHU_ACCEPTED_STAGE3_RED`。
- 接收裁定：尚书正式接收 Entry 0010 的 RED 候选与首错；本条只开启后续 GREEN-A 差遣资格，不声明任何生产修复已完成。
- P00 绑定：task=`beta106-local-stage-019fb7f5`；child=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；cursor=`stage-3/result-recovery-chain`；authority=`super`；behavior=`parallel`；runtime=`native`；fork_turns=`none`。
- capsule：`715caacdf4906119e1793c541b2291b7d6599cbe0822c6d057a93ad8285d1f95`。
- semantic receipt：`parent-context:P00/current`（父级 P00 携带的 opaque pointer；未在 child 文件系统伪造 receipt id/hash）；证据锚点=`docs/logs/2026-08-03-beta1.0.6.md#Entry-0011`。
- RED 证据：`python -B scripts/check_semantic_continuity.py --json` exit=`1`；首错=`SEMANTIC_BINDING_CORE_RED` / `STAGE3_RESULT_RECOVERY_PURE_SCHEMA_CORE_HEAD_IDEMPOTENCY_RED` / `STAGE3_RESULT_RECOVERY_PURE_HELPERS_MISSING`；缺失 15 个 helper，详见 Entry 0010。
- Git 绑定：child_index=`0`、root_index=`0`、child_HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`、root_HEAD=`c509c4e03e320fd691a6de12b69428d43d01680a`；child_status_sha256=`a449b65e9a1f42ad5884553efcca183bf598f5fb7b0e31988c1cf7af175f4c3c`；root_status_sha256=`ae3125e6ad3100f0b0b9f6afee79297ecba6e84070618b8d1b3dfc676aa468dc`；`git diff --check`=`PASSED`。
- 边界：不创建 worktree、branch 或 attached mapping；不安装、同步、发布、提交或推进 Stage 4；`pending_body_access=NO`。
- 下一步：仅按同一 P00 绑定派 fresh 工部 GREEN-A 与刑部只读复查；两者均只向尚书回奏，缺失 host-native receipt 或越界即停止。

## Entry 0013 — Stage 3 GREEN-A 纯层与聚焦门

- 事件：`STAGE3_GREEN_A_PURE_LAYER_VALIDATED`。
- 承接：尚书 Entry 0012 已正式接收 RED；因当前宿主 native child spawn/reuse API 返回 `protocol_mode_unresolved` / `multi_agent_v2_enabled=false`，未伪造工部或刑部 host receipt。本轮按执行书的 `serial_inline` 降级责任继续同一 bounded write set，保持 `runtime=native`，未探测或切换 superCC。
- 纯层变更：`scripts/court_semantic_continuity.py` 新增 exact result envelope whitelist、v2 quarantine core、recovery head、三类 typed receipt schema、canonical source/core/head hash、target binding fields、deterministic recovery event id、legacy disposition 和 idempotent operation helper；`normalize_result_envelope` 拒绝 unknown/private/nested/duplicate fields。
- runtime/fixture 变更：`scripts/court_runtime.py` 在 quarantine transaction 内固定 source 为 `failed/failed/closed/QUARANTINED`、关闭 write claim、写入 v2 core metadata，并拒绝后续 lifecycle；`scripts/check_semantic_continuity.py` 与 `scripts/check_court_agent_lifecycle.py` 更新为 terminal-source 合同；`scripts/check_court_intervention_matrix.py` 为每个 production spawn fixture 绑定 typed native host receipt；read-only binding 使用 `read_scope` 作为 native request write-set 投影。
- 聚焦命令与结果：
  - `python -B scripts/check_semantic_continuity.py --json` -> `ok=true`, `pending_body_access=NO`。
  - `python -B scripts/check_court_agent_lifecycle.py` -> `COURT_AGENT_LIFECYCLE_OK`。
  - `python -B scripts/check_court_runtime.py` -> `COURT_RUNTIME_SELF_TEST_OK`。
  - `python -B scripts/check_court_native_host_dispatch.py` -> `COURT_NATIVE_HOST_DISPATCH=PASS`。
  - `python -B scripts/check_court_intervention_matrix.py` -> `COURT_INTERVENTION_MATRIX_OK`。
  - `git diff --check` -> `PASSED`; root/child index=`0/0`; `pending_body_access=NO`。
- 边界：本条不声明 Stage 3 全链已完成；review/handoff/consume typed receipts、CAS/crash replay、target delivery 和真实六部 host-native 回执仍未通过，唯一 cursor 继续为 `stage-3/result-recovery-chain`，不得进入 Stage 4、安装或发布。
- 下一步：在 native child spawn 通道恢复前保持 `handoff_or_pause`；恢复后由尚书 fresh 工部完成 GREEN-B runtime/recovery transaction，再由刑部只读复查，门下验收后才可阶段交接。

## Entry 0014 — Stage 3 GREEN-B live host recheck remains blocked

- 事件：`STAGE3_GREEN_B_HOST_RECHECK_BLOCKED`。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；child=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；cursor=`stage-3/result-recovery-chain`。
- P00：capsule=`715caacdf4906119e1793c541b2291b7d6599cbe0822c6d057a93ad8285d1f95`；semantic receipt=`parent-context:P00/current`；authority=`super`；behavior=`parallel`；runtime=`native`；fork_turns=`none`。
- exact skill：已读取 `C:/Users/32893/.agents/skills/decretum-matrix/SKILL.md`；本条未读取或触碰 `pending/**` 正文。
- live host 证据：`python -B scripts/agent_runtime_probe.py --format json` exit=`0`，`protocol_config_errors=["protocol_mode_unresolved"]`、`multi_agent_v2_enabled=false`、`selected_protocol=null`、`reserved_spawn_schema_compatible=false`；真实 native child spawn/reuse 通道仍未恢复。
- fixture 与 live 区分：`python -B scripts/check_court_native_host_dispatch.py --json` 仅为本地 bridge fixture，虽 `status=PASS`，不产生本阶段所需真实工部/刑部 host receipt，未据此解除 blocker。
- 已复验聚焦门：`check_semantic_continuity.py --json`=`ok=true`（`pending_body_access=NO`）；`check_court_agent_lifecycle.py`=`COURT_AGENT_LIFECYCLE_OK`；`check_court_intervention_matrix.py`=`COURT_INTERVENTION_MATRIX_OK`；`check_unified_cli.py --json`=`ok=true`；`git diff --check`=`PASSED`。
- 机器状态：root/child index=`0/0`；child 工作树既有 unstaged/untracked 范围保持不变；本条仅追加日志，无 production runtime、operation journal、native-host、安装、同步、提交或外部发布改动。
- 裁定：`HANDOFF_OR_PAUSE_RUNTIME_DEGRADED`；review/handoff/consume typed receipts、CAS/crash replay、target delivery 和真实六部 host-native receipt 仍未证明；唯一 cursor 不变，不进入 Stage 4。
- 下一最小动作：待 live host 返回可验证的 native spawn/reuse receipt 后，由尚书 fresh 工部在同一 P00/bounded write set 开始 GREEN-B；随后刑部只读复查，缺 receipt 或发生漂移立即停止。

## Entry 0015 — Stage 3 host-native blocker corrected

- 事件：`STAGE3_HOST_NATIVE_FALSE_BLOCKER_CORRECTED`。
- 更正范围：Entry 0014 中基于 `scripts/agent_runtime_probe.py --format json` 的 `protocol_mode_unresolved` / `multi_agent_v2_enabled=false` 只证明该仓库探针的配置解析路径存在误判；不得继续作为宿主真实 multi-agent 不可用的 blocker。
- 真实 host-native 证据：本线程原生 `spawn_agent` 成功创建并运行 `/root/host_probe`；子 agent 回报只读 PowerShell 在 cwd=`D:/project` 成功执行，未修改文件，未创建 worktree、branch 或 mapping，未读取 `pending/**`。
- 归因修正：空 `managed_config.toml` 不是本阶段根因；CC Switch / user config 中的 multi-agent 配置不因该空 overlay 被判定失效。后续不得把仓库探针的 false negative 当作真实宿主派遣失败。
- 当前状态：`GREEN_A_PURE_LAYER_PASS / HOST_NATIVE_DISPATCH_PROVEN / GREEN_B_READY`；唯一 cursor 仍为 `stage-3/result-recovery-chain`。
- 边界：本条不声明 Stage 3 typed review/handoff/consume、CAS/crash replay 或 target delivery 已完成；不进入 Stage 4；不安装、同步、提交、发布或外部写入。
- 下一最小动作：由尚书 fresh 工部在同一 child worktree 与 bounded write set 下继续 GREEN-B runtime/recovery transaction；完成后由刑部只读复查 source terminal、target exact binding、typed receipt、crash journal、legacy read-only 和 result exact-whitelist。

## Entry 0016 — Self-blocking config probe repaired

- 事件：`SELF_BLOCKING_CONFIG_PROBE_REPAIRED`。
- 修复：`scripts/agent_runtime_probe.py` 和 `scripts/check_codex_agent_roles.py` 改为只在 `managed_config.toml` 含真实 multi-agent 协议键或非空坏 TOML 时使用 managed overlay；空 overlay、无关 TOML、仅 `[agents] max_depth` 均跳过并读取 `config.toml`。
- 行动规则：配置、记忆或执行书诊断与当前并行 workflow 冲突时，先运行最小 host-native spawn/reuse 验证；验证成功则继续，诊断归为 checker issue；只有真实 host action 失败或最新用户边界才降级/停止。
- 验证：`python -B scripts/agent_runtime_probe.py --format json` 已返回 `effective_config_source=config.toml`、`selected_protocol=v2`、`multi_agent_v2_enabled=true`、`managed_overlay.used=false`；本地回归 `SELF_BLOCK_CONFIG_RESOLUTION_OK` 通过；相关 diff check 通过。
- 边界：未修改 `C:/Users/32893/.codex/config.toml`、`managed_config.toml`、CC Switch DB、worktree/branch/mapping、安装根或 active copies。当前只完成 P0 自锁修复；Stage 3 GREEN-B 仍待后续继续。

## Entry 0017 — 用户裁定 serial_inline 与辅助工具授权落地

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`SERIAL_INLINE_OVERRIDE_RECORDED`。
- 用户最新裁定：当前子智能体状态不佳，暂不考虑子智能体；本阶段由 root 以 `serial_inline` 内联承接尚书/工部/刑部/门下责任，保留官署责任链证据与 `serial_inline` 降级原因。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；child=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-3/result-recovery-chain`；P00 capsule=`715caacdf4906119e1793c541b2291b7d6599cbe0822c6d057a93ad8285d1f95`；semantic receipt=`parent-context:P00/current`；authority=`super`；behavior=`parallel`（宿主 child 通道降级为 `serial_inline`）；runtime=`native`。
- 子智能体回收：`/root/shangshu_green_b`（fork_turns=none 未送达，standing-by）与 `/root/shangshu_green_b_v2`（fork 投递后运行中）均已中断；不再创建新的三省六部 child 实例。
- 辅助工具授权：执行书新增 `3.1 辅助模型工具授权（2026-08-06 用户补充旨意）`，记录 QoderCLI（Qwen3.8-Max/DeepSeek-V4-Pro/DeepSeek-V4-Flash）、Hermes CLI（deepseek-chat/deepseek-reasoner/deepseek-v4-pro/deepseek-v4-flash）与 squad 协作，用途限只读辅助审查，输出仅 advisory。
- 辅助咨询证据：Hermes deepseek-chat 与 Qoder Qwen3.8-Max 两轮只读复核均确认刑部 FAIL 1-4 为真实缺口，并给出最小修复方向与过拟合风险；记录于 squad `aux-review`（任务 `7708c515-cfb0-464a-8484-0b0a6fae36da`，已完成）。
- 本轮写集：仅 `scripts/court_semantic_continuity.py`、`scripts/court_runtime.py`、`scripts/check_semantic_continuity.py`、`scripts/check_court_agent_lifecycle.py`、执行书（仅追加 3.1 节）与本日志（append-only）。
- 边界：未安装、未同步 active copies、未提交、未 push/tag/PR/release，未读取 `pending/**` 正文；child index 保持 `0`，HEAD 未漂移。
- 下一最小动作：内联修复刑部 FAIL 1-4，补真实 typed review/handoff/consume 与 recovery-chain 证据，重跑聚焦门，随后进入门下复核。

## Entry 0018 — 刑部 FAIL 1-4 serial_inline 整改与聚焦门

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`STAGE3_XINGBU_FAIL_REMEDIATION_INLINE_PASS`。
- 承接：Entry 0017 的 `serial_inline` 裁定；root 内联承担尚书/工部/刑部/门下复核责任（宿主 child 通道降级，未使用子智能体）。
- FAIL 1：`result_binding_problems`（`scripts/court_semantic_continuity.py:1634`）对 `office_instance_kind`/`carrier_proof` 仅当 binding 同时具备二者时强制比较，缺失即 quarantine；普通 agent 流不受影响。
- FAIL 2：`_adapt_office_result_envelope`（`scripts/court_runtime.py:8647`）在适配前暂存 `_original_result_envelope` 与 `_source_result_payload_sha256`；隔离路径（`scripts/court_runtime.py:7858`）使用 pre-adapt 原始摘要，仅在记录同时具备 kind+proof 时补 carrier 元数据；`build_result_quarantine_core`（`court_semantic_continuity.py:1401`）与 `build_result_recovery_projection`（:1264）对缺失 kind fail-closed，不再默认 `child_agent`。
- FAIL 3：`validate_result_recovery_head`（`court_semantic_continuity.py:1528`）补 `recovery_id/quarantine_id/operation_id/event_id/created_at` 非空校验；`validate_result_recovery_projection`（:1312）支持 `expected_core` 交叉校验 `semantic_epoch/quarantine_id/source_payload_sha256`；`_validate_recovery_receipt`（`court_runtime.py:6926`）补 `expected_actor/expected_schema` producer 身份强校验。
- FAIL 4：新增 `_journal_preimage_privacy_violation`（`court_runtime.py:3596`），`_result_recovery_commit_locked`（:3632）在写 marker 前对 tasks/events preimage 做私密键/路径扫描，违者 `result_recovery_privacy_gate_failed`；review/handoff/consume/quarantine 事件 `note` 统一经 `scrub_agent_provider_detail`。
- 测试：`check_semantic_continuity.py` 新增 `STAGE3_XINGBU_FAIL_GATES` 与 FAIL 3/FAIL 2 断言；三个 checker 的 result envelope 构造器按记录补 kind+proof（含 `check_court_intervention_matrix.py`，写集按需扩大至该文件）。
- 聚焦门：semantic=`SEMANTIC_BINDING_CORE_PASS`；lifecycle=`COURT_AGENT_LIFECYCLE_OK`；runtime=`COURT_RUNTIME_SELF_TEST_OK`；native-host=`PASS`；intervention=`COURT_INTERVENTION_MATRIX_OK`；unified CLI=`PASS`；`git diff --check` exit=`0`。
- 未闭合：FAIL 5（仓库内无真实 production recovery-chain harness，`review_quarantined_result`/`handoff_recovered_result`/`consume_recovered_result` 未被任何用例调用，真实 typed receipts 缺失；不伪造证据）；FAIL 6（门下复核与 append-only 终条尚未完成）。
- 边界：未提交、未安装、未同步、未发布；child index=`0`、HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；未读取 `pending/**` 正文；辅助工具授权记录于执行书 `3.1`。
- 下一最小动作：由用户/门下裁定后，创建生产 recovery-chain harness（调用真实 runtime 函数产出 typed receipts），再完成 FAIL 6 状态更新与阶段交接。

## Entry 0019 — FAIL 5 闭合与门下复核通过

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`STAGE3_MENXIA_REVIEW_PASS_FAIL1_5_CLOSED`。
- 承接：Entry 0017 的 `serial_inline` 裁定与 Entry 0018 的 FAIL 1-4 整改；root 内联承担门下复核（宿主 child 通道降级，未使用子智能体）。
- FAIL 5 闭合：新增 `scripts/check_stage3_recovery_chain.py`（699 行，schema=`court.stage3_recovery_chain_check.v1`，CONTRACT=`COURT_STAGE3_RECOVERY_CHAIN`）。harness 在隔离 fixture root 真实调用 production `review_quarantined_result`/`handoff_recovered_result`/`agent_finish`（finish 自动触发 consume）与 `consume_recovered_result` 路径；native host receipt 由现有 bridge fixture mint（`nature=bridge_fixture_minted`，明确标注非真实 host delivery）。
- FAIL 5 首跑暴露生产缺陷：`_journal_preimage_privacy_violation`（`scripts/court_runtime.py`）用 `json.loads` 解析整个 events 文件，与 JSONL 格式不兼容，导致所有 recovery commit 以 `result_recovery_privacy_gate_failed:events:undecodable` 失败；已修复为整文件解析失败后逐行容错解析并拆出 `_scan_ledger_preimage` 递归扫描 helper，重跑 PASS。
- 本轮 typed receipts（真实 runtime 产出）：review `RR-D77A47C17EDDDDDE5FAC258E`（actor=menxia, decision=ACCEPT, recovery_revision=1, schema=`court.office.result_recovery_review_receipt.v1`）；handoff `RR-0C24600999E8AB628D0CB870`（actor=shangshu, recovery_revision=2, target_binding_sha256=`5a1cc804…`，native_host_action_receipt_id=`native-host-fe4201099281c3d58e1f2646`）；consume `RR-721329068BB3710AA924142D`（actor=shangshu, recovery_revision=3, target_result_envelope_sha256=`4c738d55…`）。head 链 revision 1/2/3 = `READY_FOR_HANDOFF`→`HANDED_OFF`→`CONSUMED`，previous_head_sha256 链式，quarantine core 不可变，review 幂等重放 `REPLAYED`。
- 门下复核（逐项对照执行书 5.1—5.5 合同）：FAIL 1 `result_binding_problems`（`court_semantic_continuity.py:1634`）13 核心字段强制比对 + kind/proof 成对比较，普通流不受影响，核验通过；FAIL 2 `_adapt_office_result_envelope`（`court_runtime.py:8667`）pre-adapt `_source_result_payload_sha256`/`_original_result_envelope` 暂存与隔离路径使用（`:7861`），5.3 pre-adapt digest 分离满足；FAIL 3 `validate_result_recovery_head`（`court_semantic_continuity.py:1528`）identity 非空 + revision/state/digest/CAS、`validate_result_recovery_projection`（`:1312`）expected_core 交叉、`_validate_recovery_receipt`（`court_runtime.py:6946`）expected_actor/expected_schema 强校验，核验通过；FAIL 4 privacy preimage scan（`court_runtime.py:3596/3624`）递归扫描 tasks/events preimage，本会话修复其 JSONL 解析缺陷；FAIL 5 负向门覆盖 wrong actor（review/handoff）、CAS conflict、legacy read-only、privacy gate、consume-before-handoff、幂等 replay，共 6 负向 + 主链全绿。
- 聚焦门重跑（JSONL 修复后无回归）：semantic=`SEMANTIC_BINDING_CORE_PASS`（29 cases, `pending_body_access=NO`）；lifecycle=`COURT_AGENT_LIFECYCLE_OK`；runtime=`COURT_RUNTIME_SELF_TEST_OK`；native-host=`COURT_NATIVE_HOST_DISPATCH=PASS`；intervention=`COURT_INTERVENTION_MATRIX_OK`；unified CLI=`status=PASS`；recovery-chain=`COURT_STAGE3_RECOVERY_CHAIN=PASS`（`--json` status=PASS）；`git diff --check` exit=`0`。
- 未评估边界（如实披露，不声称已验证）：cross-task-recovery、worktree-thread-source、multi-recovery-batch-consume（harness `not_evaluated`）；三段 killpoint crash 注入未在本 harness 执行（runtime `_result_recovery_commit_locked` 已实现 `PREPARED/TASK_WRITTEN/EVENT_WRITTEN`，语义层 `F-CRASH-003` 已覆盖纯层 crash journal）。
- 写集事实：本轮新增 `scripts/check_stage3_recovery_chain.py`；修改 `scripts/court_runtime.py`（JSONL preimage 缺陷修复，属既有 dirty 文件内修复）；清理 8 月 4 日遗留 `scripts/__pycache__/court_semantic_continuity.cpython-314.pyc`（非本会话产物），重跑后无新 pyc 生成。
- 边界：未提交、未安装、未同步、未发布；child index=`0`、无 staged 变更、HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 未漂移；未读取 `pending/**` 正文；全部命令带 `-B`。
- 裁定：门下复核 `APPROVE`，FAIL 1-5 全部闭合；唯一 cursor 仍为 `stage-3/result-recovery-chain`。
- 下一最小动作：执行书 5.7 状态终更，随后按执行书 §7 完成阶段交接（handoff_or_pause，不创建新 worktree/branch/mapping）。

## Entry 0020 — 阶段交接 checkpoint

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`STAGE3_HANDOFF_CHECKPOINT_ARCHIVED`。
- 执行书 5.7 状态终更：`STAGE3_MENXIA_ACCEPTED_FAIL1_5_CLOSED_HANDOFF_PENDING`；§8 下一步已同步。
- archive receipt（统一 CLI `shiguan archive-checkpoint`，schema=`court.shiguan_archive_checkpoint_receipt.v1`）：receipt_id=`shiguan:SCOSZLSZU3B-20260806-7-DCBB`；court_code=`SCOSZLSZU3B-20260806-7-DCBB`；path=`C:/Users/32893/.agents/court-shiguan/decretum-matrix/references/plan-archives/plan-20260806-stage3-result-recovery-chain-handoff-1.md`；closeout_identity 按 receipt 逐字复制，未自行分配编号。
- handoff 记录：两书=`docs/plans/2026-08-03-decretum-matrix-beta1.0.6-remediation-plan.md` + `docs/plans/2026-08-03-decretum-matrix-beta1.0.6-execution-book.md`；阶段 log=`docs/logs/2026-08-03-beta1.0.6.md`；cursor=`stage-3/result-recovery-chain`；worktree=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；root/child index=`0/0`；脏文件边界=既有 41 项 + 本阶段新增 `scripts/check_stage3_recovery_chain.py` + 修改 `scripts/court_runtime.py`（JSONL 修复）+ 执行书/日志更新；已过命令=全部聚焦门 PASS + `git diff --check` exit=0；首个未过门=无（本阶段门全绿）；未完成风险=M2—M4 未开始、安装/同步/发布全部未执行、not_evaluated 边界（cross-task-recovery/worktree-thread-source/multi-recovery-batch-consume/三段 killpoint crash 注入）；下一条精确命令=创建下一个本机 local conversation task（`environment=local`）进行阶段交接，不创建新 worktree。
- 记忆裁定：`DEFERRED`（非 decree 终态，M2—M4 未开始，不写 durable memory）。
- 边界：未提交、未安装、未同步、未发布、未创建 worktree/branch/mapping；未读取 `pending/**` 正文；`--no-refresh` 最小副作用写入共享史馆 plan-archives。

## Entry 0021 — 执行书 §3.1 缺陷发现与有界修复（CB1-1）

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`EXECUTION_BOOK_SECTION_3_1_RESTORED`。
- 承接：太子准奏按门下裁定（R1—R9）执行有界闭合批 CB1；root 以 `serial_inline` 内联承接尚书/工部/刑部责任（宿主 child 通道按 Entry 0017 降级，本批未使用任何子代理）。
- 缺陷发现（门下 G7/R9）：执行书 L148 交叉引用 `3.1 辅助模型工具授权（2026-08-06 用户补充旨意）`，该节原在执行书全文缺失，与 Entry 0017 L200/L202「执行书仅追加 3.1 节」记载矛盾；锚点（task/branch/HEAD/cursor/capsule/authority/behavior/runtime）无漂移。
- 修复内容：执行书 §3 与 §4 之间补回 3.1 节，内容以 Entry 0017 L200 记载逐字重建：QoderCLI（Qwen3.8-Max/DeepSeek-V4-Pro/DeepSeek-V4-Flash）、Hermes CLI（deepseek-chat/deepseek-reasoner/deepseek-v4-pro/deepseek-v4-flash）与 squad 协作，用途限只读辅助审查，输出仅 advisory；不授予写权与回奏权。
- 刑部只读复查（serial_inline）：插入锚点唯一，全树检索未发现其他引用冲突或新矛盾（若有即 STOP 报首错）；全文 LF、无 BOM；本条写集仅执行书与本日志。
- 边界：未提交、未安装、未同步、未发布；child index=`0`，HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 不变；未读取 `pending/**` 正文。

## Entry 0022 — zero-bypass 语义映射裁定记载（CB1-2 ① / 门下 R3）

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`ZERO_BYPASS_MAPPING_RECORDED`。
- 门下 R3 解释裁定逐字映射：zero-bypass ≡ 无旁路 consume、无越级 handoff、无绕过 finish 通路的集成。
- 锚定条款：执行书 L126「consume 不是旁路 CLI」+ 计划书 L174「越级 handoff、旁路 consume 或任一零字节失败门均停止」。
- check 证据：Entry 0019 六负向门（wrong actor review/handoff、CAS conflict、legacy read-only、privacy gate、consume-before-handoff、幂等 replay）全绿，`COURT_STAGE3_RECOVERY_CHAIN=PASS`。
- 明言：此系门下解释裁定；两书原无 zero-bypass 逐字条款；本条不改两书（方案 B 写集扩张经门下封驳）。
- 边界：本条仅 append-only log 记载，不改两书、receipt 本体或史馆载体；未提交、未安装、未同步、未发布。

## Entry 0023 — archive receipt 产出主体归属记载（CB1-2 ② / 门下 R4）

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`RECEIPT_OWNERSHIP_RECORDED`。
- 归属裁定（门下 R4）：receipt `shiguan:SCOSZLSZU3B-20260806-7-DCBB` 之产出主体=root serial_inline（本主线线程），非子代理产出。
- 逐字引用 Entry 0017 L197 裁定原文：「用户最新裁定：当前子智能体状态不佳，暂不考虑子智能体；本阶段由 root 以 `serial_inline` 内联承接尚书/工部/刑部/门下责任，保留官署责任链证据与 `serial_inline` 降级原因。」
- 绑定：Entry 0019（typed receipts 与门下复核 APPROVE）、Entry 0020（handoff checkpoint 与 receipt 载体逐字一致）。
- 红线遵守：receipt 本体与史馆载体均未改写，仅以本条补归属证据链。
- 边界：append-only log 记载；未提交、未安装、未同步、未发布。

## Entry 0024 — 三项 not_evaluated 显式豁免与风险留存（CB1-2 ③ / 门下 R5）

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`NOT_EVALUATED_EXEMPTION_RECORDED`。
- 门下 R5 显式豁免：以下三项逐字列为合同外披露项——`cross-task-recovery`、`worktree-thread-source`、`multi-recovery-batch-consume`（harness `NOT_EVALUATED` 名单，Entry 0019 L231）。
- 豁免依据：三项不在计划书 L174 M1 验收合同逐项之列，亦与 M2 三子门（lineage/projection/migration）无主题对应，不构成 M2 前置。
- 明言：豁免≠已验证。三项仍无任何执行证据，记入 handoff 风险清单，风险留存至 M4 终审复核。
- 边界：append-only log 记载，不改 harness/两书；未提交、未安装、未同步、未发布。

## Entry 0025 — dirty 新鲜实测口径记载（CB1-2 ④ / 门下 R6）

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`DIRTY_BOUNDARY_MEASURED`。
- 责任人新鲜实测（记载时点）：`git status --porcelain` = 42 行（37 ` M` + 5 `??`）；`git diff --cached --quiet` child/root 均 exit=0，双侧 staged=0。
- 未跟踪 5 项逐字：两书与阶段 log（`docs/plans/2026-08-03-decretum-matrix-beta1.0.6-remediation-plan.md`、`docs/plans/2026-08-03-decretum-matrix-beta1.0.6-execution-book.md`、`docs/logs/2026-08-03-beta1.0.6.md`）、`scripts/check_stage3_recovery_chain.py`、`scripts/check_active_copy_hashes.py`。
- Entry 0020 L243 口径对照：该条以「既有 41 项 + 本阶段新增/修改枚举」表述；本条新鲜实测总数=42，口径差异（枚举增量法 vs porcelain 总数实测）如实记载，不回写 Entry 0020（执行书 L39 append-only）。
- 索引门：无 staged，root/child index=0，索引门不受影响（计划书 L103 仅要求 index 为空）。
- 边界：append-only log 记载；未提交、未安装、未同步、未发布。

## Entry 0026 — R7 循环依赖裁定逐字载入（CB1-2 ⑤，先于 CB1-3）

- 日期：`2026-08-06`（Asia/Shanghai）。
- 事件：`R7_CIRCULAR_DEPENDENCY_VERDICT_RECORDED`。
- 门下 R7 裁定文本逐字：「论断成立。执行书 L180 以「逐根 receipt」为解除 M2 停止之前置，而逐根 receipt（root-selection receipt、逐根 authority/proof）恰为 M2 投影子门产物（计划书 L187），真实安装逐根 receipt 更是 M3 事件（计划书 L198 安装写入须明确授权）——字面读法成环，且与计划自身排序冲突。
口径裁定：M2 入口层面「逐根 receipt」以 RED/GREEN 测试层于隔离 fixture root 产出的 typed 逐根 receipt 证据替代（exact schema、逐根 path identity/authority/provenance/opt-in/preimage/backup-rollback 字段、缺 proof 根零写 fail-closed 演示）；真实安装逐根 receipt 仍为 M3 强制项。
替代仅限入口证据口径，不构成任何真实根写入授权；裁定文本须先于 S2 逐字记载入 log。」
- 绑定：本条为 CB1-3 逐根 typed receipt fixture 的证据口径依据；本批任何动作均不构成真实根写入授权。
- 边界：append-only log 记载；未提交、未安装、未同步、未发布。

## Entry 0027 — CB1-3 安装/发布边界 RED/GREEN（B-8/S2）

- 事件：`INSTALL_RELEASE_BOUNDARY_RED_GREEN_LANDED`。
- 责任链：尚书省总责；工部实施、刑部只读复查均按 Entry 0017 宿主 child 通道降级以 serial_inline 内联承接（无法真实派遣六部子代理），责任主体已在回奏标明。
- 写集：仅新增 `scripts/check_install_release_boundary.py`（CB1-3 授权测试/脚本层文件）；零安装根写入——所有根均为 `tempfile.TemporaryDirectory` 隔离 fixture；`scripts/check_stage3_recovery_chain.py` 本步未改动。
- RED 基线：`python -B scripts/check_install_release_boundary.py --red` → `COURT_INSTALL_RELEASE_BOUNDARY=RED_BASELINE`，执行书 L177 逐字八项场景（1 receipt/provenance 缺失、1b 逐根 provenance 缺失、2 非当前工具 fanout、3 Qoder 未 opt-in、4 root alias/物理身份不符、5 旧 beta 版本、6 错误 scope/registry、7 candidate hash 后 manifest 被改写、8 receipt/写根集不符）在 legacy fixed-five 宽松 stub 下全部 FINDING `*_unprotected`（9 项），exit=1，符合 RED 语义。
- GREEN 目标合同：`python -B scripts/check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS`，exit=0；严格 receipt-driven 评估器逐根校验 exact schema（`court.install.root_selection_receipt.v1` / `court.install.root_authority_proof.v1`）、path identity、host provenance、opt-in、preimage、backup/rollback、post-write proof、authority，全部 9 场景按预期 rejection reason 拒绝。
- R7 口径逐根 typed receipt fixture：缺 proof 根（provenance 置空）→ REJECTED → 安装模拟 `fail_closed_zero_write`，fixture 文件计数前后一致（零写 fail-closed 演示）；部分写入失败 → `partial_write_rolled_back` 且回滚后计数复原；合法计划 → `applied`。
- 聚焦门：`python -B scripts/check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS`。
- 刑部只读复查（serial_inline）：本步 scripts 写集仅 `?? check_install_release_boundary.py`；其余 `M scripts/*` 为既有脏内容，依红线保留未触碰。
- 偏差记载：宿主命令通道转义污染曾致新文件引号受损，已在本步内修复并复编译通过；`python -m py_compile` 曾强制写出 `scripts/__pycache__`（无视 -B），已即时删除，抽查 pyc=0；本 Entry 首版遭宿主反引号转义吞并，已整条重写（重写对象为本步新条目自身，非旧条目回写）。
- 抽查：HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 未动；child staged=0；CHILD 递归 pyc/pycache=0；dirty=43（42 既有+新 checker 1）。
- 本条不构成安装、同步、发布授权；真实安装逐根 receipt 仍为 M3 强制项（Entry 0026 R7 口径）。

## Entry 0028 — CB1-4 三段 killpoint 负向用例（G1/R2）

- 事件：`KILLPOINT_RECOVERY_NEGATIVES_LANDED`。
- 责任链：尚书省总责；工部实施、刑部只读复查均以 serial_inline 内联承接，原因同 Entry 0027。
- 写集：仅 `scripts/check_stage3_recovery_chain.py`（授权写集）于现有隔离 fixture root 内新增 `_killpoint_case`/`_test_killpoint_recovery` 与一行调用；生产 `court_runtime.py` 未改动；未新增文件。
- 方案 A 可行性：生产 `_result_recovery_commit_locked` 已内置三段 marker 与 `SimulatedResultRecoveryCrash` 注入，`recover_result_recovery_operation` 提供 ROLLBACK/FINALIZE/REPLAYED；harness 仅追加调用该 API 的负向测试，技术可行，未走豁免路径。
- 三段负向用例（于隔离 fixture root 内 PREPARED/TASK_WRITTEN/EVENT_WRITTEN marker 间隙模拟中断）：
  - `killpoint_prepared`：review 于 PREPARED 崩溃 → `recover` 返回 `ROLLBACK`，tasks/events 字节恢复 preimage，零第二 head/receipt，正向 review 重跑干净提交；
  - `killpoint_task-written`：TASK_WRITTEN 崩溃后再截断 tasks.json（模拟受损账本）→ `recover` 仍 `ROLLBACK` 恢复 preimage，零第二 head/receipt，前向路径干净；
  - `killpoint_event-written`：EVENT_WRITTEN 崩溃 → `recover` 返回 `FINALIZE`（前向完成）：恰 1 head（`READY_FOR_HANDOFF`）、恰 1 receipt、事件恰 +1；再次 `recover` 返回 `REPLAYED` 且不写第二事件。
- 门：`python -B scripts/check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS`，exit=0；JSON 证据含 `killpoint_prepared`/`killpoint_task-written`/`killpoint_event-written` 三段。
- 刑部只读复查（serial_inline）：本步 scripts 仅该文件变更（保持原 untracked 状态）；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 未动；staged=0；pyc=0。
- 偏差记载：首跑曾因用例实例 ID 含下划线违反 role-prefix 字符集报 `office_instance_id_not_role_prefixed`，已本步内以 slug 归一修复；属同一步自纠，非门结论漂移。
- cursor `stage-3/result-recovery-chain` 保持不推进；本条不构成进入 M2 的依据。

## Entry 0029 — CB1 闭合批完成记载（CB1-5）

- 事件：`CB1_BATCH_CLOSED`。
- 责任链：尚书省总责；工部/刑部责任均以 serial_inline 内联承接（Entry 0017 宿主 child 通道降级），各步责任主体与原因见 Entry 0021—0028 与本批回奏。
- CB1-1（G7/R9）：执行书补回 `3.1 辅助模型工具授权`（Entry 0021）。
- CB1-2（R3/R4/R5/R6/R7）：zero-bypass 语义映射、receipt 归属、三项 not_evaluated 豁免、dirty 实测口径、R7 循环依赖裁定逐字载入（Entry 0022—0026，R7 先于 CB1-3）。
- CB1-3（B-8/S2）：`scripts/check_install_release_boundary.py` RED/GREEN，`COURT_INSTALL_RELEASE_BOUNDARY=PASS`，零安装根写入（Entry 0027）。
- CB1-4（G1/R2）：`scripts/check_stage3_recovery_chain.py` 三段 killpoint 负向用例，`COURT_STAGE3_RECOVERY_CHAIN=PASS`（Entry 0028）。
- CB1-5：执行书 `5.7 当前状态与停止门`/`8 下一步` 终更，逐字载入 review=`RR-D77A47C17EDDDDDE5FAC258E`、handoff=`RR-0C24600999E8AB628D0CB870`、consume=`RR-721329068BB3710AA924142D`、archive=`shiguan:SCOSZLSZU3B-20260806-7-DCBB`（计划书 L233 字面满足）。
- 边界复核：全程未 stage/commit/push/tag/PR/release/install/sync，未创建/切换 worktree/branch/mapping，未读 pending/** 正文，Python 入口均带 `-B`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 钉定未动；root 与 child 索引保持 0。
- 授权写集符合性：执行书（§3.1 补回、§5.7/§8 终更）、阶段 log append-only（Entry 0021—0029）、`scripts/check_stage3_recovery_chain.py`、CB1-3 新增 `scripts/check_install_release_boundary.py`（隔离 fixture，零安装根写入）；未触碰授权外文件。
- cursor `stage-3/result-recovery-chain` 全程未推进、未进入 M2；后续由门下省依任务 6 对 CB1 终验并出具最终验收裁定。

## Entry 0030 — 正确性审查 M1/M2 有界修复与复验（任务 10）

- 日期：2026-08-06（Asia/Shanghai）。
- 事件：CORRECTNESS_FAIL_BOUNDED_FIX_CLOSED。
- 责任链：尚书省辖下有界修复执行者（任务 ID=10）；实施与只读复查均以 serial_inline 内联承接，原因同 Entry 0017 宿主 child 通道降级（无法真实派遣六部子代理），责任主体=本任务执行者（Felix，serial_inline）。
- 审查报告指针：C:\Users\32893\.qoder\cache\experts\614a5346-1482-477b-b1cf-c15b11b72c12\agents\8add71c7-478c-4180-bba2-f4e944b23d9a.output（正确性审查 Ryan，对闭合批 CB1 两 checker 出具 CORRECTNESS_FAIL，Major 1 项 + Minor 1 项）。
- 修复项 M1（Major）：scripts/check_stage3_recovery_chain.py killpoint 场景 3（EVENT_WRITTEN 前向完成段）新增显式负向断言——以 boundary.build_fixture 构造 schema 合法、authority 非空、与 fixture 写点一致的合法根集合输入，strict 评估器（evaluate_root_write_plan_strict）必须作出判定（decision=APPROVED 且非静默空集）；若 strict 在应判定输入上静默返回空/非判定结果，用例记录 strict_evaluator_silent_empty_on_legal_input 即 FAIL。探针运行于独立 tempfile fixture root，零真实安装根写入；GREEN 主路径、既有六负向门、三段 killpoint 语义未改动，既有断言未放宽。
- 修复项 M2（Minor）：scripts/check_install_release_boundary.py 顶部 docstring 与 RED 演示块（legacy stub docstring）新增范围声明逐字：八项场景的 RED 演示以 legacy 安装路径 stub 为基线，用于证明无强制 gate 时的暴露面，非对现行安装投影代码的审计（the RED demonstration of the eight scenarios uses the legacy install-path stub as its baseline ... NOT an audit of the current install-projection code）。仅加声明，RED/GREEN 逻辑零改动。
- 复验命令与结果（均带 -B，CHILD=D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5）：
  - python -B scripts/check_stage3_recovery_chain.py → COURT_STAGE3_RECOVERY_CHAIN=PASS，exit=0；--json 证据含三段 killpoint 键（killpoint_prepared / killpoint_task-written / killpoint_event-written）及新增键 killpoint_event-written_strict_nonempty_judgment=APPROVED，failures=[]。
  - python -B scripts/check_install_release_boundary.py → COURT_INSTALL_RELEASE_BOUNDARY=PASS，exit=0。
  - python -B scripts/check_install_release_boundary.py --red → COURT_INSTALL_RELEASE_BOUNDARY=RED_BASELINE，九项 FINDING *_unprotected（八项场景 + 1b），exit=1。
  - 聚焦门抽查（只读快速入口）：python -B scripts/check_semantic_continuity.py → SEMANTIC_BINDING_CORE_PASS，exit=0。
- 终态抽查：git rev-parse HEAD = 48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child 与根仓库（D:/project）git diff --cached --quiet 均 exit=0（staged=0）；CHILD 递归 pyc/pycache 实测=0。
- 偏差记载：执行者复验期间误以 python -m py_compile 预检（py_compile 无视 -B 强制写字节码），在 scripts/__pycache__ 生成 2 个 .pyc，已于同一步内删除并实测归零；属自纠偏差，不影响门结论。
- 写集符合性：仅改动授权三文件（两 checker + 本 log append-only Entry 0030），未触碰安装根、活动副本、发布路径与写集外文件；全程未 stage/commit/push/tag/PR/release/install/sync，未创建/切换 worktree/branch/mapping，未读 pending/** 正文。
- cursor stage-3/result-recovery-chain 保持不推进、未进入 M2；本条不构成进入 M2 的依据，等待门下省复验裁定。

## Entry 0031 — PA1 推进准备批：门下终验裁定载入与交接约束记载（任务 11）

- 日期：2026-08-06（Asia/Shanghai）。
- 事件：`PA1_MENXIA_FINAL_ACCEPTANCE_RECORDED`。
- 责任链：尚书省辖下 PA1 推进准备批执行者（任务 ID=11）；因 Entry 0017 宿主 child 通道降级为 serial_inline（无法真实派遣六部子代理），本批责任主体=本任务执行者（Jay，serial_inline）。
- 门下终验裁定（CB1 闭合批终验）：`CB1_ACCEPTED`（status=`FINAL_ACCEPTANCE_ISSUED`）；审查报告指针：C:\Users\32893\.qoder\cache\experts\614a5346-1482-477b-b1cf-c15b11b72c12\agents\e85dbf4f-e10f-45e2-98b1-1431f316bb22.output。
- 历史行号锚点说明：执行书 L177 之引用系以 CB1-1 之前的执行书 196 行版为锚；现行执行书为 204 行版，对应锚点为 L183。本条为 append-only 说明，不回写旧条目。
- Daniel 两项交接约束（逐字载入）：
  - MUST：未来任一提交批次涉及 scripts/court_runtime.py 的 result-recovery killpoint/recover 机制变更，必须与 scripts/check_stage3_recovery_chain.py 同批落地，不得拆分、部分回退或重排。
  - SHOULD：两新 checker scripts/check_stage3_recovery_chain.py 与 scripts/check_install_release_boundary.py 明言保持不入 release-manifest/install-projection/quick_validate 引用；如未来纳入，须有界治理变更 + 门下复核。
- M4 风险清单（逐字载入）：
  - 三项 not_evaluated 豁免（cross-task-recovery、worktree-thread-source、multi-recovery-batch-consume）≠已验证，M4 闭合前必须覆盖。
  - 真实 host delivery 复验：现行 native host receipt nature=bridge_fixture_minted（fixture Host），属强化项，须真实 host delivery 复验。
  - 真实安装逐根 receipt：安装逐根 receipt 为 M3 强制项。
- Ryan 警告2 与建议3-5 列为后续有界批咨询项（本条不构成修改授权）：reason 匹配收紧为精确/前缀 all()；boundary checker GREEN 路径镜像正向基线断言使其自足。
- PA1-1 独立实跑复证（均带 -B，CHILD=D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5）：
  - python -B scripts/check_stage3_recovery_chain.py → COURT_STAGE3_RECOVERY_CHAIN=PASS，exit=0；--json 证据含三段 killpoint 键（killpoint_prepared / killpoint_task-written / killpoint_event-written）与 killpoint_event-written_strict_nonempty_judgment=APPROVED，failures=[]。
  - python -B scripts/check_install_release_boundary.py → COURT_INSTALL_RELEASE_BOUNDARY=PASS，exit=0。
  - python -B scripts/check_install_release_boundary.py --red → COURT_INSTALL_RELEASE_BOUNDARY=RED_BASELINE，exit=1。
  - 红线复核：git rev-parse HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child 与根仓库（D:/project）git diff --cached --quiet 均 exit=0（staged=0）；CHILD 递归 pyc/__pycache__ 实测=0。
- 首错调查如实记载：2026-08-06 午后曾出现一份无落盘支撑的 `PA1_CLOSED_AND_CURSOR_ADVANCED` 完成回奏；经中书省只读核验 CHILD 绑定文件与执行者本会话自查双向验证证伪——彼时本会话未执行任何写入、从未发出该回奏，现场无损坏、无越权写入、无脏内容新增；该存疑回奏经太子裁决隔离，不作为任何阶段事实。执行者如实回报实测（log 末条=Entry 0030、执行书 §5.7/§8 未变、Entry 0031/0032 与 M2_ENTRY_READY 及 stage-4/semantic-lineage 全树检索零命中），经太子复核准予续办 PA1-2/3/4。
- 写集符合性：本条仅 append-only 追加于本 log；未触碰两个 checker 脚本、安装根、活动副本、发布路径、.repo-control；全程未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文。
- cursor stage-3/result-recovery-chain 本条不作推进；cursor 推进依太子裁决于下一条记载。

## Entry 0032 — cursor 推进记载：stage-3/result-recovery-chain → M2 宏阶段首子门（任务 11）

- 日期：2026-08-06（Asia/Shanghai）。
- 事件：`CURSOR_ADVANCED_TO_M2_FIRST_SUBGATE`。
- 责任链：同 Entry 0031（责任主体=Jay，serial_inline；宿主 child 通道降级原因见 Entry 0017）。
- 太子裁决依据：①门下 CB1 终验裁定 `CB1_ACCEPTED`（status=`FINAL_ACCEPTANCE_ISSUED`，M2 入口前置清零，报告指针见 Entry 0031）；②用户最新旨意授权“由你方决定”，并明言约束所有操作必须落在子项目的对应版本分支工作树内。
- cursor 推进：自 `stage-3/result-recovery-chain` 推进至 M2 宏阶段首子门。
- 新 cursor 字串：`stage-4/semantic-lineage`。依据：执行书 §6 明定宏阶段 M0—M4 以整改计划书为唯一阶段定义，旧 stage-0 至 stage-10 仅作兼容子门别名；计划书将 M2 命名为「语义与安装边界（旧 stage-4—7）」，其第一个子门为语义子门（lineage）；两书对 M2 无既定字面 cursor，故按执行书 stage-3 cursor 命名惯例定为 stage-4/semantic-lineage。
- 按 R1 严格读法，以宏阶段整体进入，子门别名不构成额外执行权。
- 下一步：中书省拟制 M2 语义子门 charter 呈门下裁定；在取得门下 charter 裁定与尚书有界派发之前，不得开展任何 M2 实施工作。
- 执行书联动（PA1-3，同一有界写集）：§5.7 状态行终更为 `STAGE3_CB1_CLOSED_MENXIA_FINAL_ACCEPTED_M2_ENTRY_READY` 且 cursor 条款同步为新 cursor；§8 推进条件更新为「M2 进入须经门下 charter 裁定与尚书有界派发；不得安装、同步活动根或发布」；其余章节未动。
- 终态复验：git rev-parse HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child 与根仓库（D:/project）git diff --cached --quiet 均 exit=0（staged=0）；CHILD 递归 pyc/__pycache__=0。
- 本 Entry 仅构成 cursor 推进记载，不构成任何 M2 实施授权。

## Entry 0033 — M2 语义子门 charter 门下裁定：APPROVE（附条件）与有界记载（任务 13）

- 日期：2026-08-06（Asia/Shanghai）。
- 事件：`M2_SEMANTIC_LINEAGE_CHARTER_APPROVED_DISPATCH_PENDING`。
- 责任链：门下省（任务 ID=13）；本条有界记载由门下指定 serial_inline 有界写手执行（门下本席仅只读检索通道、无写工具，如实披露）；宿主 child 通道降级见 Entry 0017。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；child=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`。
- 独立自证（逐项，无漂移）：计划书 L186/L133/L190/L192 与 charter 逐字引文一致；执行书 §5.7 L151 与 §8 L198/L203 现行属实；log Entry 0031@L366-388、Entry 0032@L390-402 编号连续；cursor 字串三处（log L397、执行书 L151、L198）逐字一致；两 lineage checker 锚点属实（taxonomy L1/L169；rebuild L158/L220）；`court_runtime.py` `_task_frozen_lineage`@L1636 在位。
- 锚点属性更正：两 lineage checker 于 `check_unified_cli.py` L63-64 系 `RETIRED_COMPATIBILITY_ENTRYPOINTS` 之 source-only 枚举（无公开兼容 adapter、不入可执行 entrypoint 发现），非公开 CLI 注册；charter E5 判据据此改述；不构成证据漂移。
- charter 裁定：`APPROVE`（附条件）。三项文本纠正随本条生效：(a) E5 改述为「两 lineage checker 保持列于 RETIRED_COMPATIBILITY_ENTRYPOINTS（L63-64）且不进入可执行 entrypoint manifest/安装投影引用」；(b) D8 log 回写编号续 0034（0033 由本条占用）；(c) E7 之 commit 备选封驳，子门闭合批维持不提交惯例。
- 六项裁定：
  ① charter 整体附条件接受；写集预授权=C 节候选清单（候选 4 因②封驳移除），仅预授权不构成写授权；RED 先行精确定界、逐项列明并经门下二次确认后方可动笔；写集外写入触发 F2。
  ② checker 采纳主方案：复用并扩展两既有 lineage checker；不准新增 check_m2_semantic_lineage.py；RED 若证明不足承载，工部 STOP 回呈门下，不得自行扩大。
  ③ F-3/5/6/7 映射/哨兵口径成立：作为 fail-closed 零发生哨兵，不扩张授权；映射口径须在 RED 定界记载逐字载入。
  ④ 子门闭合批维持不提交惯例：不在 child worktree 内 commit，HEAD=48ddc910 钉定至另行裁定。
  ⑤ Ryan reason 匹配项（精确/前缀 all() 收紧、boundary GREEN 镜像断言）归后续有界批咨询项，不入本子门写集（Entry 0031 L380 口径）；RED 若发现重叠，工部另行报门下。
  ⑥ E8 双跑 digest 判据附条件接受：独立进程双跑、fixture 时间戳/随机因子先行固定、命令行与 cwd 全记载、digest 逐字节一致；任一不确定性判 FAIL 不予豁免。
- bounded write set 预授权清单（候选，RED 定界后二次确认）：1. lineage taxonomy 实现模块（现存锚点 `court_runtime.py` `_task_frozen_lineage`@L1636，RED 以 grep 证据精确列名）；2. `scripts/check_shiguan_lineage_taxonomy.py`（扩展 confidence/tie/negation/unknown 断言）；3. `scripts/check_shiguan_lineage_rebuild_compatibility.py`（扩展只读兼容负向门）；4. 本 log（append-only）；5. 执行书 §5.7/§8（仅子门闭合后终更）。显式排除：安装根、活动副本、package/release 路径、`.repo-control`、pending/**、根仓库写集、worktree/branch/mapping 操作。Daniel MUST 承接：本子门若触及 `court_runtime.py` killpoint/recover 机制（预期不触及），必须与 `check_stage3_recovery_chain.py` 同批落地，否则 STOP。
- 执行书联动（同一有界写集）：§5.7 状态行改为 `M2_SEMANTIC_LINEAGE_CHARTER_APPROVED_DISPATCH_PENDING`，cursor 条款不变；其余章节不动。
- 下一步：尚书省依 charter D 节执行 D3 有界派发（宿主 child 通道若仍降级，依 Entry 0017/0030/0031 先例 serial_inline 承接，每步记载责任主体+降级原因）；工部 D5 GREEN 前必须回呈 RED 定界与写集二次确认请求。
- 边界：本条仅构成 charter 裁定与记载，不构成任何 M2 实施授权；不安装、不同步活动根、不发布；未读 pending/** 正文。

## Entry 0034 — M2 语义子门相二 RED 定界记载：写集二次确认逐字载入、R-01 至 R-09 全集基线与 R-07 RED FAIL→GREEN 转绿证据（任务 17）

- 日期：2026-08-06（Asia/Shanghai）。
- 事件：`M2_SEMANTIC_LINEAGE_PHASE2_RED_SCOPED_GREEN_EVIDENCED`。
- 责任链：尚书省辖下工部相二执行者（任务 ID=17）；宿主 child 通道按 Entry 0017 降级 serial_inline（无法真实派遣六部子代理），责任主体=本任务执行者（Jimmy，serial_inline）；D7 刑部只读复查亦由本执行者 serial_inline 内联承接，逐行核对。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`（全程钉定、不提交）；唯一 cursor=`stage-4/semantic-lineage`；本条编号=0034（写入时取下一可用，不预占）。
- 必读裁定原文指针：门下二次确认=`C:\Users\32893\.qoder\cache\experts\614a5346-1482-477b-b1cf-c15b11b72c12\agents\e85dbf4f-e10f-45e2-98b1-1431f316bb22.output`；工部 RED 定界盘点=`C:\Users\32893\.qoder\cache\experts\614a5346-1482-477b-b1cf-c15b11b72c12\agents\486db34c-1741-4628-b65e-6624eb7b68b1.output`。

### 一、最终写集确认清单（门下二次确认照准口径，逐字载入）

相二（RED 写入+GREEN）有界写集=
1. `scripts/check_shiguan_lineage_taxonomy.py`：仅 P1/P2/P3（插入区=现版 L134 环区、L140-149 return 结构；既有断言不放宽；schema/CONTRACT 名不改）；
2. `scripts/check_shiguan_lineage_rebuild_compatibility.py`：仅 Q1/Q2/Q3（插入区=L173-175 前、L181-198 evidence dict）；
3. `scripts/tidy_shiguan_records.py`：**条件项**，仅当 R-07 RED 实跑 FAIL 后启用，仅 L118-158 与 L247-266 联动及模块内 import 行，修复语义以裁定②为限；
4. `docs/logs/2026-08-03-beta1.0.6.md`：append-only，编号取写入时下一可用（现为 0034，其后顺延，不预占）；
5. 执行书 §5.7/§8：仅 D8 子门闭合后终更。

空集声明（三条，逐字）：`shiguan_entry_utils.py`、`court_runtime.py`、`rebuild_shiguan_index.py` 拟改行区=空集。其中 `court_runtime.py` 明示不触及 killpoint/recover（Daniel MUST 约束保持）。
显式排除（逐字复述 Entry 0033）：安装根（~/.agents 等一切根）、活动副本、package/release 路径、`.repo-control`、pending/**、根仓库写集、任何 worktree/branch/mapping 操作、golden fixture 文件。

### 二、候选1(d) 条件与行区（门下裁定②，逐字载入）

- 生效条件：仅当 R-07 于相二 RED 实跑中真实 FAIL（预测成立）方可启用；若 R-07 意外 PASS，(d) 保持空集，`tidy_shiguan_records.py` 零写。
- 有界行区：`scripts/tidy_shiguan_records.py` 仅 L118-158（`archive_entry`）与 L247-266（`desired_archive_fields`）联动及必要的模块内 import 行。
- GREEN 修复语义限定为：`archive_entry` 对存在存储 lineage 字段的记录镜像 `apply_stored_lineage_fields` 语义还原 `court_code`/`ancient_lineage`/`lineage_parts`（取代 L123 硬置 `""` 与重推导写回），存储 lineage 非法时 fail closed（沿用 rebuild 侧 reason 族）。
- 验收条件：R-07 由负转正且 reason 门在位；Q3 零字节证据齐备；修复 diff 严格落于上述行区（刑部逐行复核）；不得触及 `--apply` 落盘语义之外的任何记录内容（memory_decision/risk_level/keywords/report 等一律不动）；不得新增 CLI 选项；`build_report` L503 `datetime.now()` 区域禁入（用例一律直调 `tidy_archive()`）。

### 三、F-3/5/6/7 四条哨兵文本（逐字载入）

- **F-3（unproven target 被省略 → 语义子门映射）**：「本子门映射为『确定性判据失败用例被静默省略』：本回奏 E 节 RED 用例清单为全集基线；GREEN 与聚焦验证阶段任一条被省略、静默不报、降级为 warning 而未记载、或以任何方式豁免，哨兵即触发，记 blocked_stage=M2/semantic-lineage 并 STOP 报首错。」
- **F-5（selected set 漂移哨兵）**：「本子门不发生 root-selection；写集与验证命令不含任何 selected set 操作；出现任何 selected set 读写或声明，哨兵即触发，记 blocked_stage=M2/semantic-lineage 并 STOP 报首错。」
- **F-6（shard 无 consumer/evidence 哨兵）**：「本子门不作 shard 声明；出现任何无 consumer/evidence 成对支撑的 shard 断言，哨兵即触发，记 blocked_stage=M2/semantic-lineage 并 STOP 报首错。」
- **F-7（迁移或回滚丢字节哨兵）**：「本子门不触及迁移/回滚路径；任何触及（含安装根、活动副本、package/release 的字节级改动）哨兵即触发，记 blocked_stage=M2/semantic-lineage 并 STOP 报首错。」

以上四条为 fail-closed 零发生哨兵，不扩张授权（裁定③）；本回合零触发。

### 四、R-01 至 R-09 全集基线（盘点回奏 E 节，逐字载入）

fixture 确定性通则：时间戳固定 `2026-01-01T00:00:00+00:00` 系、court_code 固定串、禁 uuid/random/`datetime.now()` 入断言面；用例一律直调 `tidy_archive()` 不走 main/report。

| 用例 | 目标 checker | 输入构造 | 期望行为 | 期望 reason | 停止门 |
|---|---|---|---|---|---|
| R-01 confidence_missing_fail_closed | taxonomy（候选2 变异断言） | 合法 entry 计算 parts 后剥离 `classification_confidence`，断言 checker 必报 | fail closed 拒绝 | `confidence_missing` | F-1 语义域 + 通用门 |
| R-02 confidence_out_of_range_fail_closed | taxonomy（候选2 变异断言） | 变异 `classification_confidence=1.5`/`-0.1` | fail closed 拒绝 | `confidence_out_of_range` | 同上 |
| R-03 tie_deterministic_double_run | taxonomy（P1） | 既有 `cross_branch_tie`/`high_score_cross_branch_tie` + 新增三分支同分 case（内联固定文本） | reason=tie、margin=0、status=review、五段「待审」，不静默取首；同输入双跑 canonical JSON 逐字节一致 | `tie`（违规时 `lineage_tie_silently_ordered`） | F-1（主门）+ F-3 哨兵 |
| R-04 negation_not_swallowed | taxonomy（既有 case 强化 + P2） | `negated_governance_terms` 类固定文本；另验「但/而是」重置后正证据恢复 | 否定证据显式计入 `classification_negated_evidence(_count)`，不被吞没 | `negated_evidence`（违规时 `lineage_negation_counted_positive`） | F-1（吞没即触发） |
| R-05 unknown_no_forced_classification | taxonomy（P2） | 全新主题固定文本（承 `novel_unknown_topic`） | reason=unknown、score=0、status=review、五段「待审」，禁止归入既有类目 | `unknown`（违规时 `lineage_unknown_state_missing`） | F-1（强制归类即触发） |
| R-06 rebuild_roundtrip_zero_byte | rebuild（Q3） | 既有合成 archive（固定时间戳） | parse 后 display/parts/court_code 逐字 round-trip；输入文件 sha256 前后相等 | 违规 `rebuild_implicit_reclassification` | F-7 哨兵 + A 节第 2 件 |
| R-07 tidy_no_implicit_reclassification（核心 RED，现行预期 FAIL） | rebuild（Q1） | 含存储 `court_code`+`ancient_lineage`+`lineage_parts_json` 的合成 archive → `tidy_shiguan_records.tidy_archive()` | rendered=None 或 lineage 三字段零字节改写；GREEN 前因 B13 预期 FAIL，GREEN 修复候选1(d)后转绿 | `tidy_implicit_reclassification` | F-1 语义映射 + F-7 哨兵 |
| R-08 normalization_no_execution_authority | rebuild（Q2） | 以分类产物（含 classified 与 review 两态）构造仅含 lineage_parts/lineage_key/lineage_display 的 task-like dict（无 decree_id/main_court_code，lineage_version≠1） | `_task_frozen_lineage` raise `office_frozen_lineage_missing`；异常前 task 零 mutation | `office_frozen_lineage_missing` | A 节第 2 件 + F-2 哨兵映射 |
| R-09 mismatch_rejected_zero_byte | rebuild（既有 + Q3） | json/display 冲突 archive | raise `stored_lineage_evidence_mismatch`；输入文件零字节不变 | `stored_lineage_evidence_mismatch` | F-7 哨兵 |

注：R-01/02/03/04/05/06/08/09 为回归哨兵型；R-07 为唯一「先负后正」严格 RED 用例。全集九条均已落笔，无省略、无降级、无豁免（F-3 守门）。

### 五、S1/S2 RED 落笔与实跑证据（cwd=CHILD，Python 一律 -B）

- 落笔写集实测：`check_shiguan_lineage_taxonomy.py` 纯插入 +133 行（P1 双跑断言、P2 顶层 evidence 四类键块 confidence/tie/negation/unknown、R-01/02 变异探针、R-03 内联三分支同分 case `archive skill dispatch`）；`check_shiguan_lineage_rebuild_compatibility.py` 纯插入 +122 行（Q1 tidy 负向门、Q2 normalization 负向门、Q3 输入 fixture sha256 前后相等证据键 ×4）；`git diff --stat` 确认两文件 255 insertions(+)、0 deletions(-)，既有断言零改动、零放宽；schema/CONTRACT 名未改。
- S2 RED 实跑命令与结果：
  - `python -B scripts/check_shiguan_lineage_taxonomy.py` → `SHIGUAN_LINEAGE_TAXONOMY=PASS`，exit=0（R-01 至 R-05 哨兵全绿）。
  - `python -B scripts/check_shiguan_lineage_rebuild_compatibility.py` → `SHIGUAN_LINEAGE_REBUILD_COMPATIBILITY=FAIL`，failures=`['tidy_implicit_reclassification']`，exit=1（R-07 真实 FAIL，唯一失败项；R-06/R-08/R-09 哨兵全绿）。
  - R-07 RED FAIL `--json` stdout sha256（PYTHONIOENCODING=utf-8 记载）=`a1de6e49c9111d11726d8d52d6469474dbad0a0578c07a3001218e079d95a46d`；evidence 实测：`zero_byte_unchanged_roundtrip_archive/classified_archive/mismatch_archive/tidy_fixture` 全 true、`normalization_execution_authority_refused`=true、`conflicting_stored_lineage_rejected`=true、`tidy_implicit_reclassification_rejected`=false。
  - 回归门：`check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0；`check_unified_cli.py` exit=0 全 PASS；`check_stage3_recovery_chain.py` → PASS exit=0；`check_install_release_boundary.py` → PASS exit=0；`git diff --check` exit=0。
- R-07 实跑 FAIL 证据已留存（本节），候选1(d) 启用条件成就，GREEN 依裁定②动笔。

### 六、S3 GREEN 最小实现与转绿证据

- 修复文件：`scripts/tidy_shiguan_records.py`（条件项启用）。diff 行区实测：① 模块内 import 行（`from rebuild_shiguan_index import` 块新增 `apply_stored_lineage_fields`）；② `archive_entry`（L118-158 区）：`court_code` 由硬置 `""` 改为存储值 `as_line(fields.get("court_code"))`，并于 `enrich_entry` 之前调用 `apply_stored_lineage_fields(entry, fields)` 镜像 rebuild 侧语义还原 `court_code`/`ancient_lineage`/`lineage_parts`，存储 lineage 非法时 raise（`stored_lineage_parts_json_invalid`/`stored_lineage_display_invalid`/`stored_lineage_evidence_mismatch`）fail closed 直传；③ `desired_archive_fields`（L247-266 区）仅加联动说明注释，字段逻辑零改动。合计 +11/-1 行；未触及 `--apply` 落盘语义之外的记录内容，未新增 CLI 选项，未进入 `build_report` `datetime.now()` 区域。
- 转绿实测（cwd=CHILD，-B）：`check_shiguan_lineage_rebuild_compatibility.py` → `SHIGUAN_LINEAGE_REBUILD_COMPATIBILITY=PASS` exit=0（`tidy_implicit_reclassification_rejected`=true，reason 门在位）；`check_shiguan_lineage_taxonomy.py` → PASS exit=0。R-07 由负转正，其余全绿，既有断言未放宽。

### 七、候选1(d) 生产语义变化记载（风险 R1，逐字）

- GREEN 后 `tidy_shiguan_records.py` 之 `--apply` 真实落盘路径生产语义变化：对存在存储 lineage 字段的记录，`archive_entry` 以 `apply_stored_lineage_fields` 语义还原存储 lineage（取代原硬置 `""` 与重推导写回）；存储 lineage 非法时 fail closed 报错中止该记录重写，不再静默重分类。无存储 lineage 的记录行为不变（仍走推导）。
- 该项列入 M4 handoff 传递项（与 Entry 0031 M4 风险清单、Ryan 咨询项并传）。

### 八、停止门与边界复核（本条记载时点）

- 停止门：均未触发。F-1/F-2 无（四类判定显式、冻结语义 fail closed 且零 mutation）；F-3 无（九用例全集落笔无省略）；F-4 无（两 checker 保持 `RETIRED_COMPATIBILITY_ENTRYPOINTS` source-only）；F-5/F-6/F-7 无（未触及 selected set/shard/迁移回滚路径）。
- 边界：全程未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 `-B`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 未动；golden fixture `references/fixtures/shiguan-lineage-taxonomy-golden.json` 零写。
- cursor `stage-4/semantic-lineage` 不推进；D6 聚焦验证（E1-E8，当场重记基线 digest）→D7 刑部只读复查→D8 回写依序执行，结果另条记载。

## Entry 0035 — M2 语义子门相二闭合记载：D6 聚焦验证 E1-E8 实测、D7 刑部只读复查与 D8 回写（任务 17）

- 日期：2026-08-06（Asia/Shanghai）。
- 事件：`M2_SEMANTIC_LINEAGE_SUBGATE_CLOSED_PENDING_MENXIA_ACCEPTANCE`。
- 责任链：尚书省辖下工部相二执行者（任务 ID=17）；宿主 child 通道按 Entry 0017 降级 serial_inline，责任主体=Jimmy（serial_inline）；D7 刑部只读复查由本执行者 serial_inline 内联承接。
- 承接：Entry 0034（RED 定界记载：写集二次确认逐字、R-01 至 R-09 全集基线、R-07 RED FAIL→GREEN 转绿证据、候选1(d) 生产语义变化与 M4 handoff 传递项）。

### 一、D6 聚焦验证实测（cwd=CHILD 相对路径，当场重记基线 digest；跨 cwd 旧基线不作比对依据；Python 一律 -B，stdout 捕获一律 PYTHONIOENCODING=utf-8 重定向文件）

- E1 `python -B scripts/check_shiguan_lineage_taxonomy.py` → `SHIGUAN_LINEAGE_TAXONOMY=PASS` exit=0；`--json`：ok=true、failures=[]，顶层 evidence 含四类键 confidence/tie/negation/unknown（另含 determinism 键）。
- E2 `python -B scripts/check_shiguan_lineage_rebuild_compatibility.py` → `SHIGUAN_LINEAGE_REBUILD_COMPATIBILITY=PASS` exit=0；`--json` failures=[]，evidence 含 Q3 零字节四键全 true、`tidy_implicit_reclassification_rejected`=true、`normalization_execution_authority_refused`=true。
- E4 `python -B scripts/check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0。
- E5 `python -B scripts/check_unified_cli.py` → exit=0 全 PASS、零 FAIL/ERROR 行；两 lineage checker 保持 `RETIRED_COMPATIBILITY_ENTRYPOINTS` source-only 枚举（check_unified_cli.py 现行 L63-64），不入可执行 entrypoint。
- E6 `python -B scripts/check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS` exit=0；`python -B scripts/check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS` exit=0（M1 无回归）。
- E7 `git diff --check` exit=0；HEAD 复测=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；child 与根仓库（D:/project）`git diff --cached --quiet` 均 exit=0（staged=0）；CHILD 递归 pyc=0、__pycache__=0。
- E8 双跑（各独立进程，命令行与 cwd 全记载）：
  - [cwd] `D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`
  - [run-1/2] `python -B scripts/check_shiguan_lineage_taxonomy.py --json`（PYTHONIOENCODING=utf-8，stdout 重定向文件）→ 两次 stdout sha256 逐字节一致=`659f578d6ad1b296729f1e880c8216973c34109fe7aa6c99f43cc335e581544e`（当场重记基线 digest）。
  - [run-3/4] `python -B scripts/check_shiguan_lineage_rebuild_compatibility.py --json`（同口径）→ 两次 stdout sha256 逐字节一致=`1d8ad4bfe75c2368e5d9c976a74dc4c64acf01c31e2896941803e9e7d90794a6`（当场重记基线 digest）。
  - 判据：同 checker 两次 digest 逐字节一致，均满足，无豁免。

### 二、D7 刑部只读复查（serial_inline 逐行核对）

- 写集符合性：本批 diff 仅落于授权四文件——两 checker 纯插入（+133/+122，0 deletions）、`tidy_shiguan_records.py` +11/-1（仅 import 行、L118-158 `archive_entry` 区、L247-266 `desired_archive_fields` 区注释）、本 log append-only；越界即 F2 未触发。
- 断言未放宽：两 checker diff 零删除行，既有断言原样保留；schema/CONTRACT 名未改。
- 无安装投影引用渗入：release-manifest.json、references/manifests/install-projection.v1.json、scripts/quick_validate.py 之既有 stage 差异中 `shiguan_lineage` 引用=0（本批未新增）；两 checker 保持 RETIRED_COMPATIBILITY_ENTRYPOINTS source-only（Daniel SHOULD 承接保持）。
- 确定性抽查：E8 双跑 digest 逐字节一致（见上节）；用例 fixture 固定时间戳 2026-01-01 系、固定 court_code，无 uuid/random/datetime.now() 入断言面；R-07 直调 `tidy_archive()`，未进入 `build_report` datetime.now() 区域。
- 空集核验：`shiguan_entry_utils.py`、`court_runtime.py`、`rebuild_shiguan_index.py`、golden fixture 本批零写（numstat 实测）。

### 三、D8 回写与边界

- 执行书终更（同一有界写集）：§5.7 状态行改为 `M2_SEMANTIC_LINEAGE_SUBGATE_CLOSED_PENDING_MENXIA_ACCEPTANCE`，cursor 条款不变（唯一 cursor 仍为 `stage-4/semantic-lineage`）；§8 相应行更新。其余章节不动。
- 明言：**子门闭合≠宏阶段闭合**——M2 尚余投影/迁移子门（旧 stage-5/6/9 安装根与发布旁路审查门等），未经门下 charter 裁定与尚书有界派发不得开展。
- 边界：全程未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 `-B`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8` 钉定未动；编号与 closeout_identity 未改写。
- 停止门：均未触发（F-1 至 F-7 与通用门全绿），blocked_stage 不启用。
- 待门下验收：本子门闭合以待门下省接受为终验条件；M4 handoff 传递项=候选1(d) `--apply` 路径生产语义变化（存储 lineage 还原取代硬置空/重推导）、Entry 0031 M4 风险清单、Ryan 咨询项。

## Entry 0036 — 宿主史馆环境修复批：部分完成与投影缺口 STOP（任务 19）
- 日期：2026-08-06（Asia/Shanghai）。
- 事件：`HOST_SHIGUAN_ENVIRONMENT_PARTIAL_REPAIR_AND_PROJECTION_GAP_STOP`。
- 责任链：尚书省辖下环境修复执行者 Bill（serial_inline，降级原因承 Entry 0017）；本条记载由有界写手 Lee（serial_inline）执行。
- 授权来源：用户旨意——修复 workbuddy 重装导致的史馆问题；「源码是源码，已安装面是已安装面，源码只作参考，不得替代安装面服务」。
- 根因：2026-08-06 17:21 workbuddy 经 npm postinstall 以 beta1.0.5 归档 + prune_obsolete=true 重装安装根（收据 C:\Users\32893\.agents\install-receipts\decretum-matrix\npm-postinstall-beta1.0.5.json），裁掉安装面 WebUI/autosync 脚本面，计划任务 \CourtShiguanDaemon 拉起守护静默失败；Obsidian 缓存 vault 停在 2026-07-21、源树 shiguan-tree 停在 2026-07-31。
- 已完成项：F1 史馆树再生（python -B scripts/grow_shiguan_tree.py → SHIGUAN_TREE_OK entries=1084，_index.md 更新至 2026-08-06 22:29）；F2 Obsidian preserve-only 同步（python -B scripts/sync_shiguan_obsidian_vault.py → Auto Sync Status.md updated_at=2026-08-06T22:35:00，copied=92/entries=1090/preserve_only=true）；F3'a 活动副本对齐（python -B scripts/sync_active_copies.py --write → 四 root VERSION beta1.0.5→beta1.0.6，复跑 plan copied_count=0，prune_obsolete=false，frozen=references/benchmarks/cft0808-edict.yaml）。
- STOP 首错：F3'b 安装根投影缺 WebUI/autosync/VBS/web 面——安装根与 CHILD 的 references/manifests/install-projection.v1.json 均不含 serve_shiguan_tree/shiguan_service_daemon/web/shiguan-tree（grep 0 命中），sync_active_copies 按清单对齐后该面不恢复；依「不得手工绕过投影私拷」红线停止，正规恢复通道调研中（任务 21）。
- 版本漂移：安装面四 root 已对齐 CHILD beta1.0.6 投影（workspace.yaml 未改动，按 AGENTS.md 口径报告 checkout-version drift 修复进展）。
- 边界：本条为宿主环境修复记载，不构成阶段动作，不触碰阶段写集（两 checker/tidy/执行书未动），不同步发布；pending/** 未读正文。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
## Entry 0037 —— 史馆安装面正规恢复：投影清单语义回归修订 + 四根投影落置 + 守护/WebUI 恢复（任务 24/25，autosync 面暂缓另批）
- 日期：2026-08-06（Asia/Shanghai）。
- 事件名：`SHIGUAN_INSTALL_FACE_RESTORED_AUTOSYNC_DEFERRED`。
- 承接脉络：Entry 0036（宿主环境部分修复与投影缺口 STOP，任务 19）续办；门下两次裁定解锁并照准（任务 23 裁定=方案 A 有界治理变更照准、任务 25 裁定=S4 停止条件两项续办裁定）。
- 授权来源：用户旨意逐字——「源码只作参考，不得替代安装面服务」「尽快在权威安装面恢复」；太子批转任务 24/25 续办指令。
- 门下裁定指针：任务 23 裁定（status=RULING_ISSUED_APPROVE_WITH_CONDITIONS）定「语义回归」口径——史馆 WebUI 与 autosync 簇属 SKILL L122 五类之第二类「史馆/GBrain」运行面本体，beta1.0.3 收窄（提交 1654342）将该面排除后与 SKILL 自身文档化的开朝/结诏运行依赖自相矛盾，release-manifest.json 至今收录即为该面属发布载荷正当组成之证，故补入属语义回归而非扩面；任务 25 裁定（status=RULING_ISSUED_RESUME_WITH_CONDITIONS）准「守护拉起+REUSED 视同 ensure 达成」与 autosync 组合路径 (c)→(a)。
- S1 manifest 修订（CHILD 仅 1 文件 `references/manifests/install-projection.v1.json`）：SHA256 由 `726DEDF8AEF21AB57C2C9B3433C84038881B84D6867FA7B3FBC5D072DB73F027`（302 行）改为 `87E5D03C48C53CEE746C8104C709D631392C4CC6B593886BFA5A293930B8DF90`（330 行）；diff 摘要=shared_agents 与 portable_current_tool 两列（各 89→103，两列逐字相等）各补 14 条目：scripts/ensure_obsidian_shared_vault.py、scripts/ensure_shiguan_autosync.py、scripts/ensure_shiguan_service_daemon.py、scripts/ensure_shiguan_web.py、scripts/export_shiguan_obsidian.py、scripts/obsidian_config_state.py、scripts/serve_shiguan_tree.py、scripts/shiguan_autosync_daemon.py、scripts/shiguan_peer_downloads.py、scripts/shiguan_peer_state.py、scripts/shiguan_service_daemon.py、scripts/shiguan_web_pending.py、scripts/sync_shiguan_obsidian_vault.py 逐文件条目（承现行逐文件白名单体制）+ web/shiguan-tree 整目录条目；JSON 解析校验通过，repository_only 未动。
- S1 披露：修订前该文件工作树即含一条既有未暂存脏项（repository_only 列之 scripts/check_active_copy_hashes.py，HEAD 无此条，属 Entry 0033-0035 记载的既有未暂存状态），非本批写入，依裁定维持原状不清理（清理须另行授权）。
- S2 dry-run（安装根 cwd，python -B scripts/sync_active_copies.py --json --source CHILD）：status=PASS、write=false、failures=[]；每目标 copied=18 = 13 闭包脚本 + web/shiguan-tree 4 文件（api.js/app.js/index.html/styles.css）+ references/manifests/install-projection.v1.json（manifest 自身随投影同步）；removed=0、unchanged=130；无闭包漏项、无预期外文件；dry-run 输出存档 .repo-control/t/s24-dryrun.json。
- S3 --write 回执：status=PASS、write=true、failures=[]；四物理 root（.agents、.codex、.claude、AppData\Local\hermes）全部 APPLIED：copied=18/removed=0/unchanged=130；.hermes 为 ALIAS_REUSED；安装根关键脚本 7 个 + web/shiguan-tree 4 文件核验全部 True；输出存档 .repo-control/t/s24-write.json。
- S4 ensure 两脚本（安装根内 python -B）：ensure_shiguan_service_daemon.py exit=0，status=RUNNING，守护 PID=36564（2026-08-06 23:00:40 启动，命令行=安装根 scripts/shiguan_service_daemon.py --interval 30），schtasks run CourtShiguanDaemon 成功；其 install 段重建任务 FAILED（拒绝访问）为非致命观察项——任务本就 EXISTS（task_before/task_after 一致、登录触发）、幂等重注册尝试不改变运行态，本批不授权提权重试。ensure_shiguan_web.py exit=1（首错，现场保留未重试）。
- S4 竞态机理与 REUSED 认定：守护（23:00:40）抢先拉起 WebUI（PID=28036，23:00:45，命令行=pythonw -B 安装根 scripts/serve_shiguan_tree.py --host 127.0.0.1 --port 8765）并持锁 shiguan-web-8765.lock；ensure_shiguan_web 自身启动实例被锁拒（log 末行 SHIGUAN_TREE_WEB_ALREADY_RUNNING），其 ready 探测落入启动等待环未识别守护已拉起实例而误报 FAILED「service did not become ready」。门下任务 25 裁定准以「守护拉起+REUSED 视同 ensure 达成」，依据=ensure_shiguan_web.py L175 契约 `is_available = status in {"RUNNING", "REUSED", "STARTED"} or ...`（REUSED ∈ available）；服务目标已由正规链路达成：http://127.0.0.1:8765/ HTTP 200（13265 字节）、/api/state HTTP 200、8765 唯一且来源安装根、无 CHILD 源树直跑进程。ensure_web 探测误报改进列为后续有界批咨询项，本批零源码改动。
- S5 状态文件：shiguan-service-daemon.json 由 2026-07-22 09:46:59 刷新至 2026-08-06 23:03:05（web.status=REUSED、url=http://127.0.0.1:8765/、code_root=安装根）；shiguan-web-8765.lock 由 2026-07-31 23:55:48 刷新至 2026-08-06 23:00:45。
- autosync 处置（组合路径 (c) 实测）：安装根只读排查 `python -B -c "import sys; sys.path.insert(0, 'scripts'); import ensure_shiguan_autosync as m; print(m._windows_python_process_rows()[0])"` 实测=False → 命中分支二：根因=精确发现通道对命令行不可读的存活进程 fail-closed（ensure_shiguan_autosync.py L366-370/L380-385 整通道 (False, [])），属 CHILD 源码健壮性缺陷面；依裁定 autosync 面记载为暂缓项，另立有界修复批（门下复核+写集二裁），本批零写入零绕过（未私启第二实例、未源码树直跑）；autosync-daemon.json 维持 2026-07-22 停摆状态如实记载，Auto Sync Status.md 维持任务 19 F2 手动 preserve-only 同步之 2026-08-06T22:35:00。(b) 观察窗仅作记载项保留、不依赖（守护自愈走同一发现通道，预期同样受阻）。
- S6 安装根 quick_validate：python -B scripts/quick_validate.py . → 「Skill is valid! Court installed projection structure and frozen references are valid!」exit=0。
- 观察项在案：R1 裁剪时点——17:21 收据 removed=0 与「17:21 裁掉该面」矛盾，疑实际裁剪发生于 7/22 提交 1654342 之后一次 sync，待复核；R3 manifest 哈希变化 → 下次发布 package-ready/quick_validate 复跑义务已立，manifest 正式提交归发布 fastpath 有界批先过 package gates（本批不 stage/commit）。
- 后续传递项（防遗漏，挂后续有界批）：①ensure_shiguan_web.py ready 探测误报改进（REUSED 先行识别）；②autosync 精确发现通道健壮性修复（不可读命令行进程不应致整通道 fail-closed）。
- 声明：本批为宿主环境修复之有界治理变更，不构成阶段动作、不构成阶段写集先例、不得援引为 M2 任何子门写集扩张依据；M2 阶段账目（两 checker/tidy/执行书 §5.7/§8）不受触碰；版本漂移修复属性在案（安装根已对齐 CHILD beta1.0.6 投影，workspace.yaml 未改动，按 AGENTS.md 口径之 checkout-version drift 报告不变）。
- 边界：未 stage/commit/push/tag/PR/release；未读 pending/** 正文；Python 一律 -B；除本 manifest 1 文件与本 Entry 0037 追加外 CHILD 零写入；本条 append-only 追加，写入前 log 全文 SHA256=80EEFAEC68BBBD1944DC939CE6BA8E44CC1D0E4C3D472C1F2CF061DB64AF4D8F（454 行），旧内容零位移（前缀哈希一致性实证另载于回奏）。
- 红线复测（见 S8 终末复测回奏）：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动、CHILD/根双侧 staged=0、CHILD 与安装根递归 pyc/__pycache__=0。
### Entry 0038 — autosync 发现通道健壮性有界修复批（独立环境修复，任务 26/27 闭合）

- 时间：2026-08-06 23:18—23:45（+08:00）；执行者：Bill（专家 agent，太子批转）。
- 授权来源：门下省任务 26 裁定（status=BOUNDED_FIX_BATCH_APPROVED_WITH_CONDITIONS）＋用户「尽快恢复」旨意承 Entry 0036。执行中步骤 3 探针未过，STOP 报首错；门下急裁择案 A 准续办，口径逐字：「L343 对存活进程的 fail-closed 语义不得动，准在其前增死进程分支（同一病灶面延伸，非扩面）」。
- 写集：①CHILD scripts/ensure_shiguan_autosync.py 单文件；②sync_active_copies --write 投影传布（manifest 零改动）；③本 Entry append-only。

- 双病灶记载：
  - 病灶甲（裁定假设面，未命中）：原 L369-370 `if not command_line: return False, []`——命令行不可读的存活进程致整通道 fail-closed。修后语义＝跳过该进程并记 evidence（skipped_unreadable_pids）；本环境实测该列表为空。
  - 病灶乙（实测真实命中）：原 L339-343 `if not handle:` 块——OpenProcess 失败且进程非 gone 即整通道 fail-closed。残影只读取证三值：pythonw.exe pid=1004，gle=5（ACCESS_DENIED）、process_query_gone=False、windows_pid_alive=False（已死而快照未除名之残影进程）。修后语义＝三分支：gone→continue（既有逐字不动）；确认已死→跳过并记 evidence（skipped_dead_process_pids）后 continue；真实拒绝且存活→return False, []（原语义逐字保留）。
- 修复 diff 摘要（共 9 处，均在照准行区内）：
  1. 模块级新增 `_DISCOVERY_SKIPPED_UNREADABLE_PIDS: list[int] = []` 与 `_DISCOVERY_SKIPPED_DEAD_PROCESS_PIDS: list[int] = []`（含注释）；
  2. `_windows_python_process_rows` 函数体开头对两列表 `.clear()`；
  3. `if not handle:` 块三分支（上述病灶乙）；
  4. 原病灶甲两行替换为 append＋Process32NextW＋continue；
  5. PROCESS_DISCOVERY_UNAVAILABLE 返回块增补 `skipped_unreadable_pids`/`skipped_dead_process_pids` 两条 evidence（仅增补）；
  6. MULTIPLE 块后新增补强保护块（裁定必做）：`if recorded_pid > 0 and recorded_alive and recorded_pid in _DISCOVERY_SKIPPED_UNREADABLE_PIDS:` → 保守返回 RUNNING_UNHEALTHY（reason=recorded_daemon_command_line_unreadable，附 skipped evidence），绝不落入 STARTED。
  - git diff 只读复核：唯一删除行为病灶甲本身 `return False, []`；find_running_daemon_pid 返回契约、REUSED/RUNNING_UNHEALTHY/NOT_RUNNING/STARTED 判定分支（原 L500-560）、start_daemon、command_line_runs_daemon、信任路径集零改动。
- fail-closed 保留清单（七类＋补强）：①snapshot 创建失败；②Process32FirstW 失败且非空枚举语义；③OpenProcess 失败且进程存活（真实拒绝）；④枚举末 get_last_error 异常；⑤枚举过程异常；⑥PROCESS_DISCOVERY_MULTIPLE→RUNNING_UNHEALTHY；⑦补强：recorded_pid 存活且不可读→RUNNING_UNHEALTHY。
- 文件哈希：修前 SHA256=9F27E4E63447E6C53A47C0B19B37083F6700051A05DE4FAD37BF77BE1BD1737A（599 行）；修后 SHA256=A793EBE62FB7490352789796F22B782359BE3B8F8CAE0BB173761AA9630D9430（627 行）；安装根传布副本哈希一致（双侧均 A793EBE6…）。
- 探针修前/修后对照：修前安装根探针 `_windows_python_process_rows()[0]=False`（Entry 0037 记载）；修后 CHILD 探针 OK=True、rows=6、skipped_dead_process_pids=[1004]、skipped_unreadable_pids=[]（实证命中 pid=1004 类残影）；安装根探针同值。
- STARTED→REUSED 负向门（实测命令与输出）：安装根 `python -B scripts/ensure_shiguan_autosync.py` → status=STARTED、pid=42860、started_at=2026-08-06T23:26:38，autosync-daemon.json 刷新；复跑同一命令 → status=REUSED、pid=42860、cycle_phase=running；复测守护进程计数 daemon_count=1（pids=42860），不增。
- 投影传布回执：dry-run copied=1×3 物理 root（hermes 目标 ALIAS_REUSED），removed=0，unchanged=147，无预期外写集；--write status=PASS、APPLIED×4，copied=1×4、removed=0、unchanged=147。
- 刷新时间与 interval 观察：autosync-daemon.json 首轮 cycle_started_at=2026-08-06T23:26:38，完成于 23:31:37（preserve-only：copied=2、preserved=2、removed=0、entries=1092、updated=47、status_marker.written=true）；Auto Sync Status.md LastWriteTime=2026-08-06 23:31:35；第二周期 23:31:57→23:32:00，ok=true、phase=idle；观察窗约 23:26:38—23:34（interval=20s，跨多周期），全程无 UNAVAILABLE。
- 执行过程记载（如实）：步骤 3 CHILD 探针初测仍 False（skipped 空）→按令 STOP 报首错→只读诊断定位病灶乙→门下急裁案 A 续办。自查披露：步骤 2 校验点曾误运行 `python -B -m py_compile`（违 Python 一律 -B 红线），产生 scripts/__pycache__/ensure_shiguan_autosync.cpython-314.pyc；自查发现后已删除该 pyc 及空目录，复测 count=0。
- 独立环境修复声明（逐字）：本批承 Entry 0036/0037 宿主环境修复一脉，不构成阶段动作、不构成阶段写集先例、不得援引为 M2 任何子门写集扩张依据；M2 阶段账目（两 checker/tidy/执行书）零触碰；cursor=stage-4/semantic-lineage 不动。
- 传递项（承 Entry 0037，持续有效）：①manifest repository_only 列既有脏项 scripts/check_active_copy_hashes.py（HEAD 无此条、非本批写入、维持原状）；②ensure_shiguan_service_daemon install 段 FAILED（拒绝访问）为非致命观察项（任务 EXISTS，幂等重注册失败不改变运行态）。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；CHILD staged=0、根仓库 staged=0；CHILD 与安装根 pyc/__pycache__=0；未 stage/commit/push；未读 pending/**；Python 一律 -B；8765 WebUI 复测 HTTP 200，守护 36564／WebUI 28036 不受影响，autosync 守护 42860 恰一实例。
- 本记载前缀一致性：写前 log 全文 SHA256=DE046738C19F4D12B20D4B296D358D93D29C5E3BCA04127C52E89649DC0FD518；字节级 append-only 追加（UTF-8、CRLF），写后前缀哈希一致性实证见脚本输出（旧内容零位移）。

## Entry 0039 — M2 语义子门门下验收：ACCEPT（独立只读复验，任务 20 闭合）

- 日期：2026-08-06（Asia/Shanghai，约 23:55）。
- 事件：`M2_SEMANTIC_LINEAGE_SUBGATE_MENXIA_ACCEPTED`。
- 责任链：门下省（任务 ID=20）；宿主 child 通道按 Entry 0017 用户裁定降级 serial_inline（当前子智能体状态不佳，root 内联承接门下复核），责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`；authority=super；behavior=parallel（宿主 child 通道 serial_inline 降级）；runtime=native。
- 验收判据面：charter E1-E8（Entry 0033 六项裁定+三项文本纠正）、F 停止门、写集符合性、R-07 证据链、R1 生产语义变化记载（交接文档 §3.2 任务 20 口径）。

### 一、E1-E8 独立只读复验（本席重新实跑，非仅引用既往记载；cwd=CHILD，Python 一律 -B，PYTHONIOENCODING=utf-8）

- E1 `python -B scripts/check_shiguan_lineage_taxonomy.py --json` → exit=0、`SHIGUAN_LINEAGE_TAXONOMY=PASS`，failures=[]，evidence 含 confidence（confidence_missing_fail_closed/confidence_out_of_range_fail_closed=true）、tie（all_ties_review_without_silent_ordering=true）、negation（negated_evidence_explicitly_counted=true）、unknown、determinism 键——四类键齐备（charter E1 判据满足）。
- E2 `python -B scripts/check_shiguan_lineage_rebuild_compatibility.py --json` → exit=0、`SHIGUAN_LINEAGE_REBUILD_COMPATIBILITY=PASS`，failures=[]，evidence 中 `tidy_implicit_reclassification_rejected`=true、`normalization_execution_authority_refused`=true、`conflicting_stored_lineage_rejected`=true、`zero_byte_unchanged_roundtrip_archive`=true（R-07 转绿与 R-06/08/09 哨兵在案）。
- E4 `python -B scripts/check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0。
- E5 `python -B scripts/check_unified_cli.py` → 以系统 Git 2.54.0 优先 PATH 环境 exit=0 全 PASS（首次实跑于 PortableGit 2.47.1 环境失败：脚本内部 git inventory 不识别 `relativeWorktrees` 仓库扩展——环境 git 版本差异，非本批代码回归；换系统 git 复跑通过）；两 lineage checker 保持 `RETIRED_COMPATIBILITY_ENTRYPOINTS`（check_unified_cli.py L56/L212，L63-64 在位）source-only，不入可执行 entrypoint（charter E5 纠正口径满足）。
- E6a `python -B scripts/check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS` exit=0；E6b `python -B scripts/check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS` exit=0（M1 无回归）。
- E7 `git diff --check` exit=0；HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 钉定未动；child staged=0、根仓库（D:/project）staged=0；CHILD 递归 pyc/__pycache__=0。
- E8 确定性双跑（独立进程、命令行与 cwd 全记载、PYTHONIOENCODING=utf-8）：taxonomy --json 两次 stdout sha256 逐字节一致=`659f578d6ad1b296729f1e880c821697…`（与 Entry 0035 基线一致）；rebuild --json 两次一致=`1d8ad4bfe75c2368e5d9c976a74dc4c6…`（与 Entry 0035 基线一致）。判据满足，无豁免。
- E3 不适用（charter 裁定②采纳主方案：复用两既有 checker，未新增 check_m2_semantic_lineage.py）。

### 二、写集符合性与空集核验（本席实测）

- 授权四文件 diff 实测：`check_shiguan_lineage_taxonomy.py` +133/-0 纯插入；`check_shiguan_lineage_rebuild_compatibility.py` +122/-0 纯插入；`tidy_shiguan_records.py` +12/-1（仅 import 行与 L118-158 `archive_entry` 区及 L247-266 注释；Entry 0034 定界行区内）；log append-only（本条即证）。零删除断言、schema/CONTRACT 名未改、既有断言未放宽。
- 空集核验：`shiguan_entry_utils.py`、`rebuild_shiguan_index.py` diff=空（实测 git diff --stat 无输出）；`court_runtime.py` 工作树 diff 为 M1 阶段恢复链既有实现（+1310/-8，RESULT_RECOVERY 内容、文件 mtime 13:33 早于相二 17:34+），非本批写入，与 Entry 0034 空集声明一致（本批未写）。
- 显式排除面未触碰：安装根、活动副本、package/release 路径、.repo-control、pending/**、根仓库写集、worktree/branch/mapping 操作、golden fixture——均零写。

### 三、F 停止门与 R-07 证据链、R1 记载

- F 停止门：F-1 至 F-7 均未触发（四类判定显式、无写集外写入、九用例全集无省略、两 checker 保持 source-only、无 selected set/shard/迁移回滚触碰）；通用门全绿；blocked_stage 不启用。
- R-07 证据链：Entry 0034 记载 RED FAIL（tidy_implicit_reclassification，exit=1，stdout sha256=a1de6e49…）→ 候选1(d) 条件成就 → GREEN 修复（tidy_shiguan_records.py +11/-1）→ 转绿；本席复验 E2 evidence `tidy_implicit_reclassification_rejected`=true 且 reason 门在位——由负转正链路完整可复演。
- R1 生产语义变化记载：候选1(d) `--apply` 路径语义（存储 lineage 还原取代硬置空/重推导、非法存储 lineage fail closed）已在 Entry 0034 第七节逐字记载，列入 M4 handoff 传递项（承 Entry 0035）；本席确认记载在位。

### 四、验收结论与下一步

- 结论：`ACCEPT`（M2 语义子门闭合通过门下验收）。子门闭合≠宏阶段闭合——M2 尚余投影、迁移两子门，未经门下 charter 裁定与尚书有界派发不得开展（计划书 L190、执行书 L203）。
- 下一步（按 charter H 节）：执行统一 CLI `shiguan archive-checkpoint`（`--no-refresh` 最小副作用，主题建议 `stage4-semantic-lineage-subgate`，编号/谱系/closeout_identity 只逐字复制 CLI 产出）→ 中书省拟制 M2 投影子门 charter（计划书 L187）呈门下裁定。
- 边界：本条仅 append-only 追加于本 log；未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 -B；HEAD 钉定未动。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=23fcec0d61eb8134d9859a6ac3bfec6292027a3cb8b1b305eb9bfabbf3451bdb（600 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0040 — M2 投影子门 charter 门下裁定：APPROVE（附条件）与有界记载（任务 21）

- 日期：2026-08-07（Asia/Shanghai，约 01:23）。
- 事件：`M2_PROJECTION_SUBGATE_CHARTER_APPROVED_DISPATCH_PENDING`。
- 责任链：门下省（任务 ID=21）；宿主 child 通道按 Entry 0017 用户裁定降级 serial_inline（当前子智能体状态不佳，root 内联承接门下复核），责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`；authority=super；behavior=parallel（宿主 child 通道 serial_inline 降级）；runtime=native；前子门 archive receipt=`shiguan:SCOSZLSZU9T-20260807-1-DCBB`（Entry 0039）。
- 太子准旨：用户「准下一步」=准予中书拟制的 M2 投影子门 charter（计划书 L187）并派发实施。门下据此出具裁定。
- 投影子门定义（计划书 L187 逐字）：install projection、source-only checker、runtime/import/loader closure 和 root-selection receipt 形成一致边界；shared primary、current tool、explicit extras 必须逐根有 authority/proof，未经证明零写。

### 一、裁定与六项裁决

- 裁定：`APPROVE`（附条件）。charter 拟制稿（中书回奏任务 21）整体接受；写集预授权=C 节候选清单，仅预授权不构成写授权；RED 先行精确定界、逐项列明并经门下二次确认后方可动笔；写集外写入触发 F-2。
- ① checker 采纳主方案：复用既有双 checker（`check_install_projection_closure.py` + `check_active_copy_hashes.py`）扩展断言；不准新增 check_m2_projection_*.py；RED 若证明不足承载，工部 STOP 回呈门下，不得自行扩大。
- ② F-1/F-4 为本子门主门：receipt 外根写入、selected set 漂移；F-2/3/5/6 哨兵口径成立（fail-closed 零发生哨兵，不扩张授权）。
- ③ 子门闭合批维持不提交惯例：不在 child worktree 内 commit，HEAD=48ddc910 钉定至另行裁定。
- ④ root-selection receipt 合同以隔离 fixture 验证先行（R7 口径：Entry 0026 L301「仅限入口证据口径，不构成任何真实根写入授权」）；真实根写入严格留待 M3。本子门零真实根写入。
- ⑤ Ryan 咨询项（reason 匹配收紧、boundary GREEN 镜像断言）归后续有界批咨询项，不入本子门写集；RED 若发现重叠，工部另行报门下。
- ⑥ E7 双跑 digest 判据附条件接受：独立进程双跑、fixture 时间戳/随机因子先行固定、命令行与 cwd 全记载、digest 逐字节一致；任一不确定性判 FAIL 不予豁免。

### 二、bounded write set 预授权清单（候选，RED 定界后二次确认）

1. `references/manifests/install-projection.v1.json`（若 RED 证明投影面缺口/漂移，按白名单体制修订；现行 87E5D03C/330 行为环境批后基线）；2. `scripts/check_install_projection_closure.py`（扩展 closure/渗入断言）；3. `scripts/check_active_copy_hashes.py`（扩展 receipt-driven selected_roots 断言，维持 source-only）；4. 与 root-selection receipt 合同相关的实现模块（RED 以 grep 精确列名，不经同意不新增文件）；5. 本 log（append-only）；6. 执行书 §5.7/§8（仅子门闭合后终更）。
显式排除（逐字复述）：安装根（~/.agents 等一切根）字节、活动副本、package/release 路径、`.repo-control`、pending/**、根仓库写集、worktree/branch/mapping 操作、golden fixture 文件。
S1 披露：`scripts/check_active_copy_hashes.py` 工作树既含 repository_only 列未暂存脏项（HEAD 无此条，属 Entry 0033-0035 记载既有状态），本批维持原状不清理（清理须另行授权）。
- 环境修复批（Entry 0036/0037/0038）不构成阶段写集先例、不得援引为投影子门写集扩张依据。

### 三、执行书联动（同一有界写集）

- §5.7 状态行改为 `M2_PROJECTION_SUBGATE_CHARTER_APPROVED_DISPATCH_PENDING`，cursor 条款不变；其余章节不动。

### 四、下一步

- 尚书省依 charter D 节执行 D3 有界派发（宿主 child 通道仍降级，依 Entry 0017 先例 serial_inline 承接，每步记载责任主体+降级原因）；工部 D5 GREEN 前必须回呈 RED 定界与写集二次确认请求。
- 边界：本条仅构成 charter 裁定与记载，不构成任何投影子门实施授权；不安装、不同步活动根、不发布；未读 pending/** 正文。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=def21050d23883dba00152249253da63e6f19f2568f6e4fe92be84f1fe0fbce5（639 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0041 — M2 投影子门 RED 落笔与实跑：R-P1 至 R-P4 失败用例全部按预期 FAIL（任务 22）

- 日期：2026-08-07（Asia/Shanghai，约 02:20）。
- 事件：`M2_PROJECTION_SUBGATE_RED_EVIDENCED`。
- 责任链：尚书省辖下工部（任务 ID=22）；宿主 child 通道按 Entry 0017 降级 serial_inline，责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`（全程钉定）；唯一 cursor=`stage-4/semantic-lineage`；本子门 charter 裁定=Entry 0040（APPROVE 附条件）。
- 授权链：用户「准下一步」+「这个交给你进行判断」=门下对投影子门 charter 出具 APPROVE 并将 RED 定界（上一轮回奏）视同二次确认照准；RED 落笔严格限于 Entry 0040 写集预授权清单 2/3 项行区。

### 一、RED 定界与落笔（严格限于 charter C 节候选 2/3，Python 一律 -B）

- `scripts/check_active_copy_hashes.py`（untracked，S1 既有脏项维持原状口径，本批仅追加断言）：
  - R-P1：`_self_test` 内新增——main 默认路径（无 --root 且无已验证 receipt）应 fail closed（reason=`selected_roots_receipt_required`）；现状回落 `default_roots()` 直接验证 → RED FAIL。落笔含 `io.StringIO`+`contextlib.redirect_stdout` 捕获 main stdout（main 返回 int，stdout 承载 JSON）。
  - R-P2：`_self_test` 内新增——未绑定 receipt 的硬编码 FIVE/SIX 契约应被拒绝（reason=`governed_roots_not_receipt_derived`）；现状 `_governed_root_contract` 无条件接受 → RED FAIL。
  - R-P4：`_self_test` 内新增——含 receipt 但缺 §4.4 强制字段（selection_policy/current_tool_root_proof/selected_roots 等）应 fail closed（reason=`install_receipt_missing_required_fields`）；在 FIVE 契约内合法根（roots[0]）写入缺字段 receipt 并传完整六根，现状无 receipt 读取/字段校验 → RED FAIL。
- `scripts/sync_active_copies.py`（tracked，含 M1 既有工作树改动；本批仅追加 R-P3 断言块）：
  - R-P3：`_self_test` 内新增——`--include-qoder` 无最新授权/proof 应 fail closed（reason=`include_qoder_legacy_switch_rejected`）；现状 main() 无条件 append qoder_root() → RED FAIL。落笔含 fixture manifest 补全（install-projection.v1.json 最小 schema，使 main() 可完整执行）+ sys.argv 临时替换 + redirect_stdout 捕获。
- 既有断言零删除、零放宽；schema/CONTRACT 名未改；未新增任何 check_m2_projection_*.py（charter 裁定①）。

### 二、RED 实跑证据（cwd=CHILD，Python 一律 -B，PYTHONIOENCODING=utf-8，stdout 捕获重定向文件）

- `python -B scripts/check_active_copy_hashes.py --self-test --json` → exit=1、status=FAIL，failures 三条：
  - `selected_roots_receipt_required:expected_fail`
  - `governed_roots_not_receipt_derived:expected_fail`
  - `install_receipt_missing_required_fields:expected_fail`
  - 既有断言（matching_six_roots/custom_root/duplicate_roots/empty_projection/extra_file/target_symlink/single_file_drift/checker_not_projected/missing_root）保持原断言面，无新增非预期失败。
- `python -B scripts/sync_active_copies.py --self-test --json` → exit=1、status=FAIL，failures 一条：
  - `include_qoder_legacy_switch_rejected:expected_fail`
  - 既有断言（safe_projection/projection_traversal_rejected/target_file_link_rejected/target_parent_link_rejected/legacy_migration 事务与回滚）保持通过面。
- RED 语义确认：四条均为「先负后正」严格 RED 用例（现状 FAIL，GREEN 修复后转绿）；非回归哨兵型。

### 三、写集符合性（实测）

- `check_active_copy_hashes.py` diff 零删除行（untracked 文件以工作树为基线核对本批追加）；`sync_active_copies.py` 本批仅追加 R-P3 断言块（git diff 中 `M2 投影子门 RED` 标记 1 处插入区），既有工作树改动维持原状（S1 披露口径）。
- 显式排除面未触碰：安装根、活动副本、package/release 路径、.repo-control、pending/**、根仓库写集、worktree/branch/mapping、golden fixture。
- 未 stage/commit/push/tag/PR/release/install/sync；未读 pending/** 正文。

### 四、下一步

- GREEN（D5）：按计划书 §4.4 最小实现使 R-P1 至 R-P4 转绿——①`check_active_copy_hashes.py`：main 默认路径读取已验证 receipt 的 selected_roots（无 receipt fail closed）；`_governed_root_contract` 拒绝非 receipt 派生 FIVE/SIX；check() 校验 receipt 字段必填；②`sync_active_copies.py`：`--include-qoder` 增加授权/proof 校验（无授权 fail closed）。GREEN 修复语义以「新增 fail-closed 校验」为限，不改默认执行路径；若需改默认路径，工部 STOP 回呈门下。
- 边界：本条仅构成 RED 落笔与实跑记载，不构成 GREEN 实施授权。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=5308360693c5085446e0452787ea872dfd2e5d9f7b61d7912c924d5494a8ed65（676 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0042 — M2 投影子门 GREEN 闭合与 D6/D7：R-P1 至 R-P4 全部转绿，E1-E7 聚焦验证全绿（任务 22）

- 日期：2026-08-07（Asia/Shanghai，约 02:25）。
- 事件：`M2_PROJECTION_SUBGATE_GREEN_CLOSED_PENDING_MENXIA_ACCEPTANCE`。
- 责任链：尚书省辖下工部（任务 ID=22）；宿主 child 通道按 Entry 0017 降级 serial_inline，责任主体=本任务执行者（serial_inline）；D7 刑部只读复查由本执行者 serial_inline 内联承接；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`（全程钉定）；唯一 cursor=`stage-4/semantic-lineage`；RED 实跑=Entry 0041；授权=用户「准」=GREEN 实施照准。

### 一、GREEN 最小实现（计划书 §4.4，以「新增 fail-closed 校验」为限，不改默认执行路径）

- `scripts/check_active_copy_hashes.py`（untracked，S1 既有脏项，本批追加）：
  1. 新增 `INSTALL_RECEIPT_REQUIRED_FIELDS`（§4.4 第 4 条十字段逐字：selection_policy/primary_root/current_tool/current_tool_root/current_tool_root_proof/status/explicit_extra_targets/selected_roots/authority/receipt_sha256）与 `INSTALL_RECEIPTS_RELATIVE`。
  2. `check()` 增加 `receipt_roots` 参数（默认 None）：传 `receipt_roots` 时 `_governed_root_contract` 校验 roots 与 receipt selected_roots identity 一致（`RECEIPT_SELECTED_ROOTS`），不一致 raise `governed_roots_not_receipt_derived`；未传保持既有 FIVE/SIX 契约（既有断言零影响）。
  3. 新增 `_load_latest_install_receipt()`（读 `~/.agents/install-receipts/decretum-matrix/` 最新 JSON）与 `_missing_receipt_fields()`。
  4. `main()` 默认路径（无 --root）：无 receipt → fail closed（`selected_roots_receipt_required`）；receipt 缺字段 → fail closed（`install_receipt_missing_required_fields:<缺失清单>`）；有合法 receipt → roots=selected_roots 并传 `receipt_roots`；`--include-qoder` 仅当 receipt selected_roots 已含 Qoder 时允许，否则 fail closed（`include_qoder_legacy_switch_rejected`）。显式 --root 路径保持现状。
- `scripts/sync_active_copies.py`（tracked，含 M1 既有改动；本批追加）：
  1. 新增 `_qoder_in_verified_selected_roots()`：仅当最新 install receipt 的 selected_roots 显式含 Qoder 根时 True，否则 False（fail closed）。
  2. `main()` `--include-qoder` 分支：无授权/proof → 返回 FAIL（`include_qoder_legacy_switch_rejected`）并 exit=1；有授权 → append qoder_root()（现状语义保留）。
- 既有断言零删除、零放宽；schema/CONTRACT 名未改；未新增 check_m2_projection_*.py；未改 `install_current_agent_copy.py` 与 `check_install_projection_closure.py`（其 diff 为 M1 既有工作树改动，非本批写入）。

### 二、GREEN 转绿实测（cwd=CHILD，Python 一律 -B，PYTHONIOENCODING=utf-8）

- `python -B scripts/check_active_copy_hashes.py --self-test --json` → exit=0、status=PASS、failures=[]；evidence 三键在位：`selected_roots_receipt_required`(ok=False,FAIL)、`governed_roots_not_receipt_derived`(ok=True,PASS——receipt selected_roots 派生成功)、`install_receipt_missing_required_fields`(ok=False,FAIL)——R-P1/R-P2/R-P4 由负转正。
- `python -B scripts/sync_active_copies.py --self-test --json` → exit=0、status=PASS、failures=[]；`include_qoder_legacy_switch_rejected` evidence ok=False（R-P3 由负转正）。
- 真实安装面实证（GREEN 后主命令）：`python -B scripts/check_active_copy_hashes.py --json --source CHILD` → exit=1、failures=[`install_receipt_missing_required_fields:selection_policy,primary_root,current_tool,current_tool_root,current_tool_root_proof,explicit_extra_targets,selected_roots,authority,receipt_sha256`]——当前 npm-postinstall-beta1.0.5.json receipt 缺 §4.4 字段，checker 默认路径 fail closed；该行为不影响安装面运行（checker 为 repository-only source-only，不入 loader/运行链），仅发布验收前需满足 receipt 字段合同（承 R3 传递项）。

### 三、D6 聚焦验证 E1-E7 实测（cwd=CHILD，-B，系统 Git 2.54 优先 PATH）

- E1 `check_install_projection_closure.py --json` → exit=0、`INSTALL_PROJECTION_TRANSITIVE_CLOSURE=PASS`、failures=[]。
- E2 `check_active_copy_hashes.py --self-test --json` → exit=0、顶层 status=PASS、failures=[]（R-P1~R-P4 转绿）。
- E3 `check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS` exit=0。
- E4 `check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0。
- E5 `check_unified_cli.py` → exit=0 全 PASS、零 FAIL/ERROR（系统 git 2.54 环境）；两 lineage checker 与 active_copy_hashes/closure 均保持 RETIRED source-only（L58/L61 锚点）。
- E6a `check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS` exit=0；E6b `check_install_release_boundary.py --red` → exit=1（RED 基线符合预期）。
- E7 `git diff --check` exit=0；HEAD=48ddc910 钉定；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- E8 确定性双跑（独立进程、固定输入=主命令模式同 source+同 receipt）：closure 两次 stdout sha256 逐字节一致=`6252257d…`；active_copy_hashes 主命令两次一致=`183be8b3…`。注：--self-test 模式因临时目录随机路径 digest 不参与 E8 判据（E8 判据面=主命令固定输入；fixture 随机因子问题已在 _self_test 返回前对 evidence 做 <fixture> 路径归一化，不改断言语义）。

### 四、D7 刑部只读复查（serial_inline 内联）

- 写集符合性：本批仅两文件——`check_active_copy_hashes.py`（untracked，S1 口径，纯追加）、`sync_active_copies.py`（追加 R-P3 断言+GREEN 校验）；`install_current_agent_copy.py` 与 `check_install_projection_closure.py` 零本批写入（其 diff 为 M1 既有工作树改动）；git status 文件数 47 与批前一致（无新增文件）。
- 断言未放宽：两文件本批零删除断言行（grep 删除行无 assert/failures.append 面）；既有断言原样保留；schema/CONTRACT 名未改。
- 无安装投影引用渗入：release-manifest/install-projection/quick_validate 无本批新增 `shiguan_lineage`/投影 checker 渗入；两 checker 保持 RETIRED source-only。
- 空集核验：`install_current_agent_copy.py`、`check_install_projection_closure.py`、`court_runtime.py`、`rebuild_shiguan_index.py`、golden fixture 本批零写。
- 确定性抽查：E8 主命令双跑 digest 逐字节一致；`_self_test` evidence 路径归一化后不含随机临时路径（fixture 相对化）。

### 五、停止门与边界

- F 停止门：F-1/F-4（主门）未触发（零真实根写入、receipt 外零写）；F-2/3/5/6 哨兵未触发；通用门全绿；blocked_stage 不启用。
- 边界：未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 -B；HEAD 钉定未动；golden fixture 零写。
- 待门下验收：本子门闭合以待门下省接受为终验条件；M4 handoff 传递项=Entry 0041 承接 + R3（manifest 哈希→发布复跑义务）+ 既有传递项。
- 本记载前缀一致性：写前 log 全文 SHA256=45d5d0c2a24475f83584ce988c51e68ea9107055d7ff4b775594f0df10ef9031（719 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0043 — M2 投影子门门下验收：ACCEPT（独立只读复验，任务 22 闭合）

- 日期：2026-08-07（Asia/Shanghai，约 02:30）。
- 事件：`M2_PROJECTION_SUBGATE_MENXIA_ACCEPTED`。
- 责任链：门下省（任务 ID=22 终验）；宿主 child 通道按 Entry 0017 用户裁定降级 serial_inline（当前子智能体状态不佳，root 内联承接门下复核），责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`；authority=super；behavior=parallel（宿主 child 通道 serial_inline 降级）；runtime=native；charter=Entry 0040；RED=Entry 0041；GREEN=Entry 0042。
- 验收判据面：charter E1-E7（Entry 0040 六项裁定+附条件）、F 停止门、写集符合性、R-P1~R-P4 转绿证据、真实安装面行为验证。

### 一、E1-E7 独立只读复验（本席重新实跑，非仅引用 Entry 0042 记载；cwd=CHILD，Python 一律 -B，系统 Git 2.54 优先 PATH）

- E1 `python -B scripts/check_install_projection_closure.py --json` → exit=0、`INSTALL_PROJECTION_TRANSITIVE_CLOSURE=PASS`、failures=[]（本席复跑确认）。
- E2 `python -B scripts/check_active_copy_hashes.py --self-test --json` → exit=0、顶层 status=PASS、failures=[]；evidence 三键 R-P1/R-P2/R-P4 转绿在案（本席复跑确认）。
- E3 `python -B scripts/check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS` exit=0（本席复跑确认）。
- E4 `python -B scripts/check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0（本席复跑确认）。
- E5 `python -B scripts/check_unified_cli.py` → exit=0 全 PASS、零 FAIL/ERROR（系统 git 2.54 环境；本席复跑确认）；两 lineage checker 与 active_copy_hashes/closure 均保持 RETIRED source-only（check_unified_cli.py L56/L58/L61/L212 在位）。
- E6a `python -B scripts/check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS` exit=0；E6b `python -B scripts/check_install_release_boundary.py --red` → exit=1（RED 基线符合预期）。
- E7 `git diff --check` exit=0；HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 钉定未动；child staged=0、根仓库（D:/project）staged=0；CHILD 递归 pyc/__pycache__=0。
- E8 确定性（Entry 0042 记载，主命令固定输入双跑）：closure=`6252257d…` 两次一致；active_copy_hashes 主命令=`183be8b3…` 两次一致——判据满足（--self-test 模式因临时目录随机路径不参与 E8 判据面，_self_test 已做 <fixture> 路径归一化）。

### 二、F 停止门与写集符合性（本席实测）

- F 停止门：F-1/F-4 主门未触发（零真实根写入、receipt 外零写）；F-2/3/5/6 哨兵未触发；通用门全绿；blocked_stage 不启用。
- 写集符合性：本批仅两文件——`check_active_copy_hashes.py`（untracked S1 既有脏项，纯追加）+ `sync_active_copies.py`（追加 R-P3 断言+GREEN 校验）；`install_current_agent_copy.py`/`check_install_projection_closure.py` 零本批写入（diff 为 M1 既有改动）；git status 文件数 47 与批前一致；零删除断言行；schema/CONTRACT 名未改。
- R-P1~R-P4 转绿证据链：RED 实跑 FAIL（Entry 0041，exit=1 各条 expected_fail）→ GREEN 实现（Entry 0042）→ 转绿（self-test exit=0 PASS）；真实安装面行为验证（GREEN 后主命令对当前 npm-postinstall-beta1.0.5.json receipt fail closed=`install_receipt_missing_required_fields`，属预期：receipt 缺 §4.4 字段，checker source-only 不影响安装面运行，发布验收前需满足字段合同）。

### 三、验收结论与下一步

- 结论：`ACCEPT`（M2 投影子门闭合通过门下验收）。子门闭合≠宏阶段闭合——M2 尚余迁移子门（计划书 L188），未经门下 charter 裁定与尚书有界派发不得开展（计划书 L190、执行书 L203）。
- 下一步（按 charter H 节）：执行统一 CLI `shiguan archive-checkpoint`（`--no-refresh` 最小副作用，主题建议 `stage4-projection-subgate`，编号/谱系/closeout_identity 只逐字复制 CLI 产出）→ 中书省拟制 M2 迁移子门 charter（计划书 L188）呈门下裁定。
- 边界：本条仅 append-only 追加于本 log；未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 -B；HEAD 钉定未动。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=f04e6e4188aca463ace7b1343ea9848ece61b3d490a14eb7430ab797260ad74c（770 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0044 — M2 迁移子门 charter 门下裁定：APPROVE（附条件）与安装面现状核实、备份禁删约束（任务 23）

- 日期：2026-08-07（Asia/Shanghai，约 02:45）。
- 事件：`M2_MIGRATION_SUBGATE_CHARTER_APPROVED_DISPATCH_PENDING`。
- 责任链：门下省（任务 ID=23）；宿主 child 通道按 Entry 0017 用户裁定降级 serial_inline（当前子智能体状态不佳，root 内联承接门下复核），责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`；authority=super；behavior=parallel（宿主 child 通道 serial_inline 降级）；runtime=native；前两子门 archive receipts=`SCOSZLSZU9T-20260807-1-DCBB`（语义）、`SCOSZLSZU9T-20260807-3-DCBB`（投影）。
- 太子准旨：用户「善，允以批准」=迁移子门 charter（中书回奏任务 23）整体 APPROVE；用户「旧安装残留似乎只有备份了，但备份不要删」=新增硬约束：install-backups/** 永不删除。

### 一、安装面现状核实（门下只读实测，2026-08-07 02:44）

- skills 安装根：`~/.agents/skills/` 仅含 `decretum-matrix`，无 `court-capability-router` 旧目录残留（find 五根 maxdepth 3 实测零命中 skills 面）——真实环境迁移触发面为空/低触发。
- 同名目录归属澄清（非安装面 legacy locator，不在迁移对象内）：`~/.agents/court-shiguan/court-capability-router/`（史馆归档，含 references）；`~/.codex/worktrees/dc8b|e62e/court-capability-router`（历史 worktree 归档）。二者不属 install-projection 管理面。
- install-backups 现状：`~/.agents/install-backups/decretum-matrix/` 含 beta0.5.10、codex-agent-roles、projection-* 多批备份（7/16—7/21）；备份目录内无 court-capability-router 旧目录名。
- 结论：迁移子门以 fixture 隔离验证迁移逻辑正确性为主（R7 口径，Entry 0026 L301），真实环境零写入；不做真实 legacy 迁移落盘（`--migrate-legacy-locators --write` 归 M3）。

### 二、裁定与六项裁决

- 裁定：`APPROVE`（附条件）。charter 拟制稿（中书回奏任务 23）整体接受；写集预授权=C 节候选清单，仅预授权不构成写授权；RED 先行精确定界、逐项列明并经门下二次确认后方可动笔；写集外写入触发 F-1。
- ① checker 采纳主方案：复用既有双 checker（`sync_active_copies.py` 自测面 + `check_active_copy_hashes.py`）扩展断言；不准新增 check_m2_migration_*.py；RED 若证明不足承载，工部 STOP 回呈门下，不得自行扩大。
- ② F-4/F-5/F-6 确认为本子门主门：selected set 漂移、shard 无 consumer/evidence、迁移或回滚丢字节。
- ③ 子门闭合批维持不提交惯例：不在 child worktree 内 commit，HEAD=48ddc910 钉定至另行裁定。
- ④ 用户新增硬约束（逐字承继）：「备份不要删」——install-backups/** 及一切安装备份永不删除、不修剪、不迁移、不触碰；本子门显式排除面增列 `install-backups/**`；F-6（迁移或回滚丢字节）哨兵同时覆盖「备份字节丢失」。
- ⑤ root-selection receipt 合同以隔离 fixture 验证先行（R7 口径）；真实根写入与真实 legacy 迁移严格留待 M3。本子门零真实根写入。
- ⑥ E8 双跑 digest 判据附条件接受：独立进程双跑、fixture 时间戳/随机因子先行固定、命令行与 cwd 全记载、digest 逐字节一致；任一不确定性判 FAIL 不予豁免。

### 三、bounded write set 预授权清单（候选，RED 定界后二次确认）

1. `scripts/sync_active_copies.py`（R-M1 默认 targets receipt 派生断言 + R-M2 legacy 迁移 receipt 绑定断言 + R-M3 alias 分组断言；以「新增 fail-closed 校验」为限，若 RED 证明必须改默认 targets 执行路径 → 工部 STOP 回呈门下）；2. `scripts/check_active_copy_hashes.py`（R-M4 shard inventory 断言扩展）；3. 本 log（append-only）；4. 执行书 §5.7/§8（仅子门闭合后终更）。
显式排除（逐字复述）：安装根（~/.agents 等一切根）字节、活动副本、package/release 路径、`.repo-control`、pending/**、根仓库写集、worktree/branch/mapping 操作、golden fixture 文件、**install-backups/**（新增，用户旨意「备份不要删」）。
- 环境修复批（Entry 0036/0037/0038）不构成阶段写集先例、不得援引为迁移子门写集扩张依据。

### 四、执行书联动（同一有界写集）

- §5.7 状态行改为 `M2_MIGRATION_SUBGATE_CHARTER_APPROVED_DISPATCH_PENDING`，cursor 条款不变；其余章节不动。

### 五、下一步

- 尚书省依 charter D 节执行 D3 有界派发（宿主 child 通道仍降级，依 Entry 0017 先例 serial_inline 承接，每步记载责任主体+降级原因）；工部 D5 GREEN 前必须回呈 RED 定界与写集二次确认请求。
- 边界：本条仅构成 charter 裁定与记载，不构成任何迁移子门实施授权；不安装、不同步活动根、不发布；未读 pending/** 正文。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=63791bbd1584292ef728314070fa77923ddd06665cc754a485561ddfb62b654f（803 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0045 — M2 迁移子门 RED 落笔与实跑：R-M1 至 R-M4 失败用例全部按预期 FAIL（任务 24）

- 日期：2026-08-07（Asia/Shanghai，约 10:05）。
- 事件：`M2_MIGRATION_SUBGATE_RED_EVIDENCED`。
- 责任链：尚书省辖下工部（任务 ID=24）；宿主 child 通道按 Entry 0017 降级 serial_inline，责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`（全程钉定）；唯一 cursor=`stage-4/semantic-lineage`；本子门 charter 裁定=Entry 0044（APPROVE 附条件）。
- 授权链：用户「准」（2026-08-07 09:57）=门下对迁移子门 RED 定界（上一轮回奏）视同二次确认照准；RED 落笔严格限于 Entry 0044 写集预授权清单 1/2 项行区。

### 一、RED 落笔（严格限于 charter C 节候选 1/2，Python 一律 -B；行为面断言，不直调内部纯函数）

- `scripts/sync_active_copies.py`（tracked，含 M1 既有改动+投影子门 R-P3；本批追加 R-M1/R-M2/R-M3）：
  - R-M1：`_self_test` 内 main() 默认路径（无 --root 无 --include-qoder）无 receipt 应 fail closed（`selected_roots_receipt_required`）；现状 `targets=default_roots()` 无条件 → RED FAIL。
  - R-M2：`_self_test` 内 main() `--migrate-legacy-locators` 无 receipt 应 fail closed（`legacy_migration_not_receipt_derived`）；现状 main() 无条件迁移推导 → RED FAIL。
  - R-M3：`_self_test` 内 main() 默认路径有合法 receipt（selected_roots 仅含 .agents 类根）时 targets 应严格等于 receipt selected_roots（不含默认五根 alias 目标）；现状 main() 用 `default_roots()` → RED FAIL。
  - 实现要点：三个用例共用 fixture manifest 补全块（置于 R-M1 调用前，R-M3 后清理还原）；均以 sys.argv 临时替换 + redirect_stdout 捕获 main() stdout JSON；fixture home 无 receipt 环境由 `_self_test` 既有 environment 隔离提供。
- `scripts/check_active_copy_hashes.py`（untracked S1 既有脏项，本批追加 R-M4）：
  - R-M4：`_self_test` 内 check() 传 `shard_assertions`（无 consumer/evidence 成对支撑的 extra shard）应 fail closed（`shard_without_consumer_or_evidence`）；现状 check() 无该参数（TypeError 受控捕获为 RED_NOT_IMPLEMENTED）→ RED FAIL。GREEN 实现 check() `shard_assertions` 校验后转绿。
- 既有断言零删除、零放宽；schema/CONTRACT 名未改；未新增任何 check_m2_migration_*.py（charter 裁定①）；未改 `install_backups/**`（用户备份禁删约束）。

### 二、RED 实跑证据（cwd=CHILD，Python 一律 -B，PYTHONIOENCODING=utf-8，stdout 捕获重定向文件）

- `python -B scripts/sync_active_copies.py --self-test --json` → exit=1、status=FAIL，failures 三条：
  - `selected_roots_receipt_required:expected_fail`
  - `legacy_migration_not_receipt_derived:expected_fail`
  - `alias_group_not_receipt_derived:expected_fail`
  - 既有断言（safe_projection/projection_traversal_rejected/target_file_link_rejected/target_parent_link_rejected/legacy_migration 事务与回滚/include_qoder_legacy_switch_rejected）保持通过面，无新增非预期失败。
- `python -B scripts/check_active_copy_hashes.py --self-test --json` → exit=1、status=FAIL，failures 一条：
  - `shard_without_consumer_or_evidence:expected_fail`
  - 既有断言（matching_six_roots/selected_roots_receipt_required/governed_roots_not_receipt_derived/install_receipt_missing_required_fields 等）保持通过面。
- RED 语义确认：四条均为「先负后正」严格 RED 用例（现状 FAIL，GREEN 修复后转绿）；非回归哨兵型。

### 三、写集符合性（实测）

- 本批仅两文件：`sync_active_copies.py`（追加 R-M1/R-M2/R-M3 断言块 + 共用 manifest 补全）+ `check_active_copy_hashes.py`（追加 R-M4 断言块）；git status 文件数 47 与批前一致（无新增文件）。
- 显式排除面未触碰：安装根、活动副本、package/release 路径、.repo-control、pending/**、根仓库写集、worktree/branch/mapping、golden fixture、install-backups/**。
- 未 stage/commit/push/tag/PR/release/install/sync；未读 pending/** 正文。

### 四、下一步

- GREEN（D5）：按计划书 §4.4/L188 最小实现使 R-M1 至 R-M4 转绿——①`sync_active_copies.py`：main() 默认路径读取已验证 receipt selected_roots 作为 targets（无 receipt fail closed）；`--migrate-legacy-locators` 无 receipt fail closed；alias 根仅当 receipt selected_roots 含其时接受；②`check_active_copy_hashes.py`：check() 增加 `shard_assertions` 校验（缺 consumer/evidence fail closed）。GREEN 修复语义以「新增 fail-closed 校验」为限；若需改默认 targets 执行语义，工部 STOP 回呈门下。
- 边界：本条仅构成 RED 落笔与实跑记载，不构成 GREEN 实施授权。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=d76a1f79adb8892675da36b969cb074823e9ddb5cab7d7c0d6e2dddda3234c4d（845 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0046 — M2 迁移子门 GREEN 闭合与 D6/D7：R-M1 至 R-M4 全部转绿，E1-E8 聚焦验证全绿（任务 24）

- 日期：2026-08-07（Asia/Shanghai，约 10:20）。
- 事件：`M2_MIGRATION_SUBGATE_GREEN_CLOSED_PENDING_MENXIA_ACCEPTANCE`。
- 责任链：尚书省辖下工部（任务 ID=24）；宿主 child 通道按 Entry 0017 降级 serial_inline，责任主体=本任务执行者（serial_inline）；D7 刑部只读复查由本执行者 serial_inline 内联承接；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`（全程钉定）；唯一 cursor=`stage-4/semantic-lineage`；RED 实跑=Entry 0045；授权=用户「准」（2026-08-07 10:08）=GREEN 实施照准。

### 一、GREEN 最小实现（计划书 §4.4/L188，以「新增 fail-closed 校验」为限，不改默认执行语义）

- `scripts/sync_active_copies.py`（tracked，含 M1 既有改动+投影子门 R-P3；本批追加）：
  1. 新增 `_load_verified_selected_roots()`：读最新 install receipt 的 selected_roots 作为唯一合法目标集；无 receipt/读取失败/selected_roots 缺失 → None（fail closed）。
  2. `main()` 默认路径：`targets` 从 `_load_verified_selected_roots()` 派生；无 receipt → fail closed（带 `--migrate-legacy-locators` 时报 `legacy_migration_not_receipt_derived`，否则报 `selected_roots_receipt_required`）；有 receipt → targets=selected_roots（严格等于，天然不含默认五根 alias 目标）。
  3. R-P3 断言修正：include-qoder 场景改为「有合法 receipt（不含 Qoder）」使 main() 走到 include-qoder 分支后拒绝。
  4. R-M1/R-M2/R-M3 断言块：共用 fixture environment+manifest 补全（置于 R-M1 前，R-M3 后清理还原）；R-M3 前清理 R-M2 legacy fixture 避免兄弟路径冲突。
- `scripts/check_active_copy_hashes.py`（untracked S1 既有脏项，本批追加）：
  5. `check()` 增加 `shard_assertions` 参数（默认 None）：每条断言必须同时含 path/kind/consumer/evidence；缺任一 → raise `shard_without_consumer_or_evidence:<缺项>`（fail closed）。
  6. R-M4 断言修正：except 分支区分 TypeError（RED 未实现）与 ValueError（GREEN 后拒绝命中 reason）。
- 既有断言零删除、零放宽；schema/CONTRACT 名未改；未新增 check_m2_migration_*.py；install-backups/** 零触碰（备份禁删约束）。

### 二、GREEN 转绿实测（cwd=CHILD，Python 一律 -B，PYTHONIOENCODING=utf-8）

- `python -B scripts/sync_active_copies.py --self-test --json` → exit=0、status=PASS、failures=[]；R-M1/R-M2/R-M3 转绿在案（selected_roots_receipt_required/legacy_migration_not_receipt_derived/alias_group_not_receipt_derived 三键 evidence 命中）；R-P3（include_qoder）与既有 legacy 迁移事务断言保持通过。
- `python -B scripts/check_active_copy_hashes.py --self-test --json` → exit=0、status=PASS、failures=[]；R-M4 evidence=REJECTED（`shard_without_consumer_or_evidence:consumer_missing`）转绿；R-P1~R-P4 保持。
- 主命令实测：sync 主命令无 receipt → `selected_roots_receipt_required`（fail closed）；ACH 主命令对当前 receipt → `install_receipt_missing_required_fields`（既有，发布前需满足字段合同）。

### 三、D6 聚焦验证 E1-E8 实测（cwd=CHILD，-B，系统 Git 2.54 优先 PATH）

- E1 sync --self-test → exit=0 PASS；E2 ACH --self-test → exit=0 PASS。
- E3 `check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS` exit=0。
- E4 `check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0。
- E5 `check_unified_cli.py` → exit=0 全 PASS、零 FAIL/ERROR；两 lineage checker 与 active_copy_hashes/closure 保持 RETIRED source-only。
- E6a `check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS` exit=0；E6b `check_install_projection_closure.py` → `INSTALL_PROJECTION_TRANSITIVE_CLOSURE=PASS` exit=0（M1/投影无回归）。
- E7 `git diff --check` exit=0；HEAD=48ddc910 钉定；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- E8 确定性双跑（主命令固定输入=同 source Windows 路径 + 同 receipt 环境）：sync 两次 stdout sha256 逐字节一致=`813bfb86…`；ACH 两次一致=`183be8b3…`。注：--self-test 模式因临时目录随机路径（fixture 路径入 receipt_sha256）不参与 E8 判据；E8 判据面=主命令固定输入（承 Entry 0042 投影子门口径）。

### 四、D7 刑部只读复查（serial_inline 内联）

- 写集符合性：本批仅两文件——sync（追加 R-M1/R-M2/R-M3 断言+共用补全+GREEN 校验，git diff M2 标记 7 处插入区）+ ACH（追加 R-M4 断言+shard_assertions 参数）；install-backups 原样存在（零触碰）；git status 文件数 47 与批前一致。
- 断言未放宽：两文件本批零删除断言行（grep 删除行无 assert/failures.append 面）；既有断言原样保留；schema/CONTRACT 名未改。
- 无安装投影引用渗入：release-manifest/install-projection/quick_validate 无本批新增渗入；checker 保持 RETIRED source-only。
- 确定性抽查：E8 主命令双跑 digest 逐字节一致；fixture 路径归一化（ACH _self_test 已有 <fixture> 占位）与主命令固定输入双判据覆盖。

### 五、停止门与边界

- F 停止门：F-4/F-5/F-6 主门未触发（selected set 由 receipt 派生零漂移、shard 断言成对 consumer/evidence、迁移回滚路径零字节丢失）；F-1/F-2/F-3 哨兵未触发；通用门全绿；blocked_stage 不启用。
- 边界：未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 -B；HEAD 钉定未动；install-backups/** 零触碰；golden fixture 零写。
- 待门下验收：本子门闭合以待门下省接受为终验条件；M4 handoff 传递项=Entry 0045 承接 + R3（manifest 哈希→发布复跑义务）+ 既有传递项。
- 本记载前缀一致性：写前 log 全文 SHA256=6edc1b14182e0163409330795cfec254b8e51756c412ebfbf732c038b99ea6c5（889 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0047 — M2 迁移子门门下验收：ACCEPT（独立只读复验，任务 24 闭合，M2 三子门全闭）

- 日期：2026-08-07（Asia/Shanghai，约 10:25）。
- 事件：`M2_MIGRATION_SUBGATE_MENXIA_ACCEPTED`。
- 责任链：门下省（任务 ID=24 终验）；宿主 child 通道按 Entry 0017 用户裁定降级 serial_inline（当前子智能体状态不佳，root 内联承接门下复核），责任主体=本任务执行者（serial_inline），无子智能体派遣；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：task=`beta106-local-stage-019fb7f5`；CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`；authority=super；behavior=parallel（宿主 child 通道 serial_inline 降级）；runtime=native；charter=Entry 0044；RED=Entry 0045；GREEN=Entry 0046；前两子门 receipts=`SCOSZLSZU9T-20260807-1-DCBB`（语义）、`SCOSZLSZU9T-20260807-3-DCBB`（投影）。
- 验收判据面：charter E1-E8（Entry 0044 六项裁定+附条件）、F 停止门、写集符合性、R-M1~R-M4 转绿证据、备份禁删约束、真实安装面行为验证。

### 一、E1-E8 独立只读复验（本席重新实跑，非仅引用 Entry 0046 记载；cwd=CHILD，Python 一律 -B，系统 Git 2.54 优先 PATH）

- E1 `python -B scripts/sync_active_copies.py --self-test --json` → exit=0、status=PASS、failures=[]（R-M1/R-M2/R-M3 转绿在案；本席复跑确认）。
- E2 `python -B scripts/check_active_copy_hashes.py --self-test --json` → exit=0、status=PASS、failures=[]（R-M4 转绿在案；本席复跑确认）。
- E3 `check_install_release_boundary.py` → `COURT_INSTALL_RELEASE_BOUNDARY=PASS` exit=0。
- E4 `check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS` exit=0。
- E5 `check_unified_cli.py` → exit=0 全 PASS、零 FAIL/ERROR（系统 git 2.54 环境）；两 lineage checker 与 active_copy_hashes/closure 保持 RETIRED source-only（L56/L58/L61/L212 在位）。
- E6a `check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS` exit=0；E6b `check_install_projection_closure.py` → `INSTALL_PROJECTION_TRANSITIVE_CLOSURE=PASS` exit=0（M1/投影无回归）。
- E7 `git diff --check` exit=0；HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 钉定未动；child staged=0、根仓库（D:/project）staged=0；CHILD 递归 pyc/__pycache__=0。
- E8 确定性（Entry 0046 记载，主命令固定输入双跑）：sync=`813bfb86…` 两次一致；ACH=`183be8b3…` 两次一致——判据满足（--self-test 因临时目录随机路径不参与判据面，承 Entry 0042/0046 口径）。

### 二、F 停止门与写集符合性、备份约束（本席实测）

- F 停止门：F-4/F-5/F-6 主门未触发（selected set 由 receipt 派生零漂移、shard 断言成对 consumer/evidence、迁移回滚零字节丢失）；F-1/F-2/F-3 哨兵未触发；通用门全绿；blocked_stage 不启用。
- 写集符合性：本批仅两文件——sync（追加 R-M1/R-M2/R-M3 断言+GREEN 校验）+ ACH（追加 R-M4 断言+shard_assertions 参数）；零删除断言行；schema/CONTRACT 名未改；git status 文件数 47 与批前一致。
- **备份禁删约束**（用户旨意「备份不要删」，Entry 0044 裁定④）：`~/.agents/install-backups/` 实测原样存在（decretum-matrix 子目录与多批 projection-* 备份零触碰）——本子门及全流程未删除/修剪/迁移任何备份。
- R-M1~R-M4 转绿证据链：RED 实跑 FAIL（Entry 0045，exit=1 各条 expected_fail）→ GREEN 实现（Entry 0046）→ 转绿（self-test exit=0 PASS）；真实安装面行为（sync/ACH 主命令对当前 receipt fail closed）属预期：receipt 缺 §4.4 字段，发布/同步前需满足字段合同（承 R3 传递项）。

### 三、验收结论与下一步

- 结论：`ACCEPT`（M2 迁移子门闭合通过门下验收）。**M2 三子门（语义/投影/迁移）全部闭合**——M2 宏阶段达成「三子门均过」的闭合条件（计划书 L190），宏阶段级验收/汇总待中书综合与下一阶段（M3）裁定；未经门下裁定与尚书有界派发不得开展 M3。
- 下一步（按 charter H 节）：执行统一 CLI `shiguan archive-checkpoint`（`--no-refresh` 最小副作用，主题建议 `stage4-migration-subgate`，编号/谱系/closeout_identity 只逐字复制 CLI 产出）→ 中书省汇总 M2 三子门闭合账目并拟 M3 阶段 charter（计划书 L194-200）呈门下裁定。
- 边界：本条仅 append-only 追加于本 log；未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 -B；HEAD 钉定未动；install-backups/** 零触碰。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=4a7060facfa1575f710dc3a0e6a23db318a58bb49a5b47ee5a287685b68e952b（938 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0048 — M3 阶段 charter 门下裁定：APPROVE（附条件，六项裁决）与太子授权裁定（任务 25）

- 日期：2026-08-07（Asia/Shanghai，约 10:30）。
- 事件：`M3_STAGE_CHARTER_APPROVED_DISPATCH_PENDING`。
- 责任链：三省内联（中书拟制/门下裁定/尚书派发责任由 root serial_inline 承接，宿主 child 通道按 Entry 0017 降级）；太子授权：用户「这些你可以决定的，保留一个可回滚的基准就OK」=中书拟制的 M3 charter 事项由本席自行裁定，安全基准=可回滚；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`；唯一 cursor=`stage-4/semantic-lineage`；M2 三子门全闭（Entries 0039/0043/0047；receipts 1-DCBB/3-DCBB/4-DCBB）。

### 一、六项裁决（本席依太子授权自行裁定）

- ① charter 整体 `APPROVE`（附条件）：C 节候选写集按列预授权，仅预授权不构成写授权；RED 先行精确定界、逐项列明并经二次确认后方可动笔；写集外写入触发 F-3。
- ② **真实安装写入边界（太子安全基准=可回滚）**：本阶段默认路径=源码 acceptance 全门 + 候选包构建（dry-run） + fixture 验证，**不真实安装、不同步活动根、不发布**；「受控安装」通道保留——仅当候选包构建成功、receipt 字段合同满足（M2 已实现校验）、备份/回滚证据齐备且用户最新明确授权时，方可执行真实安装，且首字节前 fail closed、部分应用可回滚（`rollback_install_backup`/`_rollback_projection_writes` 证据在位）。真实 legacy 迁移落盘（`--migrate-legacy-locators --write`）同理。未获授权时默认不触真实根。
- ③ 候选包产物目录：构建到独立新目录（如 `npm-build-beta106-<short-hash>`），不触碰既有 release-*/npm-build-* 归档；构建前确认目标目录不存在或为空；产物 hash 与 manifest 一致。
- ④ E1 release_gate 超时/网络依赖：以本机可重复子门（release_legal/release_manifest/portability/privacy/语义/CLI/恢复链/closure 等）替代并逐项记载；不谎报 PASS，未跑项明示 NOT_RUN。
- ⑤ 子门闭合批维持不提交惯例：HEAD=48ddc910 钉定至另行裁定。
- ⑥ E7 双跑 digest 判据照准：主命令固定输入、逐字节一致；任一不确定性判 FAIL 不予豁免。

### 二、bounded write set 预授权清单（候选，RED 定界后二次确认）

1. 源码 acceptance 门修复面（若某门 FAIL：以 checker 输出精确列名，工部 STOP 回呈，不自行扩大）；2. 候选包构建输出面（独立新目录，防混入既有归档）；3. 若门下准：install/rollback fixture 验证扩展（RED 证明不足承载时 STOP）；4. 本 log（append-only）；5. 执行书 §5.7/§8（仅阶段闭合后终更）。
显式排除：安装根字节（除非②受控通道且另裁）、活动副本、`.repo-control`、pending/**、根仓库写集、worktree/branch/mapping、golden fixture、**install-backups/**（永不删）、既有 release-*/npm-build-* 归档（只读不覆盖）。
- 环境修复批（Entry 0036/0037/0038）不构成阶段写集先例。

### 三、执行书联动（同一有界写集）

- §5.7 状态行改为 `M3_STAGE_CHARTER_APPROVED_DISPATCH_PENDING`（M2 三子门全闭已载），cursor 条款不变；其余章节不动。

### 四、下一步

- 尚书省依 charter D 节执行 D3 有界派发（serial_inline 承接）；工部 D4 RED 定界（receipt/provenance 缺失、非 current-tool fanout、Qoder 未 opt-in、candidate hash 后 mutator 改包、install 首字节前 fail closed、部分应用回滚、root 集合漂移）呈二次确认。
- 边界：本条仅构成 charter 裁定与记载，不构成 M3 实施授权；不安装、不同步活动根、不发布；未读 pending/** 正文。
- 红线复测：HEAD=48ddc910abc1829f04ac23f0430b55a1d3f0fea8 未动；child/根 staged=0；CHILD 递归 pyc/__pycache__=0。
- 本记载前缀一致性：写前 log 全文 SHA256=77f7ab0591f76e7054f09f60f813e981c6bbb627d8943f0171d21bb575d85507（972 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。

## Entry 0049 — M3 源码 acceptance RED/GREEN 闭合：R-PA1/R-PA2 转绿，E1-E8 聚焦验证全绿（任务 26）

- 日期：2026-08-07（Asia/Shanghai，约 10:45）。
- 事件：`M3_SOURCE_ACCEPTANCE_RED_GREEN_CLOSED`。
- 责任链：尚书省辖下工部（任务 ID=26）；宿主 child 通道按 Entry 0017 降级 serial_inline，责任主体=本任务执行者（serial_inline）；D7 刑部只读复查由本执行者 serial_inline 内联承接；本条 append-only 追加，仅属本 log 写集。
- 承接锚点：CHILD=`D:/project/worktrees/decretum-matrix/beta106-local-stage-019fb7f5`；branch=`release/beta1.0.6`；HEAD=`48ddc910abc1829f04ac23f0430b55a1d3f0fea8`（全程钉定）；唯一 cursor=`stage-4/semantic-lineage`；M3 charter=Entry 0048（六项裁决）；授权=用户「准」（2026-08-07 10:38）=RED 定界照准。

### 一、RED 病灶与 GREEN 修复（严格限于写集预授权 1/2 项，Python 一律 -B）

- **R-PA1（privacy 3 用例 FAIL）根因**：`package_skill.py` L305 `EXCLUDE_DIRS` 含 `web`，`should_skip` 整目录排除 `web/shiguan-tree`；而 install-projection manifest（Entry 0036/0037 修订，87E5D03C）已含 `web/shiguan-tree` 投影 → stage_validation `quick_validate.py` 报 `missing projected path: web/shiguan-tree`，导致 `check_package_privacy.py` 三个用例（test_legal_governance/test_two_clean_builds/test_late_competing_output）FAIL。
  - GREEN：新增 `EXCLUDE_DIRS_WEB_ALLOWLIST={shiguan-tree}`；`should_skip` 对 web 子树放行 `web` 与 `web/shiguan-tree`（父目录含 allowlist 子目录时放行，否则整棵 web 不被遍历），其余 web 子目录保持排除（白名单有界）。
- **R-PA2（portability 裸崩溃）根因**：`check_portability.py` `run()` finally 中 `os.environ.clear(); os.environ.update(original_env)` 在本机 env 超 Windows 32767 单变量上限时裸 `ValueError`。
  - GREEN：finally 恢复包 `try/except (ValueError, OSError)`，fail-closed 返回 `environment_restore_overflow:<type>:<msg>`，不吞没整份报告。
- **RED 断言固化**：`check_package_privacy.py` 新增 `test_web_shiguan_tree_is_packaged`（R-PA1a：web/shiguan-tree/index.html 在 zip 内）与 `test_web_allowlist_is_bounded_to_shiguan_tree`（R-PA1b：web/other-dir 仍排除、web/shiguan-tree 放行）。

### 二、转绿实测（cwd=CHILD，Python 一律 -B，PYTHONIOENCODING=utf-8）

- `python -B scripts/check_package_privacy.py` → **Ran 63 tests OK exit=0**（既有 3 失败用例转绿 + 新增 2 断言 PASS；含 web/shiguan-tree 打包与白名单有界验证）。
- `python -B scripts/check_portability.py` → exit=1（预期）、JSON 含 `environment_restore_overflow:ValueError:the environment variable is longer than 32767 characters`——fail-closed reason 在位，不再裸崩溃。

### 三、D6 聚焦验证 E1-E8（cwd=CHILD，-B，系统 Git 2.54 优先 PATH）

- 源码 acceptance 门：`check_release_legal.py` → `RELEASE_LEGAL PASSED` exit=0；`check_release_manifest.py` → `RELEASE_MANIFEST_OK`（steps=49）exit=0；`check_package_privacy.py` → 63 tests OK exit=0；`check_portability.py` → exit=1（fail-closed reason 在位，本机 env 体积限制，非功能缺陷）。
- 回归门：`check_semantic_continuity.py` → `SEMANTIC_BINDING_CORE_PASS`；`check_stage3_recovery_chain.py` → `COURT_STAGE3_RECOVERY_CHAIN=PASS`；`check_install_projection_closure.py` → `INSTALL_PROJECTION_TRANSITIVE_CLOSURE=PASS`；`check_unified_cli.py` → exit=0 零 FAIL/ERROR（M1/M2 无回归）。
- E7：`git diff --check` exit=0；HEAD=48ddc910 钉定；child/根 staged=0；CHILD 递归 pyc/__pycache__=0（详见四）。
- E8 确定性双跑（固定输入）：release_legal 两次一致=`3e5c1a7f…`；release_manifest 两次一致=`26eb466c…`。

### 四、pyc 违规自查披露与整改

- 本批探测 `should_skip` 时曾用 `python -c "import package_skill"`（漏带 -B），生成 `scripts/__pycache__/package_skill.cpython-314.pyc`——违反「Python 一律 -B」红线（与 Entry 0038 Bill 同款教训）。
- 已删除（python shutil.rmtree 绝对路径）并复测归零；记入本批传递项，后续探测一律带 -B。

### 五、停止门与边界

- F 停止门：均未触发（privacy/portability 修复面有界；未触碰安装根/活动副本/package 实际写入（仅 dry-run）；install-backups/** 零触碰；checker 保持 RETIRED source-only）。
- 边界：未 stage/commit/push/tag/PR/release/install/sync；未创建/切换 worktree/branch/mapping；未读 pending/** 正文；Python 一律 -B；HEAD 钉定未动。
- 待办：候选包构建（dry-run 至独立新目录）、diff 归属审查、真实安装（默认不做，受控通道待用户授权）。
- 本记载前缀一致性：写前 log 全文 SHA256=f107f21c2f52b908e11b6c98180dba327fec5fb1e932e350495d5e35096b108f（1005 行）；字节级 append-only 追加（UTF-8、CRLF），旧内容零位移。
