# beta1.1.3：职责清晰、接口简洁与长期可维护性

状态：本版源码改进和定向验证完成；最终同提交候选验收见控制根 release-staging/decretum-matrix/validation/beta1.1.3 下的回执。活动安装、真实宿主验收和发布仍分别核验。

## 版本主线与接受标准

用户最新明确要求：以下长期维护原则是 beta1.1.3 的更改方向。首批常量与校验复用属于这条主线的起点，不代表整个版本已经完成。

| 原则 | 本版落点 | 接受标准 |
| --- | --- | --- |
| 按职责组织 | 公共 API 与工作流／检索后端按调用职责连接 | 帮助和无状态边界说明在后端未加载时仍可使用 |
| 接口隐藏内部细节 | 调用者使用既有公开 API，不自行组装后端依赖 | 保持方法签名、返回契约及应有权限检查 |
| 数据与行为各有归属 | 预载限额归配置模块，排序归召回器 | 同一事实有唯一维护位置，输出与执行含义一致 |
| 只抽象相同规则 | 共享派遣结构与召回排序结果 | 公共预检查不取得完整准入权限；旧调用方式兼容 |
| 独立验证 | 后端隔离、结构负例、公开分数行为测试 | 用真实外部行为和独立预期发现回归，不由实现生成全部预期 |
| 展开后容易读懂 | 显式局部导入、具名结果和短适配函数 | 不引入反射式路由、Markdown 变量解释器或通用配置语言 |

每个后续切片必须对应已观察到的耦合、重复或错误，沿用本计划记录范围；不按行数机械拆分主运行时，也不以新增状态系统管理现有状态系统。

## 第二批职责收敛切片

1. 公共 API 当前在导入时同时加载 court_runtime 和 query_shiguan_index，导致帮助和无状态说明依赖无关后端。将加载推迟到实际需要该职责的函数，保留公开签名和输出。
2. GBrain 排序使用 select_matches，却用旧 score_entry 返回分数。由召回模块提供已排序的分数与记录，旧 select_matches 保留只返回记录的适配，GBrain 直接消费结果，不另算一套分数。
3. query_shiguan_index 中重复的脚本根路径初始化收敛为一次，保持直接 CLI 与兼容入口运行方式。
4. CLI 内嵌启动说明与已有 governing reference 重复维护；CLI 直接读取原启动文档，保留结构化帮助元数据，说明正文归回文档职责。
5. 先保留隔离失败和分数尺度错误的 RED，再验证两条查询路径、MCP、负面场景及源码预算。既有失败继续与基线区分。

## 范围和基线

- 用户要求：在下一版本分支优化可维护性，先处理已确认的重复事实和规则。
- 分支：`release/beta1.1.3`，基线 `1b73cb502ea4ed684583ea976691250b3cdc25b4`。
- 受管任务：`beta1.1.3-engineering-20260912`；通过根 repo-control 创建和挂载。
- 源码活动版本现为 beta1.1.3；根清单 accepted version 仍为 beta1.1.2。源码定版不自动改变已接受发布线。
- 安装 skill 完整读取：`C:/Users/32893/.agents/skills/decretum-matrix/SKILL.md`，以及 normal-startup 指引。该记录不替代其他任务自己的读取门禁。
- 本轮串行实施；不宣称真实官署并行或宿主验收。

## 首批具体改变

1. 将 20 KiB 入口／官署预载限额放在已有轻量 office configuration 模块；fastpath、native bridge 与 CLI guidance 复用。保留旧模块导出名称供既有调用者使用。
2. 在已有 dispatch policy 模块维护共用结构校验。公共预检查和完整校验复用角色、上级、必需文本、可见性、实例标识与依赖规则。
3. 完整入口继续独立要求可信预载、case reference、动作／停止条件和实例归属；结构通过不等于允许派遣。
4. 明确拒绝缺失 instance_key；畸形计划条目返回验证失败，不因预载校验先访问 .get 而泄漏 AttributeError。

## 保留边界

不改权限等级、层级、20 KiB 数值、正式状态机、回执身份、安装／发布实现；不新增模板引擎、全局常量杂项模块或哈希校验实现。旧分支及其未提交文档保留原状。

## 验证

- 先运行既有 dispatch policy 基线，再以新增回归保留缺失实例与畸形输入的失败对照。
- 同一批结构正反例覆盖公共和完整入口；可信预载不匹配仍被完整入口拒绝。
- 重跑 dispatch policy、MCP API、fastpath、native bridge、入口预载及源码预算相关检查。
- 通过既有 CLI 性能检查确认轻量配置引用没有扩大准备路径开销。
- 本轮只形成开发分支候选；不以源码检查代替安装、发布或真实宿主证据。

## 第一批完成记录

预载数值现由 `scripts/court_office_config.py` 的 `ENTRY_PRELOAD_BUDGET_BYTES` 唯一维护；CLI guidance、fastpath 和 native bridge 都报告 20,480 字节，旧导出名称保留为别名。启动文档引用 SKILL 中的可读规则，未引入 Markdown 变量解释器。

派遣结构校验归入原 `court_dispatch_policy.py`。`DispatchPlanProblem` 同时保存公共诊断码与原完整校验错误说明；两条入口使用同一套条件，完整入口保留额外的可信预载和实例契约。

RED 对照：新增用例在基线上因 `[None]` 条目触发完整预载校验 `.get` 的 AttributeError 而失败。修复后 13 组结构正反例在两个入口结果一致，畸形条目、缺失实例、重复实例和被篡改预载均正确拒绝。

| 检查 | 结果 |
| --- | --- |
| `python -B scripts/checks/check_court_dispatch_policy.py` | exit 0 |
| `python -B scripts/checks/check_court_mcp_server.py` | exit 0 |
| `python -B scripts/checks/check_court_open_fastpath.py --serial-probes --json` | exit 0 |
| `python -B scripts/checks/check_court_native_bridge.py` | exit 0 |
| `python -B scripts/checks/check_court_preload_semantics.py` | exit 0 |
| `python -B scripts/checks/check_startup_fastpath_contract.py --json` | exit 0 |
| `python -B scripts/checks/check_cli_performance.py --samples 3 --json` | exit 0 |
| `python -B scripts/checks/check_source_state_budget.py --json` | exit 0 |

CLI 性能检查仅为合成准备路径：cold P50=207.202 ms，warm P50=1.62 ms；不代表真实官署端到端性能。

源码预算：9298846 / 9,300,000 字节，hard_fail=[]；预算未放宽。

扩展多实例套件：基线 28 个失败，本轮 25 个失败，新增失败案例为 0，剩余失败详情与基线完全相同。3 个原先在前置阶段异常的拒绝案例现在通过。其余测试 fixture 缺少当前 case_ref，仍出现旧的 NoneType.get 异常；本轮没有改写这些历史测试以凑全绿。

工作区检查：旧检出及其未提交文档原样保留；新分支位于受管 task worktree，并挂载到 attached/decretum-matrix。根与子索引均为空。当前修改留在新分支工作副本；版本标识／发布清单尚未进入下一版定版投影，不能当作已发布 beta1.1.3。


## 第二批完成记录

- 公共 API 的三个既有验证入口保留显式签名，通过短适配函数加载实际验证后端；状态查询和史馆查询各自在需要时加载所属模块。
- 后端隔离测试先 RED：禁用 court_runtime 和 query_shiguan_index 后，导入公共 API 即失败。修复后帮助、memory-scan 边界说明与 archive dry-run 可用，仍声明不写入。实测普通导入中 court_runtime、query_shiguan_index、court_office_bootstrap 从已加载变为未加载。
- 召回分数测试先 RED：结构命中的结果 score 为零，不能解释排序。现在 rank_matches 在原召回模块中返回分数与记录；select_matches 保留原记录列表接口，GBrain 直接使用排序计算结果。空查询和非区分查询保留时间排序，分数明确为零。
- court open 的文本帮助读取现有 normal-startup 文档，结构化帮助仍保留原 schema 与字段；已验证公开 CLI 输出与文档正文一致。帮助内容更完整，消除的是重复维护，不宣称帮助输出 token 变少。
- 查询入口重复的脚本根初始化已收敛。没有增加模块文件、模板引擎、动态反射路由或第二套记忆存储。

| 检查 | 结果 |
| --- | --- |
| `python -B scripts/checks/check_court_dispatch_policy.py` | exit 0 |
| `python -B scripts/checks/check_court_mcp_server.py` | exit 0 |
| `python -B scripts/checks/check_shiguan_recall_precision.py --json` | exit 0 |
| `python -B scripts/checks/check_court_open_fastpath.py --serial-probes --json` | exit 0 |
| `python -B scripts/checks/check_court_native_bridge.py` | exit 0 |
| `python -B scripts/checks/check_court_preload_semantics.py` | exit 0 |
| `python -B scripts/checks/check_startup_fastpath_contract.py --json` | exit 0 |
| `python -B scripts/checks/check_cli_performance.py --samples 3 --json` | exit 0 |
| `python -B scripts/checks/check_source_state_budget.py --json` | exit 0 |

最终说明调整后的源码预算再次核对：9,299,092 / 9,300,000 字节，hard_fail=[]，未提高预算。原索引／权限／可信预载边界的相关回归继续通过；首批记录的扩展多实例基线问题仍单独保留，不能据这九项通过宣称全量验收。

## 后续切片的选择与版本完成条件

已完成的是预载／派遣规则复用、公开 API 依赖隔离、排序结果归属和帮助正文归位。还不能宣称工作流参数已整体简化或一万余行运行时已经完成职责拆分。

下一步先检查现有模板和状态读取能否自动提供调用者重复填写的修订、引用和准备信息；只对实际缺口收口。主运行时按真实变化原因选择一个完整职责迁出或收拢，不按行数机械分拆。任何六部／三省独立职责、授权和证据要求的变化都必须作为明确产品决定，不能由代码重构暗改。

版本完成以代表性任务的结果不退化、协议返工减少、知识可正确复用、负面权限场景仍被拒绝和文档／代码维护点清楚为依据。微基准、脚本行数和局部检查只能提供部分证据。继续在本分支及同一受管任务推进；保留原 worktree 的全部未提交内容。

## 第三批：恢复多实例回归的有效性

用户要求解释先前的 25 个失败并进行下一步。本批先解决这个妨碍验证的具体缺口；前两批修改保留。

### 失败性质与根因

`check_court_multi_instance_dispatch.py` 共 29 项，先前 4 项通过、25 项失败。25 项全部在 `_validate_trusted_preload_manifest → case_reference(None)` 提前触发 AttributeError，尚未执行到各自的业务断言，不能解释为 25 项产品功能已坏。

提交 `3ad2e35448983cf7044f8d0a1a00a6e29aaec8ff`（beta1.1.1）已经把这条派遣身份链从内容摘要迁移到 case_ref。该测试未同步：样本和可信预载均缺少 case_ref，还保留 6 个旧摘要字段反例。补齐引用后即有 23/29 通过，剩余恰好是这 6 个旧契约反例。

### 最小修复与边界

- 独立测试样本使用明确的 court_code 与 charter_revision；没有从被测实现生成期待值。
- 6 个旧反例迁移为：缺失引用、非对象引用、错误 court_code、过期修订、缺失修订、布尔修订。仍验证错误身份必须拒绝。
- 实现侧先验证引用类型，再调用已有 case_reference；缺失／畸形输入返回 exact_preload_contract_gate 的 ValueError，保留引用不一致拒绝，不再泄漏 AttributeError。
- 同时覆盖可信预载自身缺失 case_ref 的负面路径；结构检查通过仍不能代替可信预载。
- 该套件只验证元数据策略，删除不再参与断言的临时 profile/skill 文件和摘要生成。删除前后当前 29 项结果完全一致；其他安装／预载完整性检查不变。
- 不恢复已撤回的运行时摘要身份，不新增哈希校验实现，也不删除负面测试来制造通过。

当前结果：多实例 29/29 通过，本批 11 项相关回归全部 exit 0。这是离线策略测试结果，不是真实多代理宿主或发布验收。


### 第三批最终验证

| 检查 | 结果 |
| --- | --- |
| `python -B scripts/checks/check_court_multi_instance_dispatch.py` | exit 0 |
| `python -B scripts/checks/check_court_dispatch_policy.py` | exit 0 |
| `python -B scripts/checks/check_court_mcp_server.py` | exit 0 |
| `python -B scripts/checks/check_court_preload_semantics.py` | exit 0 |
| `python -B scripts/checks/check_runtime_no_file_rehash.py` | exit 0 |
| `python -B scripts/checks/check_court_native_bridge.py` | exit 0 |
| `python -B scripts/checks/check_startup_fastpath_contract.py --json` | exit 0 |
| `python -B scripts/checks/check_court_open_fastpath.py --serial-probes --json` | exit 0 |
| `python -B scripts/checks/check_shiguan_recall_precision.py --json` | exit 0 |
| `python -B scripts/checks/check_cli_performance.py --samples 3 --json` | exit 0 |
| `python -B scripts/checks/check_source_state_budget.py --json` | exit 0 |

最后追加的 fastpath 特征测试也通过：`current_references_derived`、`case_ref_drift`、`plan_drift` 均为 true。原有语义回执漂移拒绝测试继续通过。已确认现有准备路径会从当前任务回填业务引用和语义回执，显式旧引用仍拒绝，因此没有另写重复的回填接口。

最终源码预算：9298688 / 9,300,000 字节，hard_fail=[]；预算未放宽。此结果取代前两批“多实例仍有 25 个失败”的当前状态，旧段落保留为当时验证记录。

本批下一步的实际交付是：恢复多实例回归的有效性、修复业务引用错误的输入边界、精简无效测试 I/O，并确认及保护已有引用回填能力。尚未定版、安装、发布，改动仍在同一受管开发 worktree 中。


## 全仓复审记录（2026-09-12）

用户要求评估当前是否符合预期并找出下一方向。全仓 362 个 Python 文件 AST 解析、6 个 JS/MJS 文件语法检查通过；现有源码综合门禁首次 44 步中 39 通过、5 失败。

本助手上一批引入的两处兼容遗漏已做最小修复：启动文档恢复 CLI 约定标题；GBrain 保留 score_entry 的旧导出，新召回输出仍使用 rank_matches。完整 unified CLI 的 11 组、governance framework、召回和源码预算定向复测通过。三个剩余构建阻塞均来自尚未更新的 release payload 清单，本次未为审查刷新发布内容清单。

新发现的下一优先项：普通 apply_transition 的事件写入失败会留下已更新的任务和未更新的事件，临时故障注入已复现；相关函数 AST 与 HEAD 相同。另已复现配置固定日志目录时同名检查重跑覆盖前次日志。候选实现暂未修改，建议先收拢普通状态转换的事务处理，再处理检查执行与日志归属；有限事件查询读取完整历史的问题需做规模测量后再决定。

图文审查报告与原始检查输出保存在 OS 临时目录：architecture-review-decretum-20260912.html、architecture-review-decretum-20260912.md、decretum-beta113-full-review-20260912/。报告区分首次综合执行和修复后的定向复测，不声称整轮重跑通过或真实宿主验收。

当前方向与局部结果符合长期维护目标，整版仍未完成。后续涉及公开入口／导出调整时，验证范围应包含 unified CLI 和治理框架的既有消费者，不仅检查本模块的直接用例。

## 第四批：普通流程可靠性与本地候选收尾

最新授权：自主推进，使流程顺畅并形成可验证结果；不向真实运行链引入哈希校验脚本。本批继续串行、同一分支、同一计划；无外部发布或活动安装。

- 已复现 apply_transition 的半写状态；普通创建、转换、心跳、计划评审与官署生命周期现共用 `_commit_task_event`。在原有操作日志模块记录任务前后像与事件追加位置，异常回滚；进程中断后只读入口拒绝半成品，下一次取得运行锁的合法写操作恢复后重试。冲突时保留两份账册及恢复记录。
- 九段重复回滚被收拢。已有专用结诏、decree、结果恢复事务保持其原协议，不改任务编号、权限或回执身份。
- atomic_write_text 复用同一字节写入实现；恢复保留原始字节。事件追加及恢复使用 fsync，目录同步沿用已有平台尽力保证，不承诺物理断电绝不丢数据。
- 有限事件查询逆向按块读取，保留最新 N 条的原始顺序与任务筛选；完整历史逐行解析。没有新增索引、缓存服务或哈希计算。
- 重跑同名检查产生不同日志目录，前次回执继续指向原输出。
- 将规范检出已有的 CONTEXT、工程配置和文档／交接／决策索引整合到本分支，规范检出的脏状态原样保留；当前计划进入阅读入口，历史记录继续按原版本解释。
- 源码预算显式增加 20,000 字节（约 0.22%），只调整 portable_source.max_bytes 为 9,320,000，容纳恢复和故障测试及必要文档。文件数、模块行数、权限、隐私和安装门禁保持原上限。未新增运行模块或脚本文件。

定向验证已通过：完整 runtime 自测、并发 CLI 8 个写入者、任务写入失败、部分事件写入、两处进程退出、只读拒绝、外部冲突保护和重试；有限事件读取含跨块中文与畸形行。另通过运行预载无文件 rehash、版本元数据和源码预算。新增事务仅验证实际写入字节与事件尾部，不建立摘要身份或启动完整性扫描。

完整门禁、包与隔离入口结果在同任务最终验证记录中逐项登记；候选不冒充已安装或已发布版本。安装／包装需要的检查直接复用已有源码工具，不重新实现摘要算法或向活动 runtime 投影安装检查。

### 计划流程的补充断点修复

补跑原有 check_court_plan_artifacts 时，原提交与本分支均有相同 4 个失败：初始模板对空计划调用 plan_reference；状态投影读取计划不再持久保存的 plan_ref；绝对范围验证晚于编号分配；尚书测试样本缺少现行 case_ref。已保留原提交实跑对照，分别修复消费者、前置验证和样本，不放宽引用／层级约束。

深入执行还暴露重复评审重新分配 review_id 的断点。现在所有身份、阶段和回奏验证先执行；同一计划、决定及生产者重试保留原评审，变更决定生成新记录。状态投影在提交前准备，避免渲染异常发生在写入成功之后。现有计划套件加上重试／变更／空引用检查后 20/20 通过；标准绑定 7 项及完整结诏 21 项也通过。

该计划套件已加入原源码门禁及其独立必需步骤策略。初次综合执行已标 INTERRUPTED_FOR_CONFIRMED_REPAIR，保留先前日志，不视为通过；随后使用修复后的完整配置重新运行。文档接入中的本机路径归入 repository-only 领域配置，通用 AGENTS 通过它定位，移植性检查通过。本次一次调试导入产生的单个 pyc 已移到临时证据目录保留，源码生成物重新为零，后续调试入口统一带 -B。

事件查询实测：同一份 50,000 条、7,788,890 字节的本地样本读取最新 50 条，两版结果相同。各三次中位数为原版 219.057 ms、新版 0.813 ms；这是本机样本测量，非生产延迟承诺。

### 源码综合执行与候选验收定位

source-20260912-01 完整执行耗时 634.6 秒，45 步中 44 通过，唯一失败是增加计划用例后独立计数仍期待 44；全部选中的 required_checks 通过。随后同步独立计数，并将曾漏跑的多实例套件一并加入原门禁，最终源码步骤为 46，继续保留独立必需名称与步骤计数断言。旧失败报告不覆盖。

新增事务路径另经一次可丢弃调试：准备样本后禁用 hashlib 与 CRC 计算函数，Pending→Taizi、heartbeat 和有限事件查询仍成功；此监测不进入产品运行。启动说明中的旧 plan hash 字样修正为已有 plan reference。

最终候选由通过已知修复验证的干净本地提交构建，使用现有 npm local-install-candidate 流程在隔离 home/prefix 显式安装并实际调用公开 CLI，再由 pre-install 门禁对同一 ZIP、提交及执行回执核验。该门禁会完整重跑最终 46 个源码步骤；最终是否通过只读取其实际报告，不由本段声明。根清单与真实活动安装保持 beta1.1.2，未创建 tag 或外部发布。

首个候选提交 3fae048 的隔离安装与公开 CLI 已通过，但预安装门发现根入口预算 20,494 > 20,480 字节：本助手更正 plan hash 文案时扩展的说明造成 14 字节回归。保留该候选与失败输出，只精简该段重复说明，不调整预算或删除测试；修订候选使用新提交及独立目录重新验收，旧候选不视为接受。
