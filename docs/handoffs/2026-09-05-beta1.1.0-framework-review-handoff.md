# beta1.1.0 架构、复核与整改交接书

**状态：HANDOFF_ACCEPTED / GOAL_ACTIVE；全新会话已接收并以原生 goal 推进 1.1.0。**
用户已回复“准”，要求主线程 Astra ultra、子官署 Terra ultra、super 并行，理清后
以 `/goal` 推进；随后明确实施落在新版本 1.1.0。本指示覆盖此前“待复核／只读接收／
串行实施”的临时限制，真实活动安装与外部发布边界仍保留。

## 当前游标

`USER_APPROVED / TARGET_BETA1.1.0 / SUPER_PARALLEL_NATIVE / ASTRA_ULTRA_ROOT / TERRA_ULTRA_OFFICES / T00_CLARIFY_THEN_GOAL`

这是新一轮架构与健壮性审查，不恢复已暂停的 CCR-R2-SHIR-20260714-A02 历史执行。

## 用户已经确认的选择

1. 三省会审、六部办差是大步骤；实际必须对应具体官署履职，不合并成一个“三省代理”。
2. 正式非轻量任务中书、门下、尚书都履职，按依赖协作复议，不要求固定机械顺序。
3. 尚书按职责、依赖和证据价值选择必要六部。
4. 串行允许同一主线程显式代行，但分别记录各署责任、回奏与证据。
5. 原始架构核心方向不变时，MCP、谱系分类、三省细化等合理演进以新版本为准。
6. 必须是全新会话，不能 fork，也不能以分支会话代替。

7. 三省材料往返、六部主办／协办与横向会办，以及整改计划／任务安排均已批准。
8. 本次实施为 super 并行 native；主线程 `gpt-6-astra`、effort=`ultra`，全部子官署
   与工作子实例 `gpt-5.6-terra`、effort=`ultra`。产品 serial 模式仍允许显式代行，
   但它不是本次新会话的执行模式。
9. 在新版本 1.1.0 实施，按既有 court-beta 命名为 `beta1.1.0`；以 1.0.9 为基线，
   保留 1.0.8／1.0.9 发布身份，不在历史版本分支继续提交本轮修复。
10. 理清后调用原生 `create_goal`，不指定 token budget，按已授权范围持续推进，
    不停在只读接收或重复询问是否开始。

## 读取入口

先读以下文件，只有具体语义争议才展开历史原文：

1. [项目框架](../wiki/Architecture.md)，包含“三省协作与六部会办”。
2. [整改清单](../plans/2026-09-05-beta1.1.0-remediation-register.md)。
3. [计划书](../plans/2026-09-05-beta1.1.0-robustness-plan.md)。
4. [任务书](../plans/2026-09-05-beta1.1.0-robustness-task-book.md)。
5. [源码复核](../reports/2026-09-05-beta108-beta109-review.md)和
   [实际安装报告](../reports/2026-09-05-beta108-beta109-isolated-install.md)。
6. [机器证据摘要](../reports/beta109-review-20260905/evidence-summary.json)。

历史来源索引：[docs/architecture](../architecture/README.md)，五份 2026-07-12 原始 MD
已字节一致复制到来源子目录，原目录保留。

## 本机状态与禁止覆盖的工作

- 控制仓：`D:\project`，根分支 `codex/decretum-matrix/release/beta1.0.9`。
- 产品仓：`D:\project\decretum-matrix`，分支 `release/beta1.0.9`，
  HEAD `0003b3800cd245abf870a82ff464a802cc0faeaf`。
- 1.0.8 审查副本：`D:\project\.staging\dm108-review-20260905\decretum-matrix`，
  detached HEAD `37df40da5232f230cde578599c8030982188bbd7`。
- 1.0.9 安装审查副本：`D:\project\.staging\dm-install-audit-20260905\decretum-matrix`，
  detached HEAD `0003b3800cd245abf870a82ff464a802cc0faeaf`。
- 产品仓保留本会话未提交文档：README、PROVENANCE、开发手册索引、Wiki 架构／治理页，
  以及本次 docs/architecture、docs/reports、docs/plans、docs/handoffs 的新增材料。
  不 reset、stash 或覆盖这些文档来获得“干净”外观。交接时核实 root／child 索引为空。
- 当前 root workspace.yaml 是旧登记检出；产品位置已由用户明确目标和本地应用项目确认。
  若后续需要新工作树，先按 repo-control 管理映射，不直接创建第二个同分支 worktree。

## 验证事实

- 1.0.8 串行源码检查 32 项，27 通过、5 失败；另有 10 个边界探针（含对照）。
- 安装器 fixture 清理环境影响后，32 安装例、31 配置例通过，但真实 manifest 仍冲突。
- 两版未修改的生产 install_current_agent_copy 入口，plan/apply 均返回
  `REJECTED / projection_manifest_invalid / repository_only_overlap`。
- 冲突项：`scripts/install_codex_plugin_projection.py`。
- 两个独立 home 均未创建，targets 为空；未访问真实配置、未覆盖活动安装。
- 安装后冒烟为 `NOT_RUN_NO_INSTALLED_COPY`，不得写成安装成功。
- 1.0.8 预载通过，1.0.9 预载超限；其余问题与新版保留项见整改清单。

## 用户确认后的交接动作

用户确认已经收到，按以下动作执行：

1. 重新核对产品 HEAD、文档 diff、root／child 索引与隔离证据。
2. 通过 `create_thread` 创建项目本地的全新会话；不是 fork，不另开分支会话。
3. 创建时设置根模型 `gpt-6-astra`、thinking=`ultra`，初始消息携带精确游标与批准事实。
4. 接收者逐字回复 `HANDOFF_ACCEPTED`，并报告实际 HEAD、索引状态、三项选择和下一步。
5. 接收者理清后建立 goal 并继续实施；通过宿主支持的模型参数真实派遣 Terra ultra
   官署，`fork_turns=none`，按层级／互斥写集分工。Git 操作集中串行，正常树上限 16／深度 4。
6. 原会话核验接收与 goal 启动结果，记录真实 task id；未收到不得声称完成。

拟用初始消息：

```text
这是诏令矩阵 beta1.1.0 的全新本地会话交接，不是分支会话，也不是 fork。
用户已批准架构、整改计划及实施，要求 super 并行 native；根模型 gpt-6-astra/ultra，
全部子官署和工作子实例 gpt-5.6-terra/ultra，新派遣 fork_turns=none。先读取
D:\project\decretum-matrix\docs\handoffs\2026-09-05-beta1.1.0-framework-review-handoff.md
及其中的架构、整改清单、计划书、任务书和验证报告，按 T00 理清基线、依赖和三省职责。
保留未提交文档，不 reset/stash，不覆盖活动安装或配置，不外部发布。
核实 HEAD、根与子仓索引、批准事实后回复字面量 HANDOFF_ACCEPTED，随后调用原生
create_goal 建立 1.1.0 实施目标，不设 token budget，按批准的任务持续推进。
准备 1.1.0 开发状态须遵守 repo-control 路径规则；全新会话与新 Git 开发分支是不同概念。
不要因旧文档的待审／串行临时限制再次等待授权；实际宿主能力失败则给出具体证据。
```

## 交接回执

- destination_task_id：`01a0715e-8957-7910-9800-eb19ed5d1437`（local，全新会话，非 fork）。
- root_model：创建请求及实际 turn_context 均确认 `gpt-6-astra / ultra`。
- child_model_contract：`gpt-5.6-terra / ultra`；首批中书、门下、尚书实际 turn_context 均已核验，后续实例继续由接收任务核验。
- acceptance：已收到字面量 `HANDOFF_ACCEPTED`，基线 `0003b38`、根／子仓空索引及批准文档均已确认。
- goal_status：原生 `create_goal` 已成功返回 `status=active`，未设置 token budget，目标为 beta1.1.0 的 T01–T09 实施与验证。
- evidence：见 [handoff receipt](2026-09-05-beta1.1.0-handoff-receipt.json)。
- user_review：APPROVED，包含 super 并行、模型配置、goal 推进与新版本 1.1.0。
- next_task：T00 理清后建立 goal，准备 1.1.0 开发状态并按依赖并行实施。
